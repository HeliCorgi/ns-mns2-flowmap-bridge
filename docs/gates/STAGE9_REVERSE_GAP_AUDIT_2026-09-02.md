# STAGE9_REVERSE_GAP_AUDIT — 2026-09-02 — one-shot reverse-gap analysis of the parked BH/Γ-depletion branch and the K12 complex: 19 candidate estimates, counterexample adjudication, one selected theorem (RECORD-ONLY)

**Repo:** `c:/Users/corgi/Downloads/ns-mns2-flowmap-bridge` · **Status: RECORD-ONLY.** No frozen verdict moves, no operative-frontier change, no park change. Every map consequence in this record is a **proposal** for a future freeze review.

**Commissioning provenance.** Executed under the user's 2026-09-02 `stage9_reverse_gap_task_v2` instruction: a one-shot, explicitly sanctioned bounded analysis permitted to touch the PARKED BH/Γ-depletion branch and the K12 complex. The park itself is untouched; the branch's registered termination rule (end/pivot) has already fired and the pivot act remains with the user — nothing here reverses or pre-empts that.

**Method.** Sequential offices: (1) a plug-spec office read the Lean continuation interface verbatim from source; (2) candidate offices generated independently falsifiable quantitative estimates in three lanes — whole-space ladder (L_a–L_e), axisymmetric (L-AX1–7), K12 complex (KC-1–7 with KC-3 split a/b/c) — each with 7 fields; (3) a counterexample office ran the standing battery (scaling, frozen-B2 concentration rows, thin-shell/separatrix families, W★/C13 tuples, axis degeneracy, steady-Euler witnesses, counterprofiles u_τ/𝒱, failcase registry FC-077–087) **before** any proof search; (4) a routes/scores office produced proof routes and leverage × distance × Lean-connectability scores; (5) this adjudication resolved inter-office disagreements, independently re-verified the selected candidate's survival reasoning, and fixed the single selected theorem. Rules enforced throughout: RECORD-ONLY; ban on global-regularity restatements; C0 language discipline (no "unique closer"/"only route"/"necessary" absent proof); counterexample-before-proof; no new Lean infrastructure (lemma decompositions on paper only); claims tagged [H]/[V]/[C]/[D]/[V?]. Γ-lane candidates carry the closure facts of `docs/gates/GAMMA_OSC_FEASIBILITY_2026-09-02.md` (**VIOLATED** — known drift-machinery route closed) and `docs/gates/EXT_GAMMADEP_DECISION_2026-09-01.md` (**snapshot no-go**, UNDERDETERMINED) at full weight. All Lean anchors named below were re-greped this pass and both gate files confirmed present [V, this pass].

---

## SS-1 — The Lean plug spec (condensed)

All statements read verbatim from `.lean` source by the spec office [V]; anchor presence re-confirmed by grep this pass [V].

- **Carrier.** `R3HsVelocity 3` stores the Bessel coordinate g = J³U, J = (1−(2π)⁻²Δ)^{1/2}; the carrier norm **is** the Bessel-potential H³ norm of the decoded field U = J⁻³g under the repo Fourier convention (`Formal/R3SobolevCarrier.lean:12–28`, `:64` `besselPotential_r3HsToTempered_eq_coordinate`, `:76` `r3HsToTempered_memSobolev`). No two-sided paper-H³ ↔ carrier norm lemma exists in Lean; even ‖`r3H3ToL2Operator`‖ ≤ 1 is absent (`Formal/R3FiniteEnergy.lean:17`).
- **Mild predicate.** `IsR3EndpointSafeProjectedMildSolutionOn` (`Formal/R3EndpointSafeProjectedDuhamel.lean:156`; abstract `Formal/EndpointSafeTwoSpaceDuhamel.lean:607–613`): 0 ≤ T, continuity on Icc 0 T, u 0 = u0, Duhamel identity with **minus** sign and endpoint-safe zero extension (`R3EndpointSafeProjectedDuhamel.lean:56–72`).
- **Extension.** `r3EndpointSafeProjected_exists_extension_of_bounded` (`Formal/R3MildContinuation.lean:84`): carrier bound R on Icc ⇒ extension by `r3MildLifespan nu R` = (δ(R)/(1+(π√ν)⁻¹+δ(R)))², antitone (`:58`), δ(r) ~ c/r (`Formal/R3QuantitativeLifespan.lean:38–46`); explicit-lifespan solver + ball bound ‖u t‖ ≤ ‖u0‖+1 (`:193–205`).
- **Engine and dichotomy.** `r3EndpointSafeProjected_horizons_unbounded_of_uniform_bound` (`R3MildContinuation.lean:113`); `r3EndpointSafeProjected_blowup_dichotomy` (`:134`); Schwartz instantiation `r3AdmissibleSchwartzDatum_blowup_dichotomy` (`Formal/R3SchwartzInitialData.lean:279`). The escape branch quantifies **existentially** over certified solutions/times; no maximal trajectory or pointwise limsup object exists (scope guard `:22–25`).
- **Uniqueness / realness / semantics.** `r3EndpointSafeProjectedMildSolution_unique` (`Formal/EndpointSafeTwoSpaceUniqueness.lean:222`, no ball restriction) + `IsMildSolutionOn.mono`/`.restart`/`.concat`; unconditional coordinate realness `…isR3RealVelocity` (`:234`); decoded strong-L² NS semantics at interior times `r3EndpointSafeProjectedMild_navierStokes` (`Formal/R3NavierStokesEquation.lean:142–174`); Schwartz capstone `r3AdmissibleSchwartzDatum_navierStokes` (`R3SchwartzInitialData.lean:233`), decode∘encode = id on the Schwartz core (`:100`).
- **Plug form [D].** **L_bridge(Q,R):** if a physical quantity Q of the decoded field satisfies Q ≤ Q₀ on [0,T′] along any certified solution/horizon T′ ≤ T, then sup_{[0,T′]}‖u t‖_carrier ≤ R(T,ν,‖u0‖,Q₀) < ∞. §3 derivation: if horizons were BddAbove with S = sSup, instantiate at T = S+1, feed the fixed R* = R(S+1) as `hbound` — contradiction; uniqueness discharges the ∀-solutions quantifier (a bound along one solution per horizon suffices). Lean-absent bridge layers any candidate needs (paper-only): **B1** energy/commutator ladder + Grönwall; **B2** encode–decode norm transport; **B3** Leray compatibility; **B4** realness transport; **B5** Icc endpoint bookkeeping. Banned degenerate endpoint: Q = the carrier norm itself (L_e below). Mismatches: complex carrier; interior-time-only differential semantics; local horizons only; fixed Fourier/Leray convention; minus-sign Duhamel; no energy inequality in Lean (Stage-9 stop rule forbids building one).

---

## SS-2 — Reverse-gap candidate table

Verdicts by the counterexample office, upheld in adjudication. **This pass: 0 REJECTED · 1 ILL-POSED/BANNED · 18 SURVIVE** (two with statement-fixing defects flagged, one with an under-specification, one at high falsity risk pending a completion target). "S" = SURVIVES COUNTEREXAMPLE SEARCH. All axisym/K12 solutions-only statements are untouched by snapshots (u_τ/𝒱 solve no PDE; W★/C13 are exponent tuples, not flows) — the recurring survival mechanism, applied with the verdict-semantics guard (VIOLATED ⇏ negation).

### Ladder lane (whole-space; no Γ-gate distance)

| ID | Estimate (falsifiable form) [D] | Bridge → plug | Status | Crit. | Key extra estimates | Stress / distance | Verdict |
|---|---|---|---|---|---|---|---|
| **L_a** | ∫₀^{T′}‖∇U‖_{L^∞}dt ≤ G(T;ν,‖u0‖) over certified horizons T′ ≤ T | BKM (13)/(14) + Grönwall + B2–B5; **7–9 paper lemmas** (shortest); R = ‖u0‖e^{C·Q₀} [V office: Beale–Kato–Majda, Commun. Math. Phys. 94 (1984) 61–66, incl. NS remark p.62; Kato–Ponce, CPAM 41 (1988) 891–907 [H]] | bridge KNOWN; estimate OPEN, Clay-equivalent-or-harder | critical | KP constant at s=3 under repo J; B2 convention constants; H³↪C¹ non-banned witness | exactly scale-invariant; Type-I profiles log-diverge it — correct falsification polarity; FC-086 binds proofs only | **S** |
| **L_b** | ∫₀^{T′}‖ω‖_{L^∞}dt ≤ G(T) | BKM theorem + log-inequality (15) + L² vorticity (8)–(10) + log-Grönwall; 9–12 lemmas; R double-exponential [V office, BKM verbatim; BMO refinement Kozono–Taniuchi, Math. Z. 235 (2000) [H]] | bridge KNOWN; estimate OPEN Clay-equiv. | critical | BKM(15) constant; curl-decode multiplier; NS viscous sign check | strictly weaker hypothesis than L_a (strain-dominated scenarios falsify L_a, spare L_b); log⁺ self-reference = formalization coupling hazard, not a gap | **S** |
| **L_c** | sup_{[0,T′]}‖U‖_{L³} ≤ G(T) | ESS (Russian Math. Surveys 58:2 (2003)) qualitative; Tao arXiv:1908.04958 quantitative → reduces to L_a chain; honest mass = L_a chain + Tao corpus (~50–100 lemmas) | bridge KNOWN; estimate OPEN — the critical-norm Clay wall | critical | Tao O(1) exponents (cf. Palasek ARMA 2021); t₀ splice with the already-formal ball bound | self-similar L³ profiles excluded (Nečas–Růžička–Šverák 1996 [H]); blow-up forces ‖u‖_{L³}→∞ (Seregin CMP 312 (2012)) at logloglog rate — numerics-invisible | **S** |
| **L_d** | ‖U‖_{L^q_tL^p_x} ≤ G(T) per pair, 2/q+3/p ≤ 1 | Prodi (1959)/Serrin (1962)/Ladyzhenskaya/Giga JDE 62 (1986)/Struwe CPAM 41 (1988) energy+slab bridges [H]; ~8–12 lemmas reusing L_a's chain; (3,∞) endpoint = L_c | bridges KNOWN (1959–1988); every pair OPEN Clay-equiv. | mixed | GN/Young constants; slab count e^{C_ν Q₀^q}; Leray floors c_p | most numerics-friendly falsification targets: Leray rate floors (T*−t)^{−(1−3/p)/2}; supercriticality gap 3/2→1 untouched (FC-087) | **S** |
| **L_e** | Q = sup_t‖u t‖_carrier itself | zero lemmas — it **is** `hbound` verbatim (`R3MildContinuation.lean:84,:113`) [V] | degenerate: the conclusion restated | subcritical | (as TARGET only) B2 transport constants; absent decoder bound | correct roles, both preserved: (i) **pinned endpoint** — never re-proposable as "new"; (ii) the **target metric** for all bridge distances (L_a 7–9 · L_b 9–12 · L_d 8–12+L_a · L_c +Tao corpus · AX 10–12 layers); escape-branch semantics note: "carrier norm blows up" is not itself a certified statement | **BANNED** (rule 2; ban check precedes battery) |

### Axisymmetric lane (all solutions-only; all carry the escalated no-go [V, EXT §B2.2]: any proof must consume time evolution; Γ-typed rows carry the VIOLATED closure at full weight)

| ID | Estimate | Bridge → plug | Status | Crit. | Key extra estimates | Stress / distance | Verdict |
|---|---|---|---|---|---|---|---|
| **L-AX1** | sup_{r≤δ₀}\|Γ\|(ln 1/r)² ≤ Q₀ (Wei 3/2-variant carries [V?] constant normalization) | Lei–Zhang, Pacific J. Math. 289 (2017) [V abstract-exact]; qualitative conclusion suffices; **AX-STACK 10 layers** (B1–B6 + AX1 equivariance, AX2 smoothing, AX3 cylindrical reduction, AX4 gluing) | bridge KNOWN; estimate OPEN (no unconditional near-axis Γ-decay known) | critical | (δ₀,Q₀) τ-uniformity is the entire content | u_τ/𝒱 violate at r ≍ τ^β — snapshots only, no rejection; frozen rows do **not** locate Γ-saturation (BH_BETAV pinning), so not forced-violated in frozen B2; VIOLATED distance full | **S** |
| **L-AX2** | sup_x r\|u\| ≤ Q₀ | CSTY-I (IMRN 2008) / KNSS (Acta Math. 203 (2009)); same 10-layer stack; genuinely load-bearing (no L^∞ near axis) | bridge KNOWN; estimate OPEN — the recognized axisym critical gap (best unconditional: √\|ln r\|/r², a full power above) | critical | Γ-max discharge of swirl part; C⁰ decode semantics | exactly **anti-B2**: violation forced class-wide at τ^{α−γ} (K4 + forced-amplitude [V corpus]); live falsification territory = Type II; scale-invariance floor (SSSZ/SVZ) bars generic-drift proofs | **S** |
| **L-AX3** | \|U\| ≤ Q₀(r²+(T−t))^{−1/2}; **separate form ‖U(t)‖_∞ ≤ Q₀(T−t)^{−1/2} adjudicated primary** | CSTY-II (Comm. PDE 34 (2009)); 10 layers; T-weight bookkeeping non-circular [D checked] | bridge KNOWN; estimate OPEN | critical | reference-time bookkeeping; far-field rider only in combined form | exact complement of frozen B2 (K2: √τ‖u‖_∞ ≍ τ^{1/2−γ}); measured γ > 1/2 with validated error bars = the falsification event; strictly stronger than integral Type-I conditions (spikes fenced) | **S** |
| **L-AX4** | ‖r^d u_θ‖_{L^q_tL^p_x} ≤ Q₀, 0 ≤ d < 1, 2/q+3/p ≤ 1−d | Chen–Fang–Zhang (DCDS 37 (2017), arXiv:1505.00905); 10 layers + time-norm concatenation lemma across gluing points | bridge KNOWN; every member OPEN; d=1 boundary unconditional (Γ-max) yet outside range | mixed | (d,p,q) margin; d→1 vacuity/plausibility tension (honest, C0-fenced) | **is** a Γ-decay family in disguise (Γ ≤ Q₀r^{1−d}): VIOLATED + Wu Thm 1.8 bear on all known machinery; u_τ defeats endpoint form for all d < 1 (K9) — snapshot only; any member proved kills the whole shrinking-saturation branch | **S** |
| **L-AX5** | axis cylinders: R^{−1/2}‖U‖_{L^{10/3}(Q(R))} ≤ Q₀ (lnln-growing literal variant weaker; f-component [V?]) | Seregin, JMFM 24 (2022) no. 18, Thm 1.3 [V verbatim, DR-2] + CKN circle argument; **12 layers** (widest; suitable-class entry) | bridge KNOWN (M-part verbatim-verified); estimate OPEN — weakest known-theorem-bridged axisym hypothesis | critical | Seregin f-prefactor [V?] quarantined before constants a_S ≤ 1/224 consumed; R₀, Q₀ | = rung AX-L7 read in reverse; frozen B2 forces power-rate violation on all S_blob — power-vs-lnln gap separates cleanly; hardest to falsify numerically, most economical to prove if any | **S** |
| **L-AX6** | (Γ-DEP)_fld: ∃c_*<c₀, δ, τ₀: sup_{r≤δτ^α}\|Γ(·,T*−τ)\| < c_*Γ₀ for every frozen-B2 solution | **no plug shape**; 12+ layers + AX5 (B2 semantics absent in Lean) + AX6 (scenario exhaustion — UNPROVED, C0 bars asserting it); corpus chain [V]: (Γ-OSC)_op ⇒ (Γ-DEP)_fld ⇒ β_v ≤ α ⇒ middle limb closed | OPEN; snapshot no-go (UNDERDETERMINED) + machinery VIOLATED both carried; VIOLATED ⇏ ¬(Γ-DEP)_fld | critical | §5.3 structure register (i)–(vi); K12/K12″ reuse barred (EXT §A.5) | field-level violated by u_τ — **not a solution** (promotion is the open core); vacuity hazard if B2 empty; ordering [D]: plug-shaped progress strictly dominates its value | **S** |
| **L-AX7** | (Γ-OSC)_op: τ-uniform oscillation contraction (θ,q) on axis parabolic cylinders at core scale | one level below L-AX6; purely parabolic, modular; same 12+ absent layers | OPEN; the VIOLATED ruling is literally about this object (all known-sufficient rungs defeated; ladder floor at scale invariance SSSZ JDE 252 (2012)/SVZ ARMA 207 (2013)/Wu JDE (2024); multi-scale closed at (ln N)^{−p}, p ≤ 1 vs known exp(−N^{O(1)}), arXiv:2210.10030) | supercritical | (ln N)^{−p} re-opening threshold; OOV-a Osgood face | contraction must hold against forced-supercritical drift ν^{−1}τ^{α−γ}; Wu refutes only the bare-drift analogue; §B2.4 standing test-case rule binds any proof claim | **S** |

### K12 lane (steady-Euler / chain objects; no Lean interface exists for any [V corpus])

| ID | Statement | Chain role | Status | Crit. | Key extra estimates | Stress / distance | Verdict |
|---|---|---|---|---|---|---|---|
| **KC-1** | s(u) ≥ c·δ_geo(u)^θ, θ ∈ [1,4), on A_NS, **Scope B** (no p = p(ψ)) | terminal rigidity of CHAIN-K12: kills the no-swirl-reduced profile conditionally ([C] viscous premise unpromoted) | Scope-A proved (θ=1 sharp, c=1/157 [H]); Scope-B OPEN; Jiu–Xin CMP 287 (2009) second-hand (B8); no quantitative small-swirl Liouville in literature (re-checked) | critical | Scope-B orbit-measure substitute; s_level ≤ Cδ debt; Jiu–Xin hypothesis class first-hand | Hill outside A_NS (kills per-level routes only); **NEW this pass**: ring-corridor family BLOCKED by the [H] identity ∫u_r²+∫u_θ² = 2∫u_z² (forces s ≈ 1/2 at far-field isotropy); s→0 requires critical oblateness, only known realization axis-grazing — ON the bound | **S** |
| **KC-2** | collapsing-core scalar PB: osc_J⟨Γ⟩_J ≤ CΓ₀e^{−cλ₀N(τ)} + (layer remainder), N(τ)=∫ν s^{−2α}ds → ∞ for α ≥ 1/2 | CHAIN-K12 engine; + KC-3a/b/c ⇒ (Γ-DEP)_fld on the α ≥ 1/2 quasi-static sub-wedge ⇒ β_v ≤ α (EXT §B.6) | OPEN; rigorous PB is 2-D steady (arXiv:2111.03996; Adv. Math. 2024) + 2-D quasi-periodic only [V re-confirmed]; genuinely dynamical — survives the no-go by consuming streamline geometry + accumulated time | supercritical | λ₁ never computed for any profile; χ ≥ 1; layer width vs T̂; Kirchhoff data; adiabatic error | **Wu arXiv:2106.02408 Thm 1.8 exclusion = mandatory unresolved pre-proof obligation**; remainder unquantified (EP-5); inert on α < 1/2 (W★ untouched); never class-wide | **S** |
| **KC-3a** | nested-foliation bulk hypothesis (energy fraction ≥ 1−η, τ-uniform) | class membership for KC-2's volume-label reduction; gates escape E7 | OPEN — **highest falsity risk of the register** | subcritical | cell count vs τ; η; ω-cutoff Scope-B adversary | **near-miss** stacked counter-rotating cell family (N = τ^{−ε}; passes K2/K5a/K5b (2ε < α−2γ+1 via A7-strict)/K6/K11; violates the bulk clause); incomplete at (i) matched compactly-supported steadiness, (ii) VIOLATED-grade row table → EP-7 completion target | **S** (incomplete refutation) |
| **KC-3b** | τ-uniform gap λ₀ for ∂_J(Â²χ∂_J·) + isoperimetric A³ ≥ c·min(V₁,V₂)² | rate-maker for KC-2's exponent; α = 1/2 grey-line quantifier | OPEN; isoperimetric [C] identified as the standard relative isoperimetric inequality in O(1)-aspect John domains (upgrade path to known math) | subcritical | Hill-cell λ₁ (natural first probe); axis-corner −(1/r)∂_r degeneracy (blocking caveat, unresolved) | **printed middle link vacuous** (inf_J Â² = 0 on any nested foliation) — EP-1; dumbbell adversary blocked single-cell, deliberate two-cell bottleneck untested | **S** (defect flagged) |
| **KC-3c** | axis-topology Dirichlet switch: outer energy leaf = axis level; ⟨Γ⟩ boundary trace → 0 | the depletion-vs-constancy switch (audit §2.3 repair); KC-2+KC-3c ⇒ operative (Γ-DEP)_fld | OPEN as τ-uniform statement; fixed-time ingredients [H] in-corpus (C¹ ⇒ Γ = 0 on axis-touching level, series-free) | subcritical | ρ(τ) trace rate; η-fraction tolerance; C¹ threshold (DVEP warning real) | u_τ/𝒱 **satisfy** it (Hill topology; [D recomputed]) — snapshots stress nothing, as a switch should behave; printed falsifier attacks the KC-3a∧KC-3c conjunction, not KC-3c — EP-4; F-2 honored (no σ>α consumption) | **S** |
| **KC-4** | Gavrilov/CLV shell continuation to the Hill-type separatrix; σ(A) = c√(A_H−A) | sharpness/negative control: settles (★) (inf_{A_NS} s = 0), pins θ = 1; deliberately **not** branch-deciding | OPEN; most classically tractable of the register (ODE-flavored; both primary texts read in full in-corpus; no interior obstruction [H] — all difficulty at the degenerate endpoint) | subcritical | **[C-num] constants (0.82; σ(A) expansion) consumption-BARRED** until re-derived with corrected k = −P′/2 = (A′−B′)/2; R-2 [V?] discharged | failure is **kill-favorable** (reopens inf s > 0) — unusual polarity; Hill shows the topology is not forbidden, proves nothing about the localizable branch | **S** |
| **KC-5** | Scope-B existence: ∃? nontrivial u ∈ A_NS with u·∇p ≢ 0 (NO-form: D(u) = ‖u·∇\|u\|²‖_{L¹} forced 0 — eikonal overdetermination) | scope gate: NO ⇒ Scope-A machinery unconditional on the localized class; YES ⇒ Scope-A kills void, trigger (T-c) | OPEN both directions; forcing direction empty in 3-D (CLV arXiv:1903.11699, Peralta-Salas–Slobodeanu, Sato–Abe all ASSUME localizability); DVEP arXiv:2005.04380 asserted-witness-only, outside A_NS | critical | (L) relaxation bookkeeping; C¹ jump-death threshold; quantitative NO-stability | Hill blocks Bernoulli-only proof routes ([H] twice-derived: B = B(ψ) ⇏ p = p(ψ)); **CAP-adjacent on the YES side** — posture literature-watch, endorsed unchanged | **S** |
| **KC-6** | nonlinear ζ = ω_θ/r PB (O(1) feedback): osc_J⟨ζ⟩ homogenizes; B′ → const on core foliation; closes escape E6 on α ≥ 1/2 | narrows route (P) — the audit's **generic** escape (slab [H]: compact support forces Osgood failure) | OPEN — hardest of the register; no rigorous 3-D nonlinear PB anywhere [V re-confirmed] | supercritical | corrected r³-weight gap λ₀′ (F-3: printed "same operator, same gap" reduced equation unusable as-is); axis-segment flux defect; partial-homogenization sufficiency **now load-bearing** (EP-6) | dead-core template undecided (steady, never shown dynamically persistent); **its Osgood singularity sits AT the free boundary — possible scope mismatch with the "core foliation" conclusion (EP-6)**; Wu analogue check mandatory (ζ-operator has opposite-sign drift term, clean maximum principle) | **S** |
| **KC-7** | separatrix-sliver no-feedback at α = 1/2: admissible-width sliver ⇒ ‖ε²F̂F̂′‖_{H^{−1}} ≲ τ^{2(γ−α)}w^{−1/2}·polylog → 0 | upgrades K12′ from {α > 1/2} to {α ≥ 1/2}; fixes the residue geography {1/2 < γ < 3/4, max(1−γ,2γ/3) < α < 1/2} | OPEN; literature empty on circulation sheets over collapsing separatrices | mixed | **width-floor normalization (EP-3 — vacuity risk on both readings)**; H^{−1} vs L² fixed by the poloidal corrector problem; T̂ powers for Hill-type separatrices | 2-D local maximization never run (1-D reduction invalid in the layer — recorded failure); no Θ(1)-feedback witness anywhere; OOV-2/3 log-vocabulary discipline applies | **S** (defect flagged) |

---

## SS-3 — K12-complex re-verification

**Chain shape (current, re-verified [V corpus] + this pass).** CHAIN-K12 = KC-2 engine + KC-3a (membership) + KC-3b (rate) + KC-3c (Dirichlet switch) ⇒ (Γ-DEP)_fld on the α ≥ 1/2 quasi-static sub-wedge ⇒ β_v ≤ α ⇒ middle limb closed **there**; KC-6 narrows route (P); KC-7 extends route-(F) closure to the α = 1/2 line; KC-1 is the terminal rigidity vs the surviving no-swirl-reduced profile (branch elimination only **with** the unpromoted [C] sub-viscous erasure premise); KC-4 pins sharpness ((★), θ = 1); KC-5 gates scope. **Every link is OPEN; the chain is conditional, sub-wedge-scoped, and nowhere class-wide** (α < 1/2 untouched — W★ residue; γ > 3/4 empties the interior fibre only if the KC-2 sub-chain lands). Consistency items re-confirmed this pass: the [H] identity ∫u_r² + ∫u_θ² = 2∫u_z² (now load-bearing in KC-1's new ring-corridor block); audit §7.1 advection cancellation in the r dr dz label [H]; χ ≥ 1 accelerates (corrected direction); A7-strict consumed correctly in the KC-3a near-miss arithmetic; F-1 geography (α = 1/2 meets the wedge for γ ∈ (1/2,3/4) open) carried; F-2 honored everywhere (conclusions only in (Γ-DEP)_fld form); K12/K12″ reuse bar (EXT §A.5) upheld; verdict-semantics guard applied throughout.

**Erratum / repair PROPOSALS (record-only; adoption requires a future freeze review; none moves a frozen verdict):**

- **EP-1 (NEW; KC-3b).** The printed middle link λ₁ ≥ c·(inf_J Â²)/J_max² ≥ λ₀ is **vacuous**: on any nested foliation the streamsurfaces shrink onto the elliptic centre, so inf_J Â² = 0 identically. Propose restatement with endpoint-weight normalization (Jacobi-type disk weights); simultaneously propose upgrading the isoperimetric step from [C] via the standard relative isoperimetric inequality in O(1)-aspect John domains. Adjudicator re-verified the vacuity argument [D].
- **EP-2 (CARRIED, confirmed; F-3 / KC-6).** ζ = ω_θ/r solves the **5-D-Laplacian** equation (+(2/r)∂_r, meridional weight r³), not Γ's operator; the printed "same operator, same gap" reduced equation is unusable as-is; λ₀′ must be re-derived before any consumption.
- **EP-3 (NEW; KC-7).** The printed sliver width floor w(τ) ≥ c(ν²ln(1/τ))^{1/2} is frame/units-ambiguous and on **either** reading empties the hypothesis window for small τ (rescaled: exceeds 1; physical: exceeds the core scale τ^{1/2}) — vacuous truth — and collides with the recorded "admissible at α = 1/2" status. Adjudicator re-checked the arithmetic at α = 1/2 [D]. Statement-fixing debt; OOV log-face discipline applies.
- **EP-4 (NEW; KC-3c).** The printed falsifier (off-axis vortex-ring sub-core with insulated Γ-plateau) violates KC-3a's nestedness, so it attacks the KC-3a∧KC-3c **conjunction**, not KC-3c within its stated class. Propose re-pointing the falsifier or restating KC-3c unconditionally.
- **EP-5 (NEW; KC-2).** The "(separatrix-layer remainder)" term is unquantified — an under-specification making the statement harder both to falsify and to consume; fix at statement level (KC-7/KC-3b territory; the 1-D reduction is invalid in the layer).
- **EP-6 (NEW; KC-6).** Scope-mismatch risk: the C^∞ dead-core template's Osgood singularity sits at the free boundary, plausibly **inside** the layer the conclusion excludes — KC-6 as stated could be true yet fail to close E6 at the boundary. The "partial homogenization excludes the Osgood endpoint" item is hereby flagged **load-bearing for the chain**, not cosmetic.
- **EP-7 (NEW; KC-3a).** The stacked counter-rotating cell near-miss (Hicks/Moffatt multi-lobe, N(τ) = τ^{−ε}) is the designated completion target: PROPOSAL to route KC-3a to the stress office (complete matched steadiness + full VIOLATED-grade row table) **before** any KC-2-chain proof search.
- **EP-8 (CARRIED, re-affirmed; KC-4).** [C-num] constants (0.82; σ(A) expansion) remain consumption-barred until re-derived symbolically with the corrected k = −P′/2 = (A′−B′)/2 and the R-2 [V?] discharged (one [C-num] decision in this corpus was already refuted).

**Past-record error count:** two genuine printed-statement defects found this pass (EP-1, EP-3), one falsifier mispointing (EP-4), one scope-mismatch risk (EP-6), plus confirmations of already-flagged items (F-3/EP-2, EP-8). No frozen verdict is implicated by any of them.

---

## SS-4 — Survivors and proof routes

**Survivors: 18 of 19** (all but L_e). Routes generated by the routes office for the 14 plug-shaped or chain-load-bearing objects; condensed (strategy — wall):

- **L_a:** R1 Tao-quantitative descent from L_c (open-to-open; records the ordering L_c ⇒ L_a). R2 Constantin–Fefferman direction-depletion (Indiana Univ. Math. J. 42 (1993)) — no a priori coherence modulus exists. R3 new monotone scale-critical quantity — barred generically by Tao's averaged-NS obstruction (arXiv:1402.0290; FC-086). R4 Constantin–Iyer stochastic representation (CPAM 61 (2008)) — circular in the target quantity.
- **L_b:** R1 Deng–Hou–Yu localized line-geometry (CPDE 30 (2005)) — hypotheses are what candidates marginally satisfy. R2 Biot–Savart stretching-kernel cancellation — defeated by strain-aligned configurations. R3 via L_c — inherits its wall. R4 Kozono–Taniuchi BMO endpoint — supercriticality untouched.
- **L_c:** R1 rescale + ancient-solution Liouville — the needed Liouville theorem is open (known only 2-D and axisymmetric-under-caps, KNSS). R2 Carleman amplification of Tao's scheme — cascade quantified, not excluded. R3 critical-element method (Kenig–Koch) — same open Liouville at the rigidity step. R4 axisymmetric restriction first — same scale-invariance floor; would consume D5(i).
- **L_d:** R1 intermittency/CKN descent of the interpolation exponent — fifty years, zero movement. R2 pressure cancellation — survives averaging (FC-086). R3 endpoint-first — inherits L_c.
- **L-AX1:** R1 self-consistent coupled drift (D5(i): the drift's swirl IS Γ) — no self-coupling theorem; sign unverified (DR-4). R2 pre-T* axis Dirichlet trace propagation (D5(ii)) — lives or dies on whether Wu's construction can carry the trace [V?]. R3 KC-2/K12 accumulated-time chain (D5(i)/(iv)) — KC-2 open, core-scale only. R4 critical-index contraction (D5(vi)) — exponentially beyond known constants.
- **L-AX2:** R1 growth-hypothesis Liouville — circular as known. R2 one-power improvement of the √|ln r|/r² line — dies on the SSSZ/SVZ scale-invariance floor. R3 axis-expulsion sign structure (D5(iii)) — the unsigned 2u_r/r is the recorded obstruction.
- **L-AX3:** R1 epochs-pigeonhole sharpened to all times — defeated by measure-zero spikes. R2 Type-II rescaling to steady Euler + KC-1 rigidity — compactness unproved, premise [C]. R3 exponent squeeze (D5(v)) — barred at snapshot level by the escalated no-go.
- **L-AX4:** R1 Hölder-modulus interpolation at d→1 — through the closed ladder; needs D5(ii)/(vi). R2 KC-2 depletion upgraded across dyadic scales — sub-wedge only. R3 weighted energy bootstrap — exponent count stays supercritical for every d < 1 [D].
- **L-AX5:** R1 axis-weighted local energy for the R^{1/2} decay — the supercritical gap in local form; swirl fails the naive computation at the axis. R2 CKN ε-regularity as a priori — pigeonholing gives subsequences of scales, not the cap. R3 inheritance from L-AX2/AX3 — open-to-open; but confirms L-AX5 as the weakest target.
- **L-AX6:** R1 via L-AX7; R2 via KC-2+KC-3a/b/c (three open links; KC-3a falsity risk); R3 direct boundary-Harnack with trace (D5(ii), Wu residual); R4 vacuous-truth via anti-B2 caps (full-strength open problems).
- **L-AX7:** R1 (ln N)^{−p} critical-index contraction — fundamental advance required. R2 coupled-pair contraction (D5(i)) — no theorem; §B2.4 rule binds. R3 level-set-aligned transverse diffusion (D5(iv) = KC-2 in oscillation form) — KC-3a risk transfers. R4 Osgood-intermediate modulus — out-of-vocabulary face (OOV-a).
- **KC-1:** R1 global integral-identity method (the [H] identity + oblateness-deficit inequality) — missing Scope-B orbit-measure substitute. R2 first-hand Jiu–Xin quantification — hypothesis class unverified; ineffective constants. R3 variational vortex-ring rigidity — A_NS far exceeds the variational class. R4 KC-4-informed extremal matching — no monotone deformation exists.
- **KC-2:** R1 action-angle + adiabatic invariance + Taylor dispersion — layer remainder load-bearing (EP-5); Wu exclusion mandatory. R2 spectral-gap adiabatic semigroup — KC-3b unanchored (EP-1; λ₁ never computed). R3 lift of 2-D PB proofs — moving separatrix is the new hard analysis.
- **KC-4:** R1 singular-endpoint shooting (Frobenius at the degenerate axis-touching endpoint). R2 Crandall–Rabinowitz branch continuation in weighted spaces — setup nonexistent. R3 Gavrilov-method extension to a critical level — genuinely different construction. (Failure kill-favorable.)

**Unrouted six — adjudication.** The routes office scoped out KC-3a/b/c, KC-5, KC-6, KC-7; upheld with reasons: KC-3a's completion target (EP-7) and KC-3b/KC-7's statement defects (EP-1/EP-3) mean **counterexample/statement-fixing precedes proof search** per rule 4; KC-3c's route content lives inside KC-2's R1/R2; KC-5 stays literature-watch (CAP-adjacent YES side, unbounded cost); KC-6's routes are premature until λ₀′ (EP-2) and the boundary-scope question (EP-6) are fixed.

**Office disagreements, resolved explicitly:** (1) L-AX3 primary form — verdict office's recommendation adopted: the **separate form** ‖U(t)‖_∞ ≤ Q₀(T−t)^{−1/2} is primary in any future proposal (drops the far-field rider). (2) KC-3b middle link — verdict office upheld over the printed candidate (EP-1). (3) KC-3c falsifier — verdict office upheld (EP-4). (4) KC-7 floor — verdict office upheld (EP-3). (5) Ranking scope gap — adjudicator-assigned [ADJ] scores added below for completeness. (6) L_b vs L_d tie at 40 — broken for L_b (single canonical statement; clean hypothesis-strength ordering vs L_a) over the graded family. (7) L_e — offices concordant (banned; both recorded roles preserved). (8) KC-2's Wu-exclusion obligation — concordant, carried as a binding pre-proof gate.

---

## SS-5 — Ranking and the single selected theorem

Convention: **leverage** (payoff if proved) × **distance** (nearness to provable; 5 = most tractable) × **Lean-connectability** (economy onto the certified interface), each 1–5. [ADJ] = adjudicator-assigned this pass. **C0 note:** this product ranks leverage-per-formalization-distance of independently falsifiable targets; it asserts neither provability nor exclusivity of any route. Since every critical candidate's unconditional head is open, the discriminator is honest bridge economy — the ranking names the theorem whose proof would plug the certified interface most economically, nothing more.

| # | Candidate | Lev | Dist | Lean | Π | Justification (condensed) |
|---|---|---|---|---|---|---|
| 1 | **L_a** | 5 | 2 | 5 | **50** | Clay-target payoff for the certified class; conditional half 1984-known, unconditional half Clay-equiv (Tao obstruction); shortest bridge (7–9 lemmas) onto confirmed plug targets |
| 2 | L_b | 5 | 2 | 4 | 40 | strictly weaker hypothesis (stronger theorem); BKM verbatim bridge; 9–12 lemmas, log-Grönwall coupling hazard |
| 3 | L_d | 5 | 2 | 4 | 40 | classical bridges per pair; supercriticality gap untouched; ~8–12 lemmas reusing L_a's chain (tie broken for L_b, see SS-4) |
| 4 | L-AX2 | 4 | 2 | 2 | 16 | axisym-class regularity + empties frozen B2; one power above critical is proved; last power on the scale-invariance floor; 10-layer stack |
| 5 | L-AX3 | 4 | 2 | 2 | 16 | cleanest Type-II-specific object (refutation IS the B2 asymptotics); a priori cap in unruled territory; 10-layer stack |
| 6 | L_c | 5 | 1 | 2 | 10 | the critical-norm Clay wall (open Liouville at every route's end); Tao-corpus formalization mass |
| 7 | KC-4 | 2 | 5 | 1 | 10 | sharpness/negative control only, but the register's most classically tractable object; no Lean layer |
| 8 | KC-1 | 3 | 3 | 1 | 9 | conditional branch elimination + free-standing steady-Euler theorem; proved Scope-A template exists; no Lean interface |
| 9 | L-AX1 | 4 | 1 | 2 | 8 | Lei–Zhang bridge kills the shrinking-saturation branch; VIOLATED ruling closes all known machinery routes |
| 9 | L-AX4 | 4 | 1 | 2 | 8 | strongest branch-kill in the axisym slate; inherits VIOLATED + Wu at full weight |
| 9 | L-AX5 | 4 | 2 | 1 | 8 | weakest bridged hypothesis (smallest-looking gap = the supercritical gap in local form); widest bridge (12 layers) |
| 12 | KC-2 | 3 | 2 | 1 | 6 | CHAIN-K12 engine; statement shape easier than vorticity-PB but 3-D time-dependent layer absent everywhere; Wu obligation |
| 12 | KC-3b [ADJ] | 2 | 3 | 1 | 6 | rate-maker only; weighted-Poincaré is assemblable standard math once EP-1 is fixed; τ-uniformity new |
| 12 | KC-3c [ADJ] | 2 | 3 | 1 | 6 | the depletion switch; fixed-time ingredients [H] in-corpus; τ-uniform persistence new |
| 15 | KC-7 [ADJ] | 2 | 2 | 1 | 4 | grey-line completion piece; bounded 2-D local analysis once EP-3 normalization is fixed |
| 16 | KC-5 [ADJ] | 3 | 1 | 1 | 3 | scope gate with real leverage; both directions open, forcing direction empty in 3-D; CAP-adjacent — literature-watch |
| 17 | L-AX6 | 2 | 1 | 1 | 2 | middle-limb closure only; both corpus closures bear directly; no plug shape |
| 17 | L-AX7 | 2 | 1 | 1 | 2 | one level below L-AX6; the VIOLATED ruling is about this object |
| 17 | KC-6 [ADJ] | 2 | 1 | 1 | 2 | hardest of register; EP-2/EP-6 debts precede routes |
| 20 | KC-3a [ADJ] | 1 | 1 | 1 | 1 | hypothesis component at high falsity risk; EP-7 completion target precedes everything |
| — | L_e | — | — | — | — | BANNED; unranked by design (pinned endpoint + target metric) |

### THE SINGLE SELECTED THEOREM

**T-SEL := candidate L_a**, in mandated shape 1 — *new quantitative rigidity estimate ⇒ existing continuation theorem ⇒ global regularity* — instantiated with the reverse-gap a priori estimate playing the rigidity role. Selection per the printed ranking (Π = 50): highest leverage at the shortest bridge, both plug endpoints already formal, hypothesis strictly lower-order than the carrier norm and cleanly falsifiable (Type-I log-divergence), zero Γ-gate distance. **C0:** a selection, not a uniqueness claim; L_b is the recorded runner-up (weaker hypothesis, one extra exponential); the ordering L_c ⇒ L_a stands recorded.

**Adjudicator verification of L_a's counterexample survival (mandated, performed independently this pass [D]):** (i) exact scale invariance of Q = ∫₀^{T′}‖∇U‖_∞dt recomputed under u_λ = λu(λx,λ²t) — invariant, and G may depend on (ν,‖u0‖,T), so zoom families move the datum and manufacture no violator; (ii) non-banned check: H³(ℝ³) ↪ C¹ (3 > 5/2) makes Q strictly lower-order (scale-invariant) than the subcritical carrier norm — a Q-bound does not restate the conclusion, and a Type-I profile ‖∇U‖_∞ ~ c/(T*−t) falsifies Q independently; (iii) per-horizon finiteness is automatic (continuous trajectory on compact Icc + embedding), so the content is horizon-uniformity at fixed T — no triviality either way; (iv) battery re-walk: B2 members are hypothesized, none constructed (refutation-shaped only); u_τ/𝒱 solve no PDE and the statement is solutions-only with no snapshot-derivability claim; W★/C13 are tuples; steady-Euler families are not unforced ν > 0 NS trajectories; no smooth finite-energy datum with divergent Q on a bounded horizon is known in the literature consulted [H]; (v) FC-086/Tao-averaged obstruction binds future unconditional proofs, not the statement. **Confirmed: SURVIVES.**

**Dependency chain (every arrow's consumed lemma named and classified):**

```
[N0]  L_a  (NEW-MATHEMATICS — OPEN; Clay-equivalent-or-harder; unproved and unclaimed)
      ∀ν>0, ∀ admissible Schwartz datum u0, ∀ finite T:  ∃ G(T;ν,‖u0‖) < ∞  such that
      every certified solution on every certified horizon T′ ≤ T satisfies
      ∫₀^{T′} ‖∇U(s)‖_{L^∞(ℝ³)} ds ≤ G(T),   U = J⁻³∘u (decoded field).
        │
        ▼  Arrow A — bridge L_bridge(Q_a,R_a): SEL-1…SEL-9 of SS-6
           [all KNOWN-MATH-TO-FORMALIZE except the EXISTING-LEAN anchors noted there;
            sources: BKM CMP 94 (1984) ineqs. (13)–(14) + NS remark; Kato–Ponce CPAM 41 (1988)]
[N1]  sup_{t∈[0,T′]} ‖u t‖_carrier ≤ R(T) := ‖u0‖·exp(C_KP·G(T))  on every certified horizon T′ ≤ T.
        │
        ▼  Arrow B — existing continuation theorem, consumed via SEL-10:
           r3EndpointSafeProjectedMildSolution_unique  [EXISTING-LEAN]
           IsMildSolutionOn.mono                        [EXISTING-LEAN]
           r3EndpointSafeProjected_horizons_unbounded_of_uniform_bound  [EXISTING-LEAN]
           (with r3EndpointSafeProjected_exists_extension_of_bounded + r3MildLifespan_antitone
            inside it)                                  [EXISTING-LEAN]
           sSup/BddAbove instantiation two-liner        [KNOWN-MATH-TO-FORMALIZE, trivial]
[N2]  r3MildHorizons hnu u0 is unbounded.
        │
        ▼  Arrow C — packaging:
           r3EndpointSafeProjected_blowup_dichotomy (left branch) [EXISTING-LEAN]
           r3AdmissibleSchwartzDatum_blowup_dichotomy             [EXISTING-LEAN]
[N3]  Global continuation of the certified class (projected mild H³ evolution with decoded
      interior-time NS semantics) for every admissible datum — the repo's formal proxy for
      global regularity. Classical global smoothness additionally needs: maximal-trajectory
      gluing (Bucket-B choice construction) and parabolic smoothness upgrade
      [both KNOWN-MATH-TO-FORMALIZE, paper; outside current Lean scope guards].
```

**Rule-2/C0 guard:** N0 is open; nothing above claims it, and the contrapositive reading (bounded certified horizons force Q → ∞) makes Q *a* — not "the" — continuation-controlling quantity. The terminal node is the certified-class statement, not an assertion about the Clay problem. A singularity candidate of the lab's program must, among other things, diverge Q on a bounded horizon — the correct two-sided polarity.

---

## SS-6 — On-paper minimal Lean-lemma decomposition of T-SEL only

Paper-only per rule 5: **no Lean code, no new Lean infrastructure is proposed here; formalization awaits a separate user commission.** Fourier/Bessel convention fixed throughout = the repo's (J = (1−(2π)⁻²Δ)^{1/2}). N0 is deliberately **not** decomposed (open head; counterexample-first discipline; FC-086 constrains any future proof attempt to exact-NS structure).

1. **SEL-1 (norm transport, B2).** For every carrier element g, the decode U := J⁻³g lies in H³(ℝ³;ℂ³) and ‖J³U‖_{L²} = ‖g‖_{L²} exactly; encode∘decode = id on the Schwartz core; the classical Sobolev norm Σ_{|α|≤3}‖D^αU‖²_{L²} is two-sidedly equivalent to ‖J³U‖²_{L²} with explicit convention constants. *Anchors (partial):* `besselPotential_r3HsToTempered_eq_coordinate` (`R3SobolevCarrier.lean:64`), `r3HsToTempered_memSobolev` (`:76`), `r3H3ToL2Operator_r3SchwartzToHsCLM` (`R3SchwartzInitialData.lean:100`); the two-sided norm statement itself is Lean-absent (`R3FiniteEnergy.lean:17`). **KNOWN-MATH-TO-FORMALIZE.**
2. **SEL-2 (embedding).** ∃C_emb: ‖U‖_{L^∞} + ‖∇U‖_{L^∞} ≤ C_emb‖U‖_{H³} for all U ∈ H³(ℝ³;ℂ³) (3 > 5/2). Gives Q well-defined and finite per horizon; witnesses the non-banned check. *No anchor.* **KNOWN-MATH-TO-FORMALIZE.**
3. **SEL-3 (identification and interior smoothing, B6/B3).** For a certified solution u on [0,T′] with solenoidal admissible datum: the decoded trajectory U(t) is a paper mild solution of Leray-projected NS matching the repo's minus-sign, endpoint-safe Duhamel form; at interior times it has the strong L²-valued derivative ∂ₜU = νΔU − P((U·∇)U); by parabolic smoothing and mild uniqueness, U ∈ C((0,T′];H^k) for all k, is H³-valued C¹ on (0,T′], and solves unprojected NS with the Helmholtz pressure. *Anchors:* interior-time semantics `r3EndpointSafeProjectedMild_navierStokes` (`R3NavierStokesEquation.lean:142–174`) **EXISTING-LEAN**; the smoothing upgrade **KNOWN-MATH-TO-FORMALIZE.**
4. **SEL-4 (commutator; BKM (13) / Kato–Ponce).** For |α| ≤ 3 and smooth divergence-free U: ‖D^α((U·∇)U) − (U·∇)D^αU‖_{L²} ≤ C_KP‖∇U‖_{L^∞}‖U‖_{H³}. **KNOWN-MATH-TO-FORMALIZE** [V office: BKM ineq. (13); Kato–Ponce CPAM 41 (1988) 891–907 [H]].
5. **SEL-5 (H³ ladder, projected form; BKM (14) + NS remark, B3).** For the SEL-3 solution on (t₀,T′]: d/dt ‖U‖²_{H³} ≤ 2C_KP‖∇U‖_{L^∞}‖U‖²_{H³} — test the D^α-equation with D^αU; the viscous term −ν‖∇D^αU‖² ≤ 0 is discarded; the transport term vanishes by div U = 0; the Leray projector (self-adjoint idempotent Fourier multiplier commuting with derivatives, norm ≤ 1) and the pressure gradient drop. **KNOWN-MATH-TO-FORMALIZE.**
6. **SEL-6 (Grönwall).** y′ ≤ m(t)y with m ∈ L¹(t₀,T′) implies y(t) ≤ y(t₀)exp∫_{t₀}^t m; applied with m = 2C_KP‖∇U‖_{L^∞}. *No Lean Grönwall infrastructure exists (B1).* **KNOWN-MATH-TO-FORMALIZE.**
7. **SEL-7 (endpoint bookkeeping, B5).** t ↦ u t is continuous on Icc 0 T′ (**EXISTING-LEAN**: the mild predicate's `ContinuousOn`, `EndpointSafeTwoSpaceDuhamel.lean:607–613`); with SEL-1, t ↦ ‖U(t)‖_{H³} is continuous on [0,T′]; letting t₀ ↓ 0 in SEL-6 yields the closed-interval bound sup_{[0,T′]}‖U(t)‖_{H³} ≤ ‖U(0)‖_{H³}·exp(C_KP Q₀), with ‖U(0)‖_{H³} = ‖u0‖ by SEL-1. Limit bookkeeping **KNOWN-MATH-TO-FORMALIZE.**
8. **SEL-8 (realness, B4).** For real admissible data the decoded field is real at every certified time (**EXISTING-LEAN**: `IsR3EndpointSafeProjectedMildSolutionOn.isR3RealVelocity`, `EndpointSafeTwoSpaceUniqueness.lean:234`), so ‖∇U‖_{L^∞} is the physical real-field quantity; all estimates run unchanged on the complex carrier. Remark-level.
9. **SEL-9 (bridge assembly = L_bridge(Q_a,R_a)).** For all ν > 0, admissible u0, finite T, certified horizon T′ ≤ T, certified solution u: if ∫₀^{T′}‖∇U(s)‖_{L^∞}ds ≤ Q₀ then ‖u t‖_carrier ≤ ‖u0‖·exp(C_KP Q₀) =: R(T,ν,‖u0‖,Q₀) for every t ∈ [0,T′] (ν-free since dissipation is discarded; ν and T enter only through the head's G). Composition of SEL-1–8. **KNOWN-MATH-TO-FORMALIZE.**
10. **SEL-10 (plug discharge).** (a) Uniqueness transfer: a bound along one solution per horizon extends to all certified solutions (**EXISTING-LEAN**: `r3EndpointSafeProjectedMildSolution_unique` + `IsMildSolutionOn.mono`). (b) If `r3MildHorizons hnu u0` were BddAbove with S = sSup, instantiate N0 at T = S+1 and feed R* = R(S+1) to `r3EndpointSafeProjected_horizons_unbounded_of_uniform_bound` (**EXISTING-LEAN**) — contradiction; hence horizons unbounded = left branch of `r3EndpointSafeProjected_blowup_dichotomy` / `r3AdmissibleSchwartzDatum_blowup_dichotomy` (**EXISTING-LEAN**). Two-line logic **KNOWN-MATH-TO-FORMALIZE.**

Ten lemmas total: six with no Lean anchor (all standard mathematics), four resting on existing anchors — consistent with the office estimate of 7–9 plus assembly.

---

## SS-7 — Claim boundary

- **RECORD-ONLY.** This document changes no frozen verdict (VIOLATED, UNDERDETERMINED, B2-UNKILLED, kill-table rows), no operative frontier, no park status. The BH/Γ-depletion branch remains **PARKED**; its fired termination rule and the pending user pivot act are untouched.
- **Nothing here asserts, approaches, or implies a resolution of the Navier–Stokes Millennium problem in either direction.** Every candidate estimate is OPEN. The selected theorem's head N0 is OPEN and Clay-equivalent-or-harder; only its bridge is known mathematics; no proof obligation of `FABLE5_HANDOFF.md` is closed or claimed closed.
- **C0 discipline:** every selection is a selection, never a uniqueness claim; "a continuation-controlling quantity", never "the"; no "only route"/"necessary" appears as an assertion anywhere in this record; VIOLATED and UNDERDETERMINED are quoted with their exact semantics (neither asserts the negation of the object whose route it closed).
- **Lean:** no code written, no infrastructure proposed; SS-6 is paper-only; formalization of T-SEL's bridge awaits a separate user commission.
- **Consumption bars upheld:** KC-4 [C-num] constants (EP-8); Seregin f-prefactor [V?]; Wei constant normalization [V?]; the Wu-exclusion pre-proof obligation on KC-2; the §B2.4 standing test-case rule (any (Γ-OSC)/(Γ-DEP) proof claim must first run against u_τ and 𝒱); the D0 un-park triggers stand unchanged (a theorem consuming D5(i)/(ii)/(iv) structure routes automatically to freeze review).
- **What a future freeze review would need to adjudicate if anything from SS-3 is adopted:** EP-1 (KC-3b restatement + [C]→known isoperimetric upgrade), EP-3 (KC-7 width-floor normalization), EP-4 (KC-3c falsifier re-pointing), EP-5 (KC-2 remainder quantification), EP-6 (KC-6 boundary-scope repair; promotion of the partial-homogenization item to load-bearing), EP-7 (KC-3a routing to the stress office with the stacked-cell family as completion target), EP-8 re-affirmation, the L-AX3 separate-form primacy note, and adoption of this register itself as a gate record. None of these moves a frozen verdict on its own; each is a bounded, named decision.

---

## SS-8 — Bottom line

Nineteen reverse-gap candidates were adjudicated across three lanes under one-shot sanction. **Zero were rejected; one is banned** (L_e — the carrier-norm restatement, pinned as the degenerate endpoint and retained only as the target metric); **eighteen survive counterexample search and every one remains open** — three carry statement-fixing debts (KC-3b, KC-7, KC-2's remainder) and one sits at high falsity risk pending a named completion target (KC-3a). The K12-complex re-verification confirmed the chain is conditional, sub-wedge-scoped, and nowhere class-wide, and produced eight record-only erratum/repair proposals. The ranking selects **L_a — the time-integrated velocity-gradient bound ∫₀^T‖∇U‖_{L^∞}dt ≤ G(T;ν,‖u0‖)** — as the single selected theorem: its bridge to the already-formal continuation interface is 1984-known mathematics in ten paper lemmas onto Lean endpoints that exist today, and its open head is exactly one independently falsifiable quantitative estimate. Proving the head would give global continuation of the certified class; exhibiting a certified solution family that diverges the integral on a bounded horizon is precisely the signature the lab's singularity program must produce. Both directions remain open. Nothing frozen has moved.

---

## SS-9. Execution addendum (main-loop, 2026-09-02)

- **Tally reconciliation.** The orchestration-level counter printed 21 candidate
  rows / 14 name-matched survivors; the adjudicated tally (this record) is **19
  candidates (KC-3 counted once, split a/b/c) = 18 SURVIVE + 1 BANNED (L_e)**.
  The 21/14 figures are a name-normalization artifact of the pipeline's exact
  string matching; the routes office consequently received 14 objects, and the
  adjudicator explicitly scoped and adjudicated the six unrouted ones (SS-4,
  "Unrouted six") with all candidates and all verdicts in hand. The adjudicated
  tally governs.
- **Main-loop spot-verification of T-SEL (independent, this pass):** exact scale
  invariance of Q = int ||grad U||_inf dt under the parabolic rescaling
  (recomputed); the non-restatement check (Q critical vs carrier norm
  subcritical; Type-I log-divergence polarity); the sSup/BddAbove plug
  derivation against `r3EndpointSafeProjected_horizons_unbounded_of_uniform_bound`;
  and the SEL-4/5/6 bridge shape (Kato-Ponce commutator at s = 3 + Groenwall,
  viscous term discarded by sign) — all agree with the record.
- **Standing instruction context:** M-1 (Hou wall closure) is **ON HOLD** by the
  user's 2026-09-02 instruction — the numerical active lane is retained, not
  discarded, and not started. This audit changes nothing about the lane or the
  park; T-SEL's formalization and any proof exploration await a separate user
  commission (expected in a fresh session per the user's stated plan).