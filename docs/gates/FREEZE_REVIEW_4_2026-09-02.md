# FREEZE REVIEW — ROUND 4 (2026-09-02) — EXECUTED

Date: 2026-09-02 (JST). **Adjudication authority: the user's dated 2026-09-02
instruction, verbatim:** "Freeze Review Round 4 を実行せよ。Γ-OSC decision record §6
の P1–P6 を個別裁定し、Γ-OSC feasibility = VIOLATED を正確な scope で frozen map に
反映する。その上で、登録済み stop rule に従い BH / Γ-depletion branch を current
active research lane として終了・park し、未解決であることと「lane の既知機構が尽きた」
ことを明確に区別せよ。D-3 / Seregin は literature watch に分離する。review 完了後、
次の active lane を SPEC.md の verified nonlinear finite-cylinder numerical candidate
program に切り替え、最初の bounded numerical milestone を1つだけ選定せよ。"
Execution scope: this record + kill-table annotation block **D0–D7** + the
**Post-round-4 frontier** (sole operative frontier text) + the new standing
register `LITERATURE_WATCH_REGISTER_2026-09-02.md` + the milestone selection §5.
Agenda source: `GAMMA_OSC_FEASIBILITY_2026-09-02.md` §6 (P1–P6) and §7 (the
registered termination rule, fired on its VIOLATED clause).

*Label hygiene:* the **D-annotation numbers of this round are unhyphenated**
(D0–D7) and continue the A (round 1) / B (round 2) / C (round 3) series; they are
unrelated to the **hyphenated external-record debts D-1/D-2/D-3**, to the
derivation tag `[D]`, to the β_v-decision's internal disagreement labels
D-1/D-2, and to the kill table's own 2026-08-19 bracketed derivation labels
`[D1]/[D1′]/[D2]` (K6/K8 rows; the D1 adversarial audit). Likewise the watch
register's **hyphenated W-numbers (W-1–W-4)** are unrelated to the kill table's
unhyphenated witness inventory W1–W5 and to the bookkeeping witness `W★`.

## 0. The park ruling (standing, user-directed) — executed as D0

**The BH / Γ-depletion branch is TERMINATED AS THE ACTIVE RESEARCH LANE and
PARKED.** Status label: **PARKED** — neither KILLED nor RESOLVED. The registered
termination rule (`FREEZE_REVIEW_3_2026-09-01.md` §6(3)/§8, adopting EXT §B2.5)
fired on its VIOLATED clause at the Γ-OSC decision; execution is this user act.

**The mandatory distinction, printed as the operative status sentence:**

> **UNRESOLVED ≠ EXHAUSTED-MACHINERY.** What is *exhausted* is this program's
> in-house inventory of known mechanisms at frozen Scope-B, in three adjudicated
> layers: (1) **frozen exponent/region arithmetic cannot pin** the middle limb
> (β_v decision, `YES (CONSISTENT)`, 2026-08-23); (2) **single-time
> NS-compatible smooth geometry cannot decide** Γ-depletion (Γ-DEP decision,
> `UNDERDETERMINED` + snapshot counterprofile no-go, imported 2026-09-01);
> (3) **the known drift–diffusion / boundary-regularity machinery cannot
> deliver** the τ-uniform contraction (Γ-OSC decision, `VIOLATED`, 2026-09-02 —
> forced-divergent caps, SSŠZ/SVZ/Wu floor, multi-scale closure). What remains
> *unresolved* — and is in no way settled by the park — is the mathematics
> itself: **B2 stays UNKILLED in Scope B; the middle limb `β_v ∈ (α,γ)` stays
> open; (Γ-OSC) and (Γ-DEP) stay live sufficient-condition targets** (C0
> semantics: their negations are asserted nowhere); the §5.3 structure register
> (D5) names the routes a genuinely new theorem could take. The park is a
> statement about this program's toolbox, not about Navier–Stokes.

Frozen at park time: **BH verdict YELLOW-RED · B2 UNKILLED in Scope B · (T-c)
OPEN · no CAP trigger · no Clay claim.** No in-house work on the branch without
a user-adjudicated un-park. **Un-park triggers** (any one suffices to put an
un-park decision on a freeze-review agenda; none un-parks automatically): (i) a
published theorem consuming one of the D5 structure candidates; (ii) a
critical-index contraction with degradation `(ln N)^{−p}`, `p ≤ 1` (the named
quantitative frontier shape); (iii) a watch-register trigger fires (D7);
(iv) explicit user instruction.

## 1. Individual rulings — Γ-OSC record §6, P1–P6

| Item | Ruling | Disposition |
|---|---|---|
| **P1** (frontier row: Γ-OSC feasibility = VIOLATED, machinery-closure scope) | **ADOPT** | Executed as **D1** — P1 adopted in substance and expanded with the master record's §2/§8 content (rates, antichain names, floor citations, certificate-conditionality tag); scope guards carried verbatim: *machinery closure at frozen scope*; the implied sub-region of `S_blob` exactly empty; sup/`L^∞_tL³`/local-energy caps forced-divergent class-wide; every minimal known-sufficient rung defeated τ-uniformly by a frozen-compatible family; **sufficient-condition semantics only — `¬(Γ-OSC)_op` and `¬(Γ-DEP)_fld` are NOT asserted** |
| **P2** (D-1/D-2 discharge annotations at the EXT record, with the D-1 erratum) | **ADOPT** | Executed as **D2**; the in-place status notes already synced at `EXT_GAMMADEP_DECISION_2026-09-01.md` (2026-09-02) are hereby formally adopted (D-1 DISCHARGED [V] with the "almost Hölder"-is-CTZ erratum; D-2 DISCHARGED [V]) |
| **P3** (five record-only true statements) | **ADOPT** | Executed as **D3**: (i) the forced-amplitude lemma [D]; (ii) the sign-cap anti-implication [D]; (iii) the 5-D divergence-form identity `∂_tΦ + div₅(b₅Φ) = νΔ₅Φ`, `div₅b₅ = 2u_r/r` [H] — **superseding every clean-transport print of `Γ/r²` wherever it appears**; (iv) Qian–Xi (arXiv:1704.02173) = the unique known paid-hypothesis theorem, conclusion-shape-incapable [V]; (v) the `(ln N)^{−p}, p ≤ 1` compensation threshold as the named quantitative frontier shape [D] |
| **P4** (standing test cases extended: the family `𝒱` + declared kinematic scan) | **ADOPT** | Executed as **D4**, under the same §B2.4 discipline as the §A.3 snapshot; applies to every future Γ-depletion-type hypothesis, including any un-park proposal |
| **P5** (structure-candidate register, Γ-OSC record §5.3 (i)–(vi)) | **ADOPT** | Executed as **D5**: recorded as the exhaustive-at-this-pass register of non-rung NS structure a future dynamical closure could consume — sufficient-condition candidates only, none with a known theorem today, no exclusivity claimed |
| **P6** (pivot execution — a user act) | **ADOPT AS EXECUTED** | The user's 2026-09-02 instruction **is** the execution. Executed as **D6**: active lane switched to the `SPEC.md` numerical candidate program (§5 below); pivot option (b) (Seregin watch) is subsumed into the watch register (D7) rather than chosen as a lane — watches are not lanes |

## 2. Γ-OSC = VIOLATED, reflected into the map at exact scope (D1 wording)

The executed frontier row (**operative text = kill-table D1**; restated here —
minor sentence-joining differences are immaterial, the kill-table print
governs):

> **(Γ-OSC)_feas = VIOLATED (2026-09-02, machinery closure at frozen scope,
> C0-clean).** No known-sufficient drift–diffusion / boundary-regularity rung is
> implied by frozen B2 — the implied sub-region of `S_blob` is exactly empty,
> and the sup / `L^∞_tL³` / CTZ local-energy caps are **forced-divergent**
> class-wide at `ν^{−1}τ^{−(γ−α)}` — resp. squared for the local-energy cap,
> `ν^{−2}τ^{−2(γ−α)}` — (forced-amplitude lemma). An explicit
> frozen-compatible smooth divergence-free axis-regular family defeats every
> minimal rung of the adjudicated known-sufficient antichain (L3; L4a/L4b;
> AX-L5; AX-L7) τ-uniformly, at power rates against at most double-log caps;
> the known ladder provably terminates at scale invariance (SSŠZ/SVZ/Wu floor;
> Wu Thm 1.8 defeats the exact Γ-operator), and the multi-scale escape closes at
> the `(ln N)^{−p}, p ≤ 1` threshold. **Scope guards: this is a statement about
> derivability at frozen scope. `VIOLATED ⇏ ¬(Γ-OSC)_op`, `⇏ ¬(Γ-DEP)_fld`; no
> NS solution is constructed; no blow-up, no regularity, no Clay claim.**
> Master record: `GAMMA_OSC_FEASIBILITY_2026-09-02.md`.

## 3. Literature-watch separation (D7)

D-3 and Seregin are **removed from every lane and moved to the standing
register** `docs/gates/LITERATURE_WATCH_REGISTER_2026-09-02.md` (created this
round) **as W-1–W-3**: the D-3 family (arXiv:2606.07869 + companion 2606.07875
+ the author cluster, with the triage's re-check triggers — W-1) and the Seregin
items (arXiv:2606.29468 log-corrected row-(i) pressure, [V?] full-text debt,
C14 — W-2; the (OO)-adjacent ancient-Euler Liouville problem — W-3). **W-4 is
additionally created this round** as the literature half of D0's un-park
triggers (plus the OOV-a Osgood face, added at register creation). Watches are **passive**: they
carry re-check triggers only, consume no in-house work beyond trigger
evaluation at literature passes, and are decoupled from the active lane and
from the parked branch alike. A fired trigger puts an item on the next
freeze-review agenda; it does not by itself reopen anything.

## 4. The re-fixed frontier (unique)

Executed in the kill table as the **Post-round-4 frontier** — the sole operative
frozen text (the round-3 block carries a supersession pointer). Content summary:
the round-3 frontier is carried unchanged in its Scope-A/Scope-B mathematics
(condensed in the operative block; the full round-3 text and the C-annotations
remain of record), with four additions: the D1 Γ-OSC row; the D0 park sentence
(branch PARKED; UNRESOLVED ≠ EXHAUSTED-MACHINERY, with the three-layer
exhaustion list and the un-park triggers); the lane line (**active research lane
= the `SPEC.md` numerical candidate program; first bounded milestone M-1**, §5);
and the D7 watch-separation line. All carried
verdicts unchanged: BH YELLOW-RED (frozen at park) · B2 UNKILLED in Scope B ·
(T-c) OPEN · no CAP trigger · no Clay claim.

## 5. Lane switch and the first bounded numerical milestone — SELECTED: M-1

**Active lane (from this round): the `SPEC.md` verified nonlinear
finite-cylinder axisymmetric-with-swirl numerical candidate program** (SPEC §§2–10
govern equations (S-U1/S-W1/S-P1/S-REC), signs, `2πr dr dz` measure, axis parity,
candidate invariants, and promotion language; `AGENTS.md` MNS-2 numerical
invariants and the Hou production wall-vorticity gate apply).

**M-1 (the single first bounded milestone): implement and verify the Hou 2022
no-slip wall-vorticity boundary closure in the existing finite-cylinder stack —
i.e., clear the recorded numerical-scope blocker of
`docs/reports/HOU_WALL_VORTICITY_BOUNDARY_AUDIT_2026-08-13.md`.**

Rationale: the audit report is this repository's own named blocker gating every
downstream goal of the lane (any controlled Hou-2022 reproduction attempt, any
late-state comparison, any longer-time run). It is bounded (eight enumerated
requirements + written verification gates), self-contained, and consumes no new
mathematics.

**Deliverables (exactly the audit report's items 1–8, restated as acceptance
gates):**

1. Discrete no-flow `ψ₁ = 0` at `r = 1` in the documented cell/face layout.
2. Discrete no-slip `∂_rψ₁ = 0` at `r = 1` with an explicitly documented
   stencil/ghost relation.
3. Vorticity-creation boundary `ω₁ = −∂_r²ψ₁` at `r = 1`, derived from the same
   discrete streamfunction closure and entering the `ω₁` transport/diffusion
   operator consistently.
4. Swirl wall condition `u₁ = 0` (equivalently `Γ = 0` at `r = 1`) consistent
   with the conservative transport/diffusion discretization.
5. Pole conditions preserved at `r = 0` (evenness / zero odd radial derivatives
   for `u₁, ω₁, ψ₁` — strengthened from the audit's lowest-order Neumann
   statement per SPEC §5; axis operator via (S-AXOP)).
6. Holomorphic tangent compatibility: the frozen tangent/JVP map differentiates
   the exact same wall update; no state-dependent `max/abs/sign/clip/branch`
   inserted into the perturbed holomorphic map.
7. Adjoint compatibility: reverse map includes all new wall-row/ghost-row
   contributions (made unconditional by choice — the stack uses physical-metric
   adjoints per STATUS).
8. Verification gates, all recorded per SPEC §8 (per-term residuals, convergence
   sequences, reload hash): finite-difference JVP checks with wall-concentrated
   perturbations; transpose/adjoint dot-product tests with wall-sensitive
   vectors; recovered wall velocities `u^r = u^θ = u^z = 0` at the advertised
   discretization order; refinement tests tracking **wall residuals separately**
   from interior norms.

**Fail-closed stencil rule (inherited, binding):** the exact Hou production
stencil is delegated by the 2022 paper to its cited methodology. M-1's first
sub-task is a primary-source pass to pin that stencil (Hou FoCM 2022, DOI
10.1007/s10208-022-09578-4, and its cited numerical methodology — e.g. the
Luo–Hou line, an unverified identification to be confirmed or replaced by the
pass itself); if it cannot be pinned, the closure is implemented and **labeled
`alternative wall closure`** with its derivation printed — never labeled Hou's
production stencil. Disallowed labels remain disallowed until the full gate
passes AND the stencil provenance is settled: no "Hou production reproduction",
no "Hou late-state validation", no "resolved Hou singular regime".

**What M-1 is NOT:** not a Hou reproduction (that is a later milestone gated on
M-1); not an `R³` result (SPEC §8.10 — finite-cylinder candidates never promote
to `R³` without a domain-transfer theorem); not a Clay claim of any kind.

**Preflight (before implementation starts):** consult the external exclusion
registry per `AGENTS.md` (read-only negative-knowledge check) and the external
equation audit for the wall-closure formulas; record the preflight result in the
milestone's evidence bundle.

## 6. Stop-rule audit (this round)

Docs-only; no numerics run; no proof attempts; no Lean source touched (the
8769-job local gate of `f65c463` remains the verification baseline). P1–P6
adjudicated individually; nothing beyond the printed agenda executed; the park
executes the registered rule on the user's instruction and invents no new
verdict; the watch separation moves recorded items without altering their
content; M-1 is a selection, not an implementation — no code was written this
round. Verdicts after this round: **BH YELLOW-RED (parked) · B2 UNKILLED in
Scope B · (T-c) OPEN · no CAP trigger · no Clay claim** — unchanged.
