# M3 × KNSS gate — the single question, answered

Date: 2026-08-20 (JST). Question (user-posed): **do M3's frozen assumptions imply the
KNSS global `|v| ≤ C/r` ancient-limit hypothesis?** One bounded pass (3 Opus analysts +
adversarial verification + critic; KNSS arXiv:0709.3599v1 full text fetched and read
first-hand; stop rules observed: no numerics, no new ansatz, no promotions, no map
edits, no Lean). Tags: [H] hard, [D] derived this pass, [V] verified first-hand from the
source, [C] conditional, [B] bookkeeping.

## 1. RULING: **NO** — the frozen face `γ₂ = γ` defeats the implication; everything below it dies

Frozen M3 (as literally defined, amplitude-carrier exponent `γ₂ ≤ γ`) does **not** imply
the KNSS hypothesis — and it is not mere silence: the frozen set **admits a defeating
member**, named exactly:

- **The survivor mechanism:** the amplitude-tie face **`γ₂ = γ`** — a second region
  carrying the *same* amplitude exponent as the Γ-saturated core. The region that breaks
  `C/r` is **the whole zoom exterior** (`r ≫ τ^γ`), at rate **O(1) in rescaled units**:
  the rescaled amplitude of R2 is `τ^{γ−γ₂}`, which fails to vanish precisely at the
  tie. The ancient limit then contains a **non-decaying plateau** — exactly KNSS's own
  declared trivial obstruction `u = b(t)`, which their paper kills **only via the
  Type-I bound (6.5)** ([V] first-hand, l.1091–1100: the receding-axis branch of KNSS
  is Type-I-only; Type II supplies no replacement).
- **Refuted en route** [ruled by the critic against one analyst]: the "matching
  annulus" is *not* where `C/r` breaks — two regions **superpose** (the envelope is the
  max of two tails, each decaying away from its own region; a Biot–Savart ledger
  confirms the core dipole tail obeys `r|u_pol| ≲ 1` for all `r ≥ τ^γ`). A single
  interpolating power law was a matched-asymptotics reflex with no matching problem.

## 2. The conditional converse — the useful half: `M3 ∩ {γ₂ < γ}` DIES

**[D], Scope-A-free, viscosity-free.** Chain: (S1) [verified by all four] every
power-law L³ carrier has `γ_j > ρ_j` — its `C/r` violation is real
(`sup r|u| ≍ τ^{ρ_j−γ_j} → ∞`) but sits at rescaled radius `τ^{ρ_j−γ} → ∞` and
**escapes the zoom**; the Γ-saturated core supplies KNSS's `|x′_k| ≤ C/M_k` step for
free (`M_k|x′_k| ≍ Γ₀`); Prop 6.1's rescaling is **amplitude-normalized and
rate-free** — *Type II is the enabler of the extraction, not the obstruction* (seeds
(c)/(S3) retired, unanimous + first-hand); with `γ₂ < γ` strictly the whole exterior
vanishes in the limit, giving a bounded ancient mild axisymmetric solution with
`|v| ≤ C/|y′|` globally ⟹ **Theorem 5.3 ⟹ v ≡ 0**, contradicting `|v(0,0)| = 1`.

Hypotheses consumed (named, not hidden): **(E)** dictionary exhaustiveness (the
poloidal field is generated by the labelled regions plus their tails — a modeling
hypothesis; it is what makes the annulus dissolve) and **(P)** class transfer (the
limit lies in KNSS's bounded-weak class; dischargeable via ε-regularity + local
pressure normalization, with the parasitic `b(t)` on the *original* flow excluded by
K5).

## 3. KNSS first-hand — the K4 debt is PAID [V]

Verbatim statements secured (arXiv:0709.3599v1, quotations re-checked by the critic):

- **Thm 5.2**: bounded weak ancient + axisymmetric **no-swirl** ⟹ `u = (0,0,b₃(t))`.
  Immediately after: *"the validity … in the absence of the 'no swirl' assumption is
  still an open problem."*
- **Thm 5.3**: bounded weak ancient + axisymmetric + `|u| ≤ C/r` on **all** of
  `R³×(−∞,0)`, `C` arbitrary ⟹ `u ≡ 0`. The proof needs the bound **at all radii**
  (the `λ → ∞` rescaling maps `{r ≤ δ}` onto all of space — no localized-in-`r`
  version exists) [D].
- **Thm 6.1** (the regularity criterion = the honest K4): axisymmetric weak solution on
  `R³×(0,T)`, `L^∞` on each `(0,T′)`, `|u| ≤ C/r` on **all** of `R³×(0,T)` ⟹
  `|u| ≤ M(C)` (mild, smooth). **No rate hypothesis — Type-II-valid.**
- **Thm 6.2 + the `u = b(t)` remark**: the `|u| ≤ C/√(T−t)` variant needs the far-field
  hypothesis (6.6) and *"fails, for trivial reasons"* without it — the exact obstruction
  the tie face realizes.
- Application mechanism (Lemma 6.1 + Prop 6.1): blow-up normalized by the **running
  amplitude maximum** `M_k = |u(x_k,t_k)|`; Leray's rate enters only as an interval
  length. Footnote residue: Prop 6.1's nondegeneracy `|v(0,0)| = 1` is asserted, proved
  carefully only in the Thm 6.2 setting.

**K4 verdict: RESTORE as [V] first-hand** with Thm 6.1 as the statement — plus a
mandatory rider: **K4 never applies to the unzoomed M3 flow** (`K₄ ∩ M3 = ∅` by (S1) —
every L³ carrier violates `C/r` at its own location); the kill runs through
Prop 6.1 + Thm 5.3 on the `τ^γ`-zoom limit. The multi-region audit's "K4 would kill it"
was true of the core in isolation, false of the frozen class.

## 4. Map consequences (report-level; execution belongs to the freeze review)

1. **K4 restore [V]** with Thm 6.1 + the rider above; retirement rationale confirmed
   false (K3's `r^{−1+ε}` family misses the `ε = 0` endpoint).
2. **New conditional row K4′ [C]**: "axisym + the amplitude-normalized ancient limit
   decays like `1/r` ⟹ regular"; excluded subset `M3 ∩ {γ₂ < γ}`, conditional on
   (E)+(P).
3. **M3 verdict moves: OPEN → RESTRICTED (no independent members).** The `γ₂ < γ` part
   dies (K4′); the tie face `γ₂ = γ` has `(γ, α₂) ∈ S_blob` by K5+K6+K11 alone (pure
   exponent arithmetic, Scope-A-free) — **the face is a B2 point wearing a Γ-saturated
   sub-core**, inheriting B2's blockers (V1, the rigidity rate). It is codimension 1 in
   M3's parameters but carries a nonempty open `(γ, α₂)` set — not dismissible as
   non-generic. **The BH Scope-A open set contracts to B2 alone.**
4. **P5 (cuts FOR the kill):** elongated axis filaments (thickness `τ^γ`, length
   `τ^{l_T}`, `l_T ∈ (2γ−1, γ)` — window nonempty for all `γ`) evade the `v ≥ 3ρ`
   geometry, carry diverging L³, are Γ-saturated, **and satisfy `C/r` at their own
   location** — a *one-region* M3 and the cleanest K4′ target.
5. **P7 legend line:** energy is not scale-invariant (`∫|v_k|² = λ_k^{−1}∫|u|² → ∞`);
   K5 constrains the original flow, never a rescaled ancient limit. (This is why the
   tie face cannot be closed by energy.)
6. **K2 row correction:** the KNSS attribution needs (6.5) **and** (6.6); "nontrivial
   swirl" is not a KNSS hypothesis (CSTY-I/II citations unchecked this pass —
   downgrade, do not delete).

## 5. Answer to the user's dichotomy, and the next bounded task

The hoped-for chain "one-scale ring × / M2 not independent / M3 × KNSS" lands **almost
entirely**: `M3 ∩ {γ₂ < γ}` is killed (conditionally, Scope-A-free), and the only
survivor is the tie face, which **merges into B2**. The NO-mechanism is maximally
informative: *the ancient limit must refuse to decay* — the amplitude must be carried
at the tie by a second region whose exponents already sit in `S_blob`. The Scope-A
counterexample landscape after this pass: **B2 (two-scale blob) alone**, blocked by V1
[C] and the quantitative rigidity rate.

**Next bounded task (critic-designated):** close or confirm the tie face by finding the
**constant-excluding row**: the face's ancient limit is pinned to a bounded ancient
2.5-D **swirl-free** NS solution equal to a **nonzero constant**, arising as a Type-II
amplitude-normalized limit. One fetch-and-verify pass over post-KNSS axisymmetric/2.5-D
Liouville results replacing the Type-I rate with Γ- or local-energy-based hypotheses
(Seregin's ancient-solution notes; Lei–Zhang Γ-criteria; KNSS follow-ups). Stop rule:
if no such statement exists first-hand, print the face as the branch's named open
object and stand down to B2's V1 — do not derive it in-house.

## 6. Debts and honesty notes

(E) and (P) are modeling/class hypotheses, not bookkeeping — they must be printed with
any use of K4′; Prop 6.1's nondegeneracy residue; one analyst's "annulus forces the
violation" refuted (its arithmetic was right, its premise wrong); one analyst's
"axisymmetry of the zoom centre [H]" hard-refuted (the centre is off-axis at
`|x′_k| ≍ τ^γ`; the correct step is `M_k|x′_k| ≍ Γ₀`, which fails exactly on the
defeater face — as it should); the tie face's `ρ₂ = α₂` requirement applies to branch
(b) only, branch (a) needs only `γ₂ = γ`. No files modified by agents; no map edits;
stop rules clean (two dictionary-extension close calls policed: admissible as negation
witnesses, inadmissible as class members).
