# FREEZE REVIEW, ROUND 2 — F21–F36 + C6 executed under the user's amended adjudication; R-B2′'s kill claim narrowed to its Γ-saturated target

Date: 2026-08-21 (JST). **Pen-holding pass on the frozen map, round 2**, executed
under the user's explicit adjudication (received 2026-08-21: "全採用" declined —
**F21/F29/F31/F33/C6 adopted with amendments, all else adopted as proposed**).
Scope: the Scope-B reconnaissance queue F21–F29
(`BH_SCOPEB_RECON_2026-08-21.md` §7) + the anchor-audit queue F30–F36 + C6
(`BH_RB2_ANCHOR_AUDIT_2026-08-21.md` §6, §5-C6). Edits applied: this record + one
authorized annotation (sections **B1–B14**) appended to
`TYPE2_KILL_TABLE_2026-08-19.md`. Prior reports keep their text; superseded lines
are superseded here.

## 1. The headline: R-B2′'s capability is revised DOWN, accurately

The anchor audit established that what Scope A really supplies is not the zoom
technique but **exhaustiveness** — "every Scope-A blob is two-scale in the tongue
sense", i.e. every B2 candidate *enters* the Γ-saturated target class. And in
Scope B, even granting (SB-ANCH) leaves R-B2′ unstatable (a level is not a
labelled region; (E⁺⁺)/(NECK) undefined there). The review therefore executes,
as the user directed, **a weakening-to-accuracy of the map's actual kill claim**
(F33, F31 — substantive, not editorial):

> **Frontier, replaced.** OLD: "Scope-A quasi-static = B2 alone, killed by
> R-B2′ [C]." NEW: "**Scope-A quasi-static = B2; its Γ-saturated realization is
> the target of R-B2′ [C].** Scope A supplies the exhaustiveness theorem that
> every Scope-A B2 candidate enters that realization (amplitude corollary /
> two-scale structure); the exhaustiveness — not the zoom — is the Scope-A
> gate."

> **R-B2′ conditions, restated.** `(E⁺⁺) = (COH-Δ) + (ANCH-κ)` (renamed, B12),
> `(P)`, `(N-Γ)` — with **(N-Γ) status-split by `ρ_T`** (B11): discharged
> [C-dict] on the tie face / `ρ_T = γ`; **[C], discharge unsupplied**, on
> `ρ_T > γ` (the centre's rescaled `Γ ≍ τ^{ρ_T−γ} → 0` there).

> **Scope B.** B2: **UNKILLED**. First named gap: **(SB-ANCH) ⟺ `β_v = γ`
> (τ-uniform corner attainment)** — and the load-bearing rider: **granting
> (SB-ANCH) does not transplant R-B2′ to Scope B**; the labelled-region /
> exhaustiveness side is a separate, independent requirement.

## 2. Adjudication table (user's rulings, applied)

| # | item | ruling | execution |
|---|---|---|---|
| F21 | scope legend | **ADOPT, amended**: "the **theorem/arithmetic rows themselves** are scope-free" — applicability NOT declared settled in Scope B; (P), K8-on-multi-region, intra-core `β_v` limb remain unresolved (recon §4) | B1 |
| F22→F33 | R-B2′ scope rider + frontier narrowing | **ADOPT** (substantive claim change): (T1)/(T2)/(T3) split; "aggregation machinery" repair; R-B2′ targets the Γ-saturated realization, Scope A supplies "all B2 enters it"; frontier lines replaced (§1) | B2 |
| F23 | DVEP recording | **ADOPT**: "authors assert non-localizability; proof not located" | B3 |
| F24 | gate verdict sentence | **ADOPT**: `A_NS` qualifier propagated; trigger and verdict now agree | B4 |
| F25 | trigger status | **ADOPT**: (T-c) OPEN @ 2026-08-21 + re-check condition | B5 |
| F26+F32 | (SB-ANCH)-final + the standing Scope-B open question | **ADOPT**: (H1) ∧ (H2); distance clause deleted from the hypotheses; F26's canonical open question printed verbatim | B6 |
| F27 | literature ledger | **ADOPT**: arXiv:2606.13462, arXiv:2608.11547; "three papers take localizability as their subject; all three assume it" | B7 |
| F28 | debts | **ADOPT**: Jiu–Xin paywall + CLV surrogate + no-swirl qualifier; DVEP (L) gauge ruling discharged [D] | B8 |
| F29 | Scope-B legends | **ADOPT, amended**: rider added — "K9/K6 strengthen in Scope B" is a loss-of-escape statement, **not a proof of any new kill** | B9 |
| F30 | A15 transparency | **ADOPT**: conditionality list names `(E⁺)` and `frozen dictionary [C-dict]` | B10 |
| F31 | (N-Γ) | **ADOPT, strengthened per user**: not blanket [C-dict] — **branch-split**: tie face / `ρ_T = γ` → discharged [C-dict]; `ρ_T > γ` → **[C], discharge unsupplied** (preventing future conditionality inflation) | B11 (+ the A15 "amplitude-saturated level" precision, executed in B2) |
| F34 | naming collision | **ADOPT, user's choice**: (E⁺⁺)'s internal level statement renamed **(ANCH-κ)**; Scope-B **(SB-ANCH)** kept | B12 |
| F35 | `β_v` legend | **ADOPT, amended**: `β_v` formally defined; **`β_v = γ` means τ-uniform constant-attainment at the envelope corner, not a bare exponent equality** — this makes F32's "equivalence" safe | B13 |
| F36 | internal consistency | **ADOPT**: fork-(c) wording reconciled; doc pointer corrected | B14 |
| C6 | fork-(c) operative definition | **FIXED per user**: fork (c) fires ⟺ an **unlisted, independent, irreducible Scope-A input** is discovered. Multiplicity of anchor *appearances* does not fire it (downstream usages derived from the same input are not new conditions). Recon §8's "consumes the anchor more than once" is **superseded** | B14 |

## 3. What the review did NOT do (round-2 discipline)

- **No new kill manufactured**: the round *narrows* the map's claim (F33) and
  *weakens* a conditional's status (F31: [C-dict] → split with a [C] branch).
  Nothing was invented to close (SB-ANCH), the labelled-region gap, or the
  exhaustiveness gap in Scope B.
- **B2 in Scope B stays UNKILLED** — printed as such.
- **The `β_v ∈ (α,γ]` limb stays an open debt in Scope B** (its home as
  (SB-ANCH); its analysis is theorem-shaped and was not attempted). In Scope A
  it is pinned to its endpoint `β_v = γ` by the amplitude corollary — B2 (T3),
  which narrows A2's debt there and says so explicitly.
- **No report rewritten**: every superseded line is named in the B-section
  that supersedes it (**the full superseded set is enumerated in B1–B14**),
  including: the round-1 closing frontier block and round-1 §3/§5 phrasings
  (B2/B11/B12); A15's "no Scope-A geometry" + "Γ-saturated level" and the
  ledger §1 occurrence (B2); A8's rationale + multiregion §6.2 DVEP clauses
  (B3); the geo-gate verdict sentence l.203–204 (B4); recon §4's one-line
  (SB-ANCH) (B6); recon §4's "σ = α razor" direction note (B9);
  constant-exclusion §1's "dischargeable from τ-uniform saturation" + the
  on-map (N-Γ) [C-dict] tags in A15/A16 (B11); the plain "(ANCH)" occurrences
  in A15/A16/A19 (B12); recon §8's fork-(c) wording (B14). Original files
  keep their text.

## 4. Hygiene executed

1. **(ANCH) → (ANCH-κ)** rename (B12): the on-map print sites — A15's row list,
   A16's decomposition, A19's "sole carrier of (ANCH)" (the T4 legend, adopting
   F20) — now read (ANCH-κ); round-1 §5's "three named objects" line is
   superseded twice over (rename + B11 split). R-NEG6's "it is not an anchor"
   refers to `κ̂ ≤ τ^{2ρ−γ−α}`, not to (ANCH-κ) — no rename there. (SB-ANCH)
   unchanged.
2. **`β_v` enters the legend** (B13) beside `σ_core`/`σ_sup` (A1).
3. **Fork-(c) operative definition** frozen (B14/C6); recon §8's wording
   superseded, not rewritten.
4. **Doc pointer** corrected (B14): B2/K9/amplitude-gate structural content
   lives in `BH_MULTIREGION_AUDIT_2026-08-20.md` §§1–3 + kill-table A1–A3, not
   in `BH_SWIRL_FRACTION_PROBE_2026-08-19.md`.

## 5. Post-round-2 frontier (frozen; full statement in the annotation's closing block)

- **Scope A quasi-static** = B2; its **Γ-saturated realization** is the target
  of R-B2′ [C] on (E⁺⁺) = (COH-Δ)+(ANCH-κ), (P), (N-Γ)[ρ_T-split] (+ imported
  layer: (E⁺), frozen dictionary [C-dict] — B10); the exhaustiveness step
  (every Scope-A B2 candidate enters the realization) is the Scope-A gate;
  (NECK) = standing dictionary-extension request (`θ_coh`, `ℓ_neck`); M2/M3
  RESTRICTED as before; BH YELLOW-RED maintained; no CAP.
- **Scope B**: B2 UNKILLED; first named gap **(SB-ANCH) ⟺ `β_v = γ` τ-uniform
  corner attainment**; rider — (SB-ANCH) alone does not transplant R-B2′
  (labelled-region/exhaustiveness required separately); one-scale ring and M2
  reopen; **M3 `γ₂ < γ`: K4′ transports as a scope-free statement, the kill
  stays conditional on (E⁺)+(P), (P)'s Scope-B status unresolved (B1)**;
  trigger (T-c) OPEN.

## 6. Next after round 2

The BH research branch is parked at its most accurate frozen state; the
remaining Scope-A content is dictionary-extension requests and the remaining
Scope-B content is (SB-ANCH) + the exhaustiveness gap — all theorem-shaped,
none commissioned. Live options (user's choice, as before): the formal (Lean)
side (Clay semantic-promotion 5 edges; glued maximal trajectory `u*`); the
deliberately deferred list (the `β_v ∈ (α,γ]` limb is now doubly relevant as
(SB-ANCH)'s home, but its analysis is theorem-shaped and needs explicit
commissioning); or literature watch per B5's re-check condition.
