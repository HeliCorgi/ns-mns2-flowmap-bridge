# R-B2′ anchor-independence audit — fork (b): no scope-free substitute; (SB-ANCH) named; B2 UNKILLED in Scope B; the true Scope-A gate is the exhaustiveness step

Date: 2026-08-21 (JST). One bounded repo-only classification pass (3 Opus analysts
(chain enumeration / substitute search / B2-class definition) + adversarial
verifier + critic; no literature, no numerics, no proof attempts; **record-only —
all wordings PROPOSED, NOT EXECUTED**). Baseline: `BH_SCOPEB_RECON_2026-08-21.md`
§8. Tags: [H] hard, [V] read first-hand (≥2 agents or critic-confirmed), [D]
derived composition (inputs listed), [C] conditional, [B] bookkeeping.

## 1. RULING: fork (b) — with two riders that change what (b) means

**No already-recorded scope-free object supplies the re-centring anchor on B2.
F22 is substantive; (SB-ANCH) is the named primary Scope-B gap; B2 is recorded
UNKILLED in Scope B.** Fork (c) does not fire under the operative test (no
**unlisted** Scope-A consumption — §3); one program-decision item rides the next
agenda (C6, §5).

**Rider 1 — the Scope-A gate is the EXHAUSTIVENESS step, not the zoom.** The
chain's machinery (extraction, boost, ledger arithmetic, KNSS 5.3) is scope-free;
what Scope A supplies is "every Scope-A blob is two-scale in the tongue sense"
(multiregion §2 [V]) — i.e. that *every* B2 candidate has a Γ-saturated level at
amplitude-corollary radius. R-B2′ is **already scope-free on the sub-class
`B2 ∩ {β_v = γ}`** (structurally the same object as the tie face); what Scope B
loses is the theorem that this sub-class *is* B2.

**Rider 2 (verifier's, sustained) — a granted (SB-ANCH) would still not make
R-B2′ statable in Scope B.** It supplies a level, not a *labelled region* with a
poloidal-exponent and thickness assignment; the ledger's tip/solenoid rows and
(E⁺⁺)/(NECK) remain "undefined, not open" there. **Outcome (a), had it fired,
would not by itself have killed B2 in Scope B.**

## 2. (SB-ANCH)-final [proposed wording F32]

The seeded co-location reduction is **CONFIRMED but is not an equivalence**: from
Γ-max, `|u_θ(x)| ≥ c‖u‖_∞ ⟹ r(x) ≤ Γ₀/(c‖u‖_∞)` — the recon's *distance clause
is derivable and deleted as a hypothesis*. But the terminal contradiction needs
Γ-saturation at the same point, and Γ-max bounds `r` above, never below:

> **(SB-ANCH).** ∃ `c, c′ > 0` τ-uniform and, along `τ_k → 0`, points `x_k` with
> **(H1)** `|u_θ(x_k,t_k)| ≥ c‖u(t_k)‖_∞` (*sup-swirl saturation*) and
> **(H2)** `|Γ(x_k,t_k)| ≥ c′Γ₀` at the **same** point (*co-located
> Γ-saturation*). `(H2) ⟹ (H1)`; `(H1) ⇏ (H2)`.
> Single equivalent form: the scope-free envelope `Γ(r) ≤ min(Γ₀, r‖u‖_∞)` is
> **attained within a constant at its corner** `r = Γ₀/‖u‖_∞` — i.e. **`β_v = γ`
> with a τ-uniform constant**, precisely the top endpoint of A2's *unanalysed*
> middle limb `β_v ∈ (α, γ]`.
> Scope A supplies (H1) with `c = 1/√42`; it supplies (H2) only on `ρ_T = γ`.
> Rider: (SB-ANCH) supplies a level, not a labelled region (Rider 2 above).

## 3. The consumption ledger (chain walk s1–s10, critic-adjudicated)

- **Anchor-touch sites: 4; irreducible: 1** (s1, the zoom-centre selection —
  ring audit §7: "a.e.-regularity supplies one, **since `|u|² = 2A(ψ)`**" [V];
  "(A2) `p = p(ψ)` — added, not removable" [V]). s3 (bounded axis distance) =
  anchor ∘ Γ-max [D]. The labelled-region consumption and the (N-Γ)/terminal
  target are covered or split (below).
- **All five `√42` constants are anchor-inherited and the chain is
  constant-agnostic** [D]: `M̃_k ∈ [H/√42, H]`, `|ṽ| ≤ √42`, `M̃_k|x̃′_k| ≤
  √42Γ₀`, `R₀ = 10√42Γ₀`, neck span `[√42Γ₀, R_k]` all trace to the single
  `1/42`; no downstream step uses its value — **any τ-uniform `c` substitutes
  verbatim**. The Scope-A dependence is existential, not quantitative.
- **Ledger labelled regions — SPLIT ruling (C1)**: the exponent data
  (`ρ_T ≥ γ`, `σ_tip = γ`, `Γ_L ≍ τ^{ρ_T−γ}`) is corollary-derived (multiregion
  §2 derives it in one sentence [V]); the region **thickness/boundary-length**
  data feeding `κ_R ≤ ‖u_pol‖_∞|∂R|` is dictionary data no point-anchor yields.
  Both are **listed by reference** (ledger §1 prints `frozen dictionary [C-dict]`
  as a chain input; A15 imports the ledger by name) ⟹ not (c); transparency
  repair F30.
- **Scope-free confirmed** [V]: extraction machinery (Prop 6.1/Lemma 6.1,
  rate-free), axial boost (by derivation), kernel facts, `R_k`/Taylor remainder,
  KNSS 5.3 as a statement, `w ≡ 0 ⟹ v = b·e_z ⟹ Γ ≡ 0`.
- **New debt found (C5, F31)**: (N-Γ)'s *printed* discharge route ("dischargeable
  from τ-uniform saturation") is **not supplied by the anchor on the `ρ_T > γ`
  sub-branch, even in Scope A** — the second scale is capped, not pinned
  (multiregion §2 [V]); Γ's exact scale invariance (A11) makes the rescaled `Γ`
  at the centre `≍ τ^{ρ_T−γ} → 0` there [D]. A debt on a *listed* conditional,
  not an unlisted consumption.
- **Fork-(c) test, executed** (§1 table of the critic's record): zoom centre =
  the anchor itself; s3 derived; ledger regions listed-by-reference; (N-Γ)
  printed-with-unsupplied-discharge; the frontier qualifier-dropping (T3, below)
  sits in prose, not in the row's conditionality list. **No unlisted
  consumption.**

## 4. The target partition (F33 / F22-final, proposed)

By the Γ-saturation-scale exponent `β_v` (kill-table A2):

- **(T1) tie face** (`ρ₁ = σ₁ = γ₁ = γ` by class definition [V]): the anchor is
  the class definition; **R-B2′ is scope-free there**, modulo (E⁺⁺)/(P); (N-Γ)
  is definitionally discharged [D].
- **(T2) `B2 ∩ {β_v = γ}`** ("a B2 point wearing a Γ-saturated sub-core" [V]):
  on B2 the anchor is **derived from the Scope-A amplitude corollary**; in Scope
  B the sub-class has **no defining clause anywhere in the corpus — undefined,
  not merely anchorless**.
- **(T3) `B2 ∩ {β_v < γ}`** (incl. K9's razor limb `β_v = α`): **empty in Scope
  A by the amplitude corollary; live and never claimed in Scope B.** R-B2′ has
  no zoom centre there. K9's razor records the *negation* of sup-swirl
  saturation on this limb (`sup_core|u_θ| ≍ τ^{−α} = o(‖u‖_∞)`).

Consequently the frontier lines printing "Scope-A quasi-static = B2 alone, killed
by R-B2′" without the "Γ-saturated" qualifier are sound only because **T3 = ∅ in
Scope A** — they must be narrowed (F33). Also: A15's "the Γ-saturated level
(amplitude corollary)" → "the **amplitude**-saturated level" (the corollary caps
`Γ(L)`, it does not bound it below) (F31).

## 5. Contradictions adjudicated (critic; compact)

C1 labelled regions: **split** — exponents anchor-derived, thickness
dictionary-only; chain analyst's blanket claim dies, verifier's residue
sustained, no (c). C2 the substitute analyst's "(T1) ≡ (T2)" equivalence —
**false** ((H1) ⇏ (H2)); (SB-ANCH) prints as a conjunction. C3 anchor-site
counts — compatible, re-indexed. C4 "narrowing B2 to `β_v = γ` is a new
assumption" — **re-scoping what the row claims is free; the coverage claim
"that sub-class is B2" is not** — content survives relabelled as the
exhaustiveness gate. C5 (N-Γ) — demoted to "printed, discharge unsupplied"
(§3). C6 **fork-(c) definition mismatch**: recon §8's printed wording
("consumes the anchor more than once") *would* have fired (≥2 sites); this
pass's operative test ("unlisted consumption") does not — **program item for
the user, riding the next agenda**. C7 the M3-gate defeater-face quote —
mis-aimed at the un-re-centred maximizer; conclusion survives. C8 the claimed
"printed-conditionality gap" — demoted to transparency (A16 legend + `[C-dict]`
reference cover it). C9 a brief-level doc-pointer error (B2/K9 content lives in
the multiregion audit, not the swirl probe) — the brief was wrong, not the map.

## 6. Consolidated freeze queue (F21–F29 standing; NEW F30–F36; all proposed, not executed)

**F30** A15 transparency: name `(E⁺)` / `frozen dictionary [C-dict]` in the
printed conditionality list. **F31** A15 precision ("amplitude-saturated level")
+ the (N-Γ) discharge debt on `ρ_T > γ`. **F32** replace the recon's one-line
(SB-ANCH) with (SB-ANCH)-final (§2); F26 cites F32. **F33** F22-final: the
(T1)/(T2)/(T3) split, "aggregation machinery" repair, frontier-qualifier
narrowing (supersedes F22 as printed). **F34** naming collision: rename one of
(ANCH) (the κ̂-level statement inside (E⁺⁺)) vs the re-centring anchor /
(SB-ANCH). **F35** legend: add `β_v` (co-location) beside `σ_core`/`σ_sup`.
**F36** internal consistency: reconcile the two fork-(c) wordings (C6); fix the
doc pointer (C9).

## 7. The single next bounded task

**Second freeze-review round over F21–F36 — the user sees the agenda first**
(confirmed by the critic; F31/F32/F33 all edit the map's one remaining
conditional kill row and its frontier prose, and F33 changes what the map
*claims* — T3 was never established scope-free). C6 rides the agenda as a
program-decision item. Rejected alternatives: analysing the `β_v ∈ (α,γ]` limb
(theorem-shaped, forbidden); re-basing (E⁺⁺) scope-free (dictionary-extension
work, not classification).

## 8. Stop-rule audit (whole pass)

CLEAN. Repo docs only; no numerics; no proof attempts — six [D] compositions
carried with inputs; no map edits (every wording "proposed, not executed"); no
promotions, four demotions executed; failures recorded with exact deficits (K9
at the wrong radius by `τ^{α−γ}`; B2's on-axis geometry by the same factor;
[H8]/CKN refuted on record; one unsound [D] and one false equivalence recorded
as analytic errors).

Docs read first-hand: `BH_SCOPEB_RECON_2026-08-21.md`,
`TYPE2_KILL_TABLE_2026-08-19.md` (+ both annotations),
`BH_CONSTANT_EXCLUSION_ROW_2026-08-20.md`, `BH_BIOT_SAVART_LEDGER_2026-08-20.md`,
`BH_MULTIREGION_AUDIT_2026-08-20.md`, `BH_M3_KNSS_GATE_2026-08-20.md`,
`BH_RING_COROLLARY_ADVERSARIAL_AUDIT_2026-08-19.md`,
`BH_GEO_SWIRL_AGGREGATION_2026-08-19.md`, `FREEZE_REVIEW_2026-08-21.md`,
`TYPE2_SURVIVAL_MAP_2026-08-18.md`, `D1_ADVERSARIAL_AUDIT_2026-08-19.md`.
