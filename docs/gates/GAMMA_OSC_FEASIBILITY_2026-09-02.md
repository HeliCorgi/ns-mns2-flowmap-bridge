# The (Γ-OSC) feasibility decision — EXECUTED

**Ruling: `VIOLATED`.** Frozen B2 implies **no** known-sufficient drift condition for the τ-uniform oscillation contraction, and an explicit smooth, divergence-free, axis-regular, finite-energy, frozen-row-compatible profile family **defeats every weakest known-sufficient rung of both ladders, τ-uniformly, on all of `S_blob`**. Fixed meaning (verdict-semantics guard, third level down): *the known drift–diffusion / boundary-regularity machinery route to (Γ-OSC)_op is closed at frozen scope.* No stronger reading may be cited: `VIOLATED ⇏ ¬(Γ-OSC)_op` and `VIOLATED ⇏ ¬(Γ-DEP)_fld` — both remain sufficient-condition objects (C0, at every level of the chain).

Date: 2026-09-02 (JST). Commissioned by the user's explicit instruction of 2026-09-02 — the reduced Γ-OSC feasibility decision of `FREEZE_REVIEW_3_2026-09-01.md` §8 (drafted in `EXT_GAMMADEP_DECISION_2026-09-01.md` §A2, adopted §B2.3): *exhibit the weakest quantitative drift condition sufficient for the τ-uniform contraction `osc_{Q_{θR}}Γ ≤ q·osc_{Q_R}Γ` at core scale `R = τ^α`; verify both directions (IMPLIED by frozen B2 / VIOLATED by a frozen-compatible smooth divergence-free profile / UNDERDETERMINED); no new exponent restriction as a substitute.* Frozen Scope-B unchanged.

**RECORD-ONLY.** No map edit, no promotion, no demotion, no re-armed row, no CAP trigger. Every map consequence in §6 is a **proposal for the next user-adjudicated freeze review**; the §7 termination consequence is the mechanical application of the **registered** rule, whose execution is a user act. Verdicts unchanged pending that review: **BH stays YELLOW-RED; B2 stays UNKILLED in Scope B; (T-c) stays OPEN.**

Method: 1 statement fixer → 3 ladder/inventory lanes (generic drift-diffusion; axisymmetric-specific; frozen-B2 drift-norm inventory) → 2 builders (violation-profile constructor; standing-test-case office running the mandatory §B2.4 discipline) → 2 adversarial breakers (IMPLIED-direction breaker hunting a missed derivation; VIOLATED-direction breaker + ladder auditor hunting a surviving rung) → 1 adjudicator, all reading the frozen sources first-hand. Literature fetched first-hand at full-text/verbatim level this pass: arXiv:2210.10030 (Ożański–Palasek), arXiv:1010.6025 (SSSZ), arXiv:2106.02408 (Wu), arXiv:1205.4364 (SVZ), arXiv:1011.1888 (Nazarov–Ural'tseva), arXiv:2109.09344 (Seregin, twice, verbatim-equation pass), arXiv:1410.6260 (Pan), arXiv:2201.01766 (Chen–Tsai–Zhang), arXiv:1704.02173 (Qian–Xi), plus abstract-level arXiv:1508.03318 (Wei), arXiv:1206.4567 (Kubica–Pokorný–Zajączkowski). The IMPLIED breaker failed to break ¬IMPLIED and strengthened it; the VIOLATED breaker failed to break the violation and completed the ladder; disagreements among offices are resolved explicitly in §2a. Debts **D-1 and D-2 are discharged** this pass (§5).

---

## 1. The decision, as fixed

Fixed uniquely by the statement-fixer record of 2026-09-02 (consumed here without modification; essentials restated for self-containment):

**Class B2** [V]: `u` axisymmetric finite-energy suitable weak solution, standard `ν`, first singular time `T*`, Type II, `‖u(t)‖_∞ ≍ τ^{−γ}`, core scale `≍ τ^α`, `(γ,α) ∈ S_blob = {1/2 < γ < 1, max(1−γ, 2γ/3) ≤ α < γ, α > 2γ−1}` (A7 strict); `Γ := r u_θ`, Γ-max `‖Γ‖_∞ ≤ Γ₀`, non-evanescence `liminf ‖Γ‖_∞/Γ₀ ≥ c₀ > 0`, envelope `|Γ| ≤ min(Γ₀, r‖u‖_∞)`; **no Scope-A hypothesis**. Γ-equation drift `b = u + 2νe_r/r`, divergence-free for `r > 0`; in axisymmetry `u_θ` is **absent** from the drift.

**(Γ-OSC)_op**: ∃ `(θ, q, τ₀)`, chosen **before and uniformly in** `(τ, z*, k)`, such that for all `0 < τ < τ₀`, all `z* ∈ ℝ`, all `k < N(q,c₀) := min{n : 2qⁿ < c₀}`: `osc_{Q_{θ^{k+1}τ^α}(z*,t_τ)}Γ ≤ q·osc_{Q_{θ^kτ^α}(z*,t_τ)}Γ`, on axis-centered closed parabolic cylinders of viscous depth `ν^{−1}R²`, shared top time `t_τ = T*−τ`. τ-uniformity of `(θ,q)` is the entire content. Chain, C0-clean at every link: `(Γ-OSC)_op ⟹ (Γ-DEP)_fld ⟹ β_v ≤ α ⟹ middle limb closed` (re-verified; consumes only axis Dirichlet + Γ-max).

**Weakest condition**: minimal element(s) under `⪯` (A ⪯ B iff every drift in the declared class satisfying B satisfies A with controlled constant, and A still suffices by a cited theorem) on declared drift classes `𝒟_gen ⊃ 𝒟_df ⊃ 𝒟_NS`; if not totally ordered, the deliverable is the antichain of minimal rungs and both tests run against every minimal element. Five-column rung schema (R1 condition/class, R2 theorem/source, R3 rescaled τ-value, R4 axis coverage, R5 structure consumed). **IMPLIED**: frozen rows force the rung's condition τ-uniformly, uniformly in `z*`, from the permitted inputs only; partial IMPLIED admissible only with R5 naming the consumed NS structure and the derivation printed. **VIOLATED**: an explicit smooth/div-free/axisymmetric/axis-regular/finite-energy family, row-check table in W★ format (each row at its own radius, `τ^{α−γ}` misfire check mandatory), drift violating **every** minimal rung τ-uniformly. Mandatory test case: the standing counterprofile of `EXT_GAMMADEP_DECISION_2026-09-01.md` §A.3, run per §B2.4. Prohibition: no rung may be, and none may be rescued by, an exponent inequality; exponent arithmetic appears only as R3 evaluation. Obligations O1–O9 (C0 language; τ-uniformity ledger; K9-style correct radius/region/**time** in both depth windows; no new exponent restrictions; D-1/D-2 discharge conditions; termination rule; separation of ladder and profile work products) all in force.

---

## 2. Ruling

**`VIOLATED`** — adjudicated on the **weakest adjudicated rungs**, i.e. on every minimal element of the known-sufficient antichain after the ladder audit (§3):

> on `𝒟_gen`: **L3**; on `𝒟_df`: **{L4a, L4b}** (incomparable); on `𝒟_df`-with-axis-Dirichlet: **AX-L5**; on `𝒟_NS`: **AX-L7** — the weakest rung overall for the commissioned conclusion shape.

**Both directions were adjudicated at every minimal rung, and they agree:**

- **IMPLIED: fails everywhere, and is refuted, not merely unestablished, for the norm-size rungs.** The frozen-B2 inventory plus the adversarial re-derivation (interpolation over all mixed indices, both O3 time windows, localized budgets, meridional-only refinement, 5-D reformulation, time-averaging) shows every frozen-derivable rescaled drift norm diverges at a positive power on **all** of `S_blob`: sup and critical `L^∞_tL³` at `ν^{−1}τ^{−(γ−α)}` — **forced**, both directions pinned, by K6-carrier + Γ-max/envelope (the drift components carry the entire critical divergence; the only enveloped component, `u_θ`, is not in the drift); best critical-index route `τ^{−(γ−max(α,1/2))}`; global floor over all indices `τ^{−α(2γ−1)}`, attained only at supercritical indices no rung consumes; sub-scale-sup (Morrey/𝒩-type) norms bounded below at rate `γ−1/2` by the viscous-scale sub-cylinder; CTZ's local energy **forced** at `ν^{−2}τ^{−2(γ−α)}` class-wide; sign-restricted caps **anti-implied** (violation forced by incompressibility + printed blob shape). The zero sets of every rate function lie in the excluded boundaries `{γ = 1/2}` (K2, Type II) and `{α = γ}` (K6): **the implied sub-region of `S_blob` is exactly empty**, and no log rung is reached (fixed power vs `lnln`, τ-uniform margin). Stall row, identical at every rung: **no frozen row supplies any τ-uniform scale-critical bound on `u` at core scale.** The single cell not closed by the inventory (cancellation-type `BMO^{−1}`, recorded nowhere in the corpus) is closed at profile level (§3, L4a). **Partial IMPLIED: not established** — every candidate R5 structure either is already consumed by a violated rung (div-free; axis Dirichlet device; the `2νe_r/r` term; suitable-weak local-energy structure; jointly consumed with enstrophy-type bounds by Wu Thm 1.8's exact-Γ-operator defeater and still failing) or carries **no known theorem** (actual nonlinear coupling; sign of `u_r` on the axis; drift–level-set alignment; K11's viscous-scale placement; the axis trace residual). No derivation exists to print, so no partial IMPLIED can be returned.
- **VIOLATED: established per the fixed §4 test.** The exhibited family `𝒱` (§3.2) — the standing counterprofile extended by exactly two declared items (coherent normalization of `Φ`; the kinematic time assignment `U(x,s) = u_{τ(s)}(x)`) — is smooth, divergence-free, axisymmetric, axis-regular (`Γ = O(r²)`), finite-energy, defined on **all of `S_blob`**; its row-check table (§3.3) passes every frozen row at its own radius; and its drift violates **every minimal rung** τ-uniformly at power rates versus (at most) double-log caps, with the mandatory misfire check run at every line. The multi-scale escape is closed at its sharp threshold (§4). The violation was independently re-audited by the adversarial breaker: PASS (three harmless nits, adopted in §3.3).

**Certificate conditionality.** K4′ is consumed nowhere — no `[(E⁺)+(P)]` print is owed. K8 `[C on (T)]` and K10 `[C]` were checked and do not fire on `𝒱`. The verdict's only `[C]`: time-integrated rung norms (AX-L7's `M`; finite-`ℓ` members of L2/AX-L5) are evaluated on the **declared** kinematic scan — a component of the exhibited family, audited conflict-free against every frozen row, claiming no PDE solved. **Every declared drift class additionally contains a snapshot-violated minimal member** (L3, L4a, L4b, AX-L5 at `(ℓ,q) = (∞,3)`), so the machinery-closure ruling survives even a snapshot-only reading; under that reading AX-L7's violation row carries the `[C-sust]` flag explicitly. Residual `[V?]` items are itemized in §5 and each is non-load-bearing, with reasons.

**Claim boundary.** This is a statement about derivability at frozen scope. **No Navier–Stokes solution is constructed; nothing here asserts blow-up or regularity for Navier–Stokes; no Clay claim of any kind is made or implied.** `𝒱` solves no PDE — consistent with the frozen escalated no-go (any (Γ-DEP)-type proof must be dynamical), which this pass sharpens to rung granularity (§4).

### 2a. Disagreements resolved during the pass

- **DR-1 (test office F-1 vs Lane 1, L3 row — error confirmed, corrected).** Lane 1 printed a divergent swirl contribution `ν^{−1}Γ₀τ^{α−β}` to the critical `L^∞_tL³` norm; recomputation (test office, independently confirmed by the VIOLATED breaker) shows the swirl's own-support `L³` contribution is τ-uniform `O(ν^{−1}Γ₀)` (exact scale-cancellation — the exactly-critical invisibility); `τ^{−(β−α)}` is the swirl's contribution to the **sup** constant at core radius (real, subdominant). §3 prints the corrected figure. Conclusion-preserving (poloidal primary dominates either way).
- **DR-2 (test office F-2 vs Lane 2, AX-L7 — discharged, violation upgraded to unconditional).** The test office correctly flagged that under the literal paraphrase (`f = R^{−1/2}·L³` space-time) the violation would fail on a sub-wedge. The verbatim fetch resolves it: that paraphrase was wrong — Seregin's `f` is `L⁴_tL³_x`-based (a rendering residue `[V?]` remains on its prefactor/outer-power placement), and `M(R) = R^{−1/2}‖v‖_{L^{10/3}(Q(R))}` is unambiguous and exactly scale-invariant. **`M` alone carries the AX-L7 violation on all of `S_blob`** (rates `γ−α` for `α ≥ 1/2`, `(10γ−4α−3)/10 > 0` for `α < 1/2`). Lane 2's rung text is corrected accordingly; the sub-wedge worry evaporates.
- **DR-3 (Lane 1's "no supercritical rung in the generic lane" vs the ladder auditor's ADD-1 — statement narrowed).** Qian–Xi (arXiv:1704.02173) is a genuine supercritical positive on `𝒟_df`, `n ≥ 3` — at heat-kernel-bound level. It is the **unique known theorem whose hypothesis frozen B2 actually pays** (the `c = 3/2` Leray family, τ-uniformly, via K5a+K5b), but its conclusion (kernel bounds with stretched-exponentially degenerate lower-bound constants on sub-parabolically shrinking balls; no Harnack/modulus below the critical endpoint) cannot feed (Γ-OSC)_op — far below even the polynomial-degradation regime §4 closes. Adjudicated wording: *no supercritical rung with an oscillation/Harnack/modulus conclusion exists*; the §2-conclusion-capable antichain is unchanged; IMPLIED gains nothing.
- **DR-4 (5-D reformulation print — breaker's algebra adopted; erratum against both lane prints).** `Φ = Γ/r²` does **not** solve the clean transport equation: the zeroth-order term does not cancel for `Γ/r²` (it cancels for `ω_θ/r`), and the correct clean statement is the **divergence form** `∂_tΦ + div₅(b₅Φ) = νΔ₅Φ` with `div₅ b₅ = 2u_r/r ≠ 0` [H]. Lane conclusions unchanged and sharpened: `𝒟_df` rungs do not transfer through the 5-D route; the Φ-level maximum principle is lost; back-transfer needs the unbounded `‖Φ‖_∞`; axis coverage in the literature comes from the 3-D form + Dirichlet device only; and the unsigned axis compressibility `2u_r/r` is named as the exact quantity any future axis-structure partial IMPLIED must control — no frozen row signs `u_r`.
- **DR-5 (AX-L5 deep-window rate — builder's figure sharpened, safe direction).** The finite-`ℓ` deep-window violation rate is not the floor `α(2γ−1)`: `𝒩`'s built-in sup over sub-scales `R' ≤ R` is driven by the viscous-scale sub-cylinder to rate `γ − 1/2` — the Type-II margin itself. Adopted.
- **DR-6 (axis-term flux constant).** The distributional divergence of `2νe_r/r` is a positive line measure of flux `4πν` per unit length (not `2πν`). Cosmetic; the sign, and the L4b axis obstruction, stand.
- **DR-7 (breaker nits N1–N3 on the profile — adopted).** `E_pol ≍ 1` (bounded, not vanishing) on the envelope edge `α = 2γ/3` — K5a needs boundedness only; the `max(χη) = 1` normalization is made explicit; the `[C-sust]` inventory stands as itemized in §2.

---

## 3. The condition ladder, as adjudicated

### 3.1 Rungs

Statuses: **B2** = IMPLIED-direction status on frozen B2; **𝒱** = status on the violation family (§3.2); **u_τ** = status on the standing counterprofile as printed (EXT §A.3), per the mandatory §B2.4 test. All rescaled values on `Q_{τ^α}(z*, T*−τ)` per the fixed convention; rates positive on all of `S_blob`; `γ−α ∈ (0, 1/4)`.

| Rung (R1 condition; class) | Source (R2) | Tag | B2 | 𝒱 | u_τ |
|---|---|---|---|---|---|
| **L1** bounded drift `R‖b‖_∞/ν`; `𝒟_gen` | Krylov–Safonov, Izv. 44 (1980); Aronson–Serrin, ARMA 25 (1967) | [V?] originals unfetched (dominated: violating L3 ⪯ L1 violates L1) | NOT implied — **forced** divergent `ν^{−1}τ^{−(γ−α)}` (§4 forced-amplitude lemma) | violates, snapshot, same rate | violates, snapshot |
| **L2** subcritical Lebesgue `2/p + n/q < 1` (n = 3; n = 5 strictly harder); `𝒟_gen` | Aronson–Serrin; LSU Ch. III | [V?] | NOT implied — best rate `e(c) > 0` on all `S_blob`, all `c < 1` | violates (`L^∞_t` members snapshot; finite-`ℓ` [C-sust]) | violates (same split) |
| **L3** critical `L^∞_tL³` / critical-Morrey-form-bounded, smallness where required; `𝒟_gen` — **minimal on 𝒟_gen** | Aronson; Fabes–Stroock (per Wu Thm 1.1); NU Thm 2.5′ | [V] | NOT implied — **forced**, pinned both directions `≍ ν^{−1}τ^{−(γ−α)}` | violates, snapshot, `ν^{−1}τ^{−(γ−α)}‖B‖_{L³}`; swirl contribution τ-uniform `O(ν^{−1}Γ₀)` (DR-1) | violates, snapshot; poloidal-driven only |
| **L4a** `b = div d`, `d` skew `∈ L^∞_tBMO_x`; `𝒟_df` — **minimal on 𝒟_df** (antichain with L4b); axis: interior-only in 3-D form (line-measure divergence, DR-6) | SSSZ arXiv:1010.6025, JDE 252 (2012), Thm 1.1 + Lemma 2.9 | [V] | NOT implied — the corpus's unique open cell: neither controlled nor forced class-wide; **closed at profile level →** | violates, snapshot: `‖d̃‖_{BMO} ≍ ν^{−1}τ^{−(γ−α)}`; fixed-shape homogeneity (`b̃ = ν^{−1}τ^{α−γ}B(y)`, `‖B‖_{BMO^{−1}} = c_Φ > 0`) forecloses cancellation by any stream choice [H] | violates on the coherent-Φ member (generic in Φ; forced under 𝒱's declared normalization) |
| **L4b** critical Morrey `M^{n/q−1}_q` + `div b ≤ 0`; `𝒟_df` — **minimal on 𝒟_df** | NU arXiv:1011.1888, Thms 2.5/2.5′/3.7 | [V?] weight convention owed (non-load-bearing: any scale-critical normalization of the flat blob diverges at the same power) | NOT implied — Morrey norm forced divergent; axis-centered cylinders not literally covered (div-sign fails in the wrong direction at the axis) | violates, snapshot (`div b̃ = 0` holds — the cap fails) | violates, snapshot |
| **AX-L5** `𝒩(R) = 2 + sup_{R'≤R}(R')^{−a}‖b‖_{L^ℓ_tL^q_x}` bounded; `𝒟_df` + axis Dirichlet (device (48)) — **minimal on that class**; axis-covered | Ożański–Palasek arXiv:2210.10030, Annals of PDE (2023), Prop 5.1 (**D-1 discharge**; contraction exponent `exp(−𝒩^{O(1)})` — doubly degenerate) | [V] | NOT implied — `𝒩(τ^α) ≳ ν^{−1}τ^{−(γ−α)}` pinned; fixed `(θ,q)` unreachable | violates: `(∞,3)` pair snapshot at `τ^{−(γ−α)}`; finite-`ℓ` deep window at rate `γ−1/2` (DR-5) | violates, snapshot at `(∞,3)` |
| **AX-L6** pointwise `r\|b_mer\| ≤ (ln\|ln(r/3)\|)^a`, `a ≤ 0.028`; `𝒟_df` meridional + Dirichlet; per-step factor `1−c/\|ln R\|` degenerate — (Γ-DEP)-route only, not §2-shape | Pan arXiv:1410.6260, JDE 2016, Thms 1.1–1.2 | [V] | NOT implied — power vs double-log | violates, snapshot; failure annulus nonempty for all small τ | violates, snapshot (∀t hypothesis) |
| **AX-L7** `f + M ≤ c₊ln^{a_S}ln^{1/2}(1/R)`, `a_S ≤ 1/224`, all axis points, all scales; `𝒟_NS` — **minimal on 𝒟_NS; weakest §2-shape rung overall** (Prop 1.4: genuine fixed-power oscillation decay) | Seregin arXiv:2109.09344, JMFM 24 (2022) Paper 18, Thm 1.3 + Prop 1.4; `M = R^{−1/2}‖v‖_{L^{10/3}(Q(R))}` verbatim (DR-2) | [V] (residual [V?] on `f`'s prefactor rendering — non-load-bearing, `M` carries the row) | NOT implied — the cap sits on the `c = 3/2` Leray family; frozen rows give `f` divergent at rate `α/2` at core scale; dissipation tail rateless (O2) | violates via `M` alone, unconditionally on all `S_blob`: rates `γ−α` / `(10γ−4α−3)/10` [scan reading for the time integral; [C-sust]] | violates (same) |
| **AX-L8** `A(z₀,R)·ω(R)^β ≤ K`, `β < 1/8` ("almost Hölder" — the true home of the D-1 variant, **erratum**: not in OP); `𝒟_NS`; degenerate at fixed ratio — (Γ-DEP)-route only | Chen–Tsai–Zhang arXiv:2201.01766, Prop 1.2 | [V] | NOT implied — **forced** class-wide: `A(z_c,τ^α) ≳ ν^{−2}τ^{−2(γ−α)}` | violates, snapshot (`sup_t`), squared rate; swirl contribution at core radius `Γ₀²τ^{β−α} → 0` | violates, snapshot |

### 3.1a Non-rung entries (floors, boundary column, additions, context)

- **FLOOR-1/2/3** [V] (**D-2 discharge**): SSSZ Thms 1.3/1.5 (elliptic, div-free, `n = 3`: no modulus from `‖b‖_{L¹}`; BMO sharp to a log-log in `n = 2`); SVZ Thm 1.3 (ARMA 207 (2013): `n = 2`, parabolic, time-dependent, every modulus broken at any supercritical Lebesgue index); Wu Thms 1.2/1.7 (`n ≥ 3`, time-independent, div-free, `L^{n−λ}` any `λ > 0`: binary discontinuity; Harnack/Hölder/Gaussian all fail; SSSZ conjecture confirmed). **Wu Thm 1.8**: the **exact Γ-operator** with axisymmetric time-dependent div-free supercritical drift plus an enstrophy-type space-time bound — bounded `v` discontinuous: div-free + the `2νe_r/r` structure + enstrophy bounds are **jointly insufficient**. B2's core drift size `|u| ≍ |x|^{−γ/α}`, `γ/α ∈ (1, 3/2]`, sits inside Wu's admissible window. The ladder terminates exactly at scale invariance; nothing weaker than the minimal antichain exists.
- **QX** (ladder-audit ADD-1) [V]: Qian–Xi arXiv:1704.02173 — per DR-3, the unique paid-hypothesis theorem; conclusion-shape-incapable of feeding (Γ-OSC)_op. Not an antichain member.
- **SGN** (ladder-audit ADD-2) [V?/D]: sign/one-component caps (KPZ arXiv:1206.4567; Zajączkowski, Demonstratio Math. 48 (2015)) — **anti-implied**: `∫u_r(r,·)dz = 0` exactly (compact support), and K6-carrier + flux balance force both signed parts of `u_r` to full supercritical size at core radius; violation forced class-wide within the printed blob shape. No live sign rung.
- **BDY** (R4 column for the whole generic lane) [H/V? search-level, twice-searched]: no theorem yields oscillation contraction at a Dirichlet boundary at critical or supercritical drift size; the axis is codim-2, outside the codim-1 boundary-Harnack line (Ancona; Cranston–Fabes–Zhao); the meridian weight `r^{−1} ∉ L¹_loc` fails A₂/Fabes–Kenig–Serapioni; the 5-D route is divergence-form with unsigned `div₅ = 2u_r/r` (DR-4) and unbounded back-transfer. All generic rungs are interior-only at the axis; only the (48)-type Dirichlet device (AX-L5) and weighted-Poincaré/Dirichlet arguments (AX-L6/L8) cover axis cylinders.
- **Γ-level criteria** (context, circular as rungs) [V abstract-exact]: Lei–Zhang arXiv:1505.02628 (`|Γ| ≤ C_*|ln r|^{−2}`, `C_*` arbitrary ⟹ regularity); Wei arXiv:1508.03318 (`|ln r|^{−3/2}`; residual discharged this pass). They take Γ-decay as input and cap the depletion lane from above; both profiles violate them at `r ≍ τ^β` — the **only** swirl-driven violation anywhere. (Γ-DEP)_fld's shrinking-core smallness does not meet their hypotheses — no tension at frozen scope.
- **Unconditional a priori theory** [V? review-level]: Lei–Navas–Zhang-line `|u_pol| ≤ C√|ln r|/r²` is a full power above critical — feeds no rung. No unconditional Γ-decay near the axis is known for suitable weak solutions; every decay statement found is conditional on a scale-critical cap Type-II B2 does not supply.

### 3.2 The violating family `𝒱` [H]

For each `(γ,α) ∈ S_blob`, `β ∈ (α,γ)`, `m ∈ (c₀,1)` (frozen-vocabulary parameters only): `U(x,s) := u_{τ(s)}(x)`, `τ(s) = T*−s`, with `u_τ = u_{pol,τ} + (Γ_τ/r)e_θ`; swirl `Γ_τ = mΓ₀χ(r/τ^β)η((z−z*)/τ^β)`, `χ(s) = s²` near 0, `max(χη) = 1` (explicit per DR-7); poloidal `ψ_{1,τ} = τ^{−γ}Φ(r/τ^α,(z−z*)/τ^α)`, `u^r = −r∂_zψ₁`, `u^z = 2ψ₁ + r∂_rψ₁`. Exactly two declared extensions of the standing counterprofile: **(1)** `Φ` smooth, unit-ball-supported, even in its first argument, `≡ 1` on the half-ball — a coherent single-signed eddy (shape choice, not an exponent restriction; needed to pin cancellation-type norms); **(2)** the kinematic time assignment (smooth in `(x,s)`, exactly divergence-free at every `s`, matching the class asymptotics at every time; **no PDE claimed solved**). Top-slab rescaled drift: `b̃ = ν^{−1}τ^{α−γ}B(y)` with `B` fixed, smooth, div-free, `≢ 0` — every rung cap is a norm, so `‖b̃‖_X = ν^{−1}τ^{α−γ}‖B‖_X` with `0 < ‖B‖_X < ∞`: no representation can produce cancellation. The swirl is absent from the Γ-drift; the axis term rescales to the τ-independent `2e_ρ/ρ`. Correct-radius discipline (test office §4): the binding radius for every drift rung is the core scale `τ^α`; violations persist generically at the swirl radius `τ^β` (rate `γ−β`) unless `Φ(0,·) ≡ 0` is specialized — a recorded face, no verdict changes.

### 3.3 Row-check table (each row at its own radius; W★/§3 format; breaker-audited PASS)

| Row | Evaluated at | Arithmetic | Verdict |
|---|---|---|---|
| `S_blob`/A7 | printed `(γ,α)` | arbitrary member; `α > 2γ−1` **strict** consumed exactly by the K5b integral | PASS |
| K1 | global | `γ > 1/2`; enstrophy exponent `(2γ−α)/2 > 1/4` | PASS |
| K2 | global | `√τ‖U(s)‖_∞ ≍ τ^{1/2−γ} → ∞` — Type II | PASS |
| Γ-max + non-evanescence | swirl subcore | `‖Γ‖_∞ = mΓ₀ < Γ₀`; `liminf = m > c₀` | PASS |
| envelope | each r | near-axis branch clears by `Γ₀τ^{γ−β}·(r/τ^β)`; at `r ≍ τ^β` the flat branch binds (`r‖u‖_∞ ≍ τ^{β−γ} → ∞`); corner attainment fails; `β_v = β ∈ (α,γ)` — middle limb realized | PASS |
| K5a | each blob's own volume | `E_pol ≍ τ^{3α−2γ}` bounded (`≍ 1` on the `α = 2γ/3` edge, DR-7); `E_θ ≍ Γ₀²τ^β`, log-free (`s²` kills the axis log, OOV-3 hygiene) | PASS |
| K5b | genuine time integral on the scan | `∫τ^{α−2γ}dτ < ∞ ⟺ α > 2γ−1` strict (A7); swirl enstrophy `Γ₀²τ^{−β}` integrable (`β < 1`) | PASS |
| K6 | designated carrier = poloidal core | `‖u_pol‖³_{L³(core)} ≍ τ^{3(α−γ)} → ∞`, power-law (A3 premise); consistent with Leray interpolation (`α ≥ 2γ/3`) | PASS |
| K6 at subcore | swirl's own volume | `∫\|u_θ\|³ ≍ Γ₀³·Θ(1)` — exactly critical; not the designated carrier (OOV-1-type face recorded) | PASS |
| K3 | core; every `ε ∈ (0,1]` | exponent `(α−γ)+ε(1/2−α)` negative on all of `(0,1]`, both α-regimes | PASS |
| K9 (CFZ endpoint) | subcore's own radius `τ^β` | `sup r^d\|u_θ\| ≳ Γ₀τ^{β(d−1)} → ∞` ∀`d < 1`; dictionary form `σ = ρ = β` supplied | PASS |
| K9 under A1 split | which σ | `σ_sup = β` at `r ≍ τ^β`; swirl vanishes at core radius (sup-swirl-poverty, C11-consistent) | PASS |
| **`τ^{α−γ}` misfire check** | performed | every row read at its own radius/amplitude/time; the factor `τ^{α−γ}` appears **only** as the violation rate — i.e. as this decision's content, never as a transported bound | CHECK PASSED |
| K11 | printed `(γ,α)` only | `γ+α ≥ 1`; on the scan `∂_sU ≍ τ^{−γ−1}`, convection `≍ τ^{−2γ−α}` responds; no per-region K11 asserted | PASS |
| K4 (zoom route) | — | `sup r\|u\| ≍ τ^{α−γ} → ∞`: `C/r` hypothesis fails; no ancient limit taken | does not fire |
| K4‴ [D] | ancient limit | transfer row, `K = ∅`; nothing to violate | CARRIED |
| K4′ [C] | `M3 ∩ {γ₂<γ}` | family ∉ M3 — not consumed; no `[(E⁺)+(P)]` owed | not consumed |
| K7 | global | power-law divergence — no cut | VACUOUS |
| K8 [C on (T)] | blob row | on-axis, non-translating scan; conservative clause holds | N/A, PASS |
| K10 [C] | `γ+α = 1` edge | interior members untouched; on edge members the scaled-norm boundedness hypothesis fails on the family — conditional pressure does not land | N/A / PASS |
| dynamic-scaling sanity (EXT §B.7) | scan speeds | inward `τ^{β−1} ≲ τ^{−γ}`; diffusion `ντ^{−β} ≲ τ^{−γ}` | PASS |

No Scope-A object appears in any computation (checked against the banned list: amplitude corollary, per-level geometry, A4 Markov/tongue data, CLV-localizability — absent). One bad `z*` defeats each rung legitimately: every rung, and (Γ-OSC)_op itself, quantifies over all axis-centered cylinders; no location premise is consumed.

---

## 4. Why — the structural reason

All offices converged on **one** reason, confirmed independently by both breakers:

> **The frozen class forces its own drift to be supercritical at core scale, and the entire known-sufficient ladder — generic and axisymmetric alike — terminates at (at most double-log above) scale invariance.**

In detail:

1. **The divergence is forced, not merely permitted.** The Γ-drift is `u_pol + 2νe_r/r`; axisymmetry removes `u_θ` — the one component the envelope caps. K6's carrier designation + Γ-max/envelope put the entire `L³` divergence on the drift components, and a Chebyshev split yields the **forced-amplitude lemma** [D]: `|u_pol| ≳ τ^{−γ}` on a positive volume fraction of the core, τ-uniformly, for every class member — so the sup, `L^∞_tL³`, weak-`L³`, and CTZ local-energy constants diverge **by force** at `ν^{−1}τ^{−(γ−α)}` (resp. squared). The zero sets of every divergence-rate function lie in the excluded boundaries `{γ = 1/2}` (K2) and `{α = γ}` (K6): **Type II + the blob shape are exactly what keep every norm divergent.**
2. **The ladder has a proven floor.** SSSZ/SVZ/Wu show failure — binary discontinuity, not constant degradation — arbitrarily little above criticality, in `n ≥ 3` even for time-independent smooth div-free drifts; Wu Thm 1.8 defeats the **exact Γ-operator** with the exact axis term and an enstrophy-type bound at any supercritical margin. The axisymmetric literature's frontier sits at `(lnln)^{small power}` allowances (`0.028`, `1/224`, `β < 1/8`); the class exceeds every such cap by a fixed power.
3. **The multi-scale escape is closed at its sharp threshold** [D]: per-step contraction `η ~ N^{−k}` summed over the `log(1/τ)` available scales gives `log(1/τ)·τ^{k(γ−α)} → 0` for every `k > 0` — the log number of scales loses to any power. Compensation requires degradation at most logarithmic in the drift constant (`η ~ (ln N)^{−p}`, `p < 1`; `p = 1` borderline constant-dependent); known constants sit **exponentially** far away (OP: `exp(−𝒩^{O(1)})`), and at supercritical index a scale-uniform per-step contraction is barred outright by Wu.
4. **The axis is not the obstruction.** The `2νe_r/r` term is the inventory's **only** τ-uniform drift object (exactly scale-invariant; `= ∇(2ν ln r)`, `ln r ∈ BMO`; critical-Morrey `O(1)`); the Dirichlet device (48) and the axis trace `Γ = O(r²)` are satisfied by both profiles. Conversely no boundary-regularity theory reaches critical drift size at the axis (BDY, §3.1a), and the 5-D route is divergence-form with unsigned compressibility `2u_r/r` (DR-4). The whole fight is, at every rung, the meridional supercriticality `ν^{−1}τ^{α−γ}` — the corpus's recurring misfire factor, here appearing as content.
5. **No rung excludes the profiles through NS-dynamic structure.** Every excluding hypothesis in both ladders is a snapshot/kinematic norm cap; every structural hypothesis (div-free, Dirichlet line, Γ-max, suitable-weak local-energy at snapshot level) is satisfied by the frozen-compatible profiles. This confirms, at rung granularity, the frozen escalated no-go: any (Γ-DEP)-type closure must consume the time evolution — and identifies exactly what the known machinery does **not** consume (§5, structure candidates).

---

## 5. Scope of the ruling — what is NOT decided; debts

1. **Not decided: `¬(Γ-OSC)_op`, `¬(Γ-DEP)_fld`.** The rungs are sufficient conditions; their defeat closes the known-machinery route only. Both objects remain live as targets for genuinely new mathematics; C0 language remains binding at every level of the chain (no "necessary", "unique closer", "only route").
2. **Not decided: anything about Navier–Stokes solutions.** `𝒱` and `u_τ` solve no PDE; `{u_τ}` has not been made a trajectory; **no blow-up, no regularity statement, no Clay claim.** The verdict is about derivability at frozen scope.
3. **Not decided: the non-rung structure routes.** Named candidates a future partial IMPLIED (or a direct dynamical theorem) could consume, **none with a known theorem attached today**: (i) actual nonlinear NS coupling — the drift's own swirl *is* Γ; (ii) the axis Dirichlet trace of Γ before `T*` — the one structural hypothesis Wu Thm 1.8's construction does not verifiably consume (residual [V?]); (iii) a sign/structure statement for `u_r` on the axis — the unsigned 5-D compressibility `2u_r/r` (DR-4) is the exact term to control, and no frozen row signs `u_r`; (iv) drift–level-set alignment (`u_pol·∇Γ` small by geometry); (v) K11's viscous-scale placement; (vi) the quantitative frontier **shape**: a critical-index contraction with Hölder-exponent degradation `(ln N)^{−p}`, `p ≤ 1`, would re-open the multi-scale route — nothing known is within exponential distance. Recorded as candidates, consumed by nothing.
4. **Debt D-1 — DISCHARGED [V], with erratum.** OP Prop 5.1 fetched full-text: hypothesis (48), drift norm (49), conclusion `osc ≲ (r/R)^{exp(−𝒩^{O(1)})}osc`; axis-centered cylinders covered. The "almost Hölder" variant is **not in arXiv:2210.10030** — it is Chen–Tsai–Zhang arXiv:2201.01766 (AX-L8). The §A.5 negative claim ("frozen B2 does not control `𝒩` τ-uniformly") is upgraded from [V?] to [V] at inventory level and to **forced at the profile level**.
5. **Debt D-2 — DISCHARGED [V].** Exact failure scope verified first-hand (elliptic and parabolic; `n ≥ 3`; div-free; time-independence suffices; binary discontinuity; sharp against `p ≥ n`). The confirmed consequence was applied: bare-drift (Γ-OSC) is at falsity risk, any IMPLIED would have had to be partial with named structure — and none was found.
6. **Debt D-3 — carried, consumed by nothing.** Identified and triaged separately (arXiv:2606.07869, load-bearing gaps confirmed, correctness NOT ESTABLISHED, no CAP fire; master record `D3_TRIAGE_2606_07869_2026-09-02.md`); it entered no direction of this decision. The Lane-2 sweep's candidate identifier arXiv:2604.21213 is flagged to the same WATCH.
7. **τ-uniformity ledger (O2) — clean.** Every constant in every consumed derivation is a class constant (`ν, Γ₀, c₀, E, m, C₁, c₁, c₂, δ`, profile shapes) or carries its printed τ-power; the single rateless object (the dissipation tail `ε(τ) → 0`) certifies nothing and was used for nothing. Both O3 depth windows were evaluated everywhere; deep-window relief is real (floor `α(2γ−1)`; sub-scale-sup floor `γ−1/2`) and never reaches zero.
8. **Residual [V?] ledger — each item named, none load-bearing:** Seregin `f`-prefactor rendering (`M` carries AX-L7); NU Morrey weight convention (power-rate violation robust to convention); classical L1/L2 originals unfetched (dominated via `⪯` by violated [V]-sourced minimal rungs); Wu Thm 1.8 axis trace (named in §5.3, consumed by nothing); boundary-Harnack citations search-level (twice-searched; negative finding); KPZ exact exponents (anti-implied at any critical-or-stronger weight); Lei–Navas–Zhang review-level (context only); ar5iv/HTML extractions are machine-assisted quotes of posted texts, not PDF-of-record collation.
9. **Out-of-vocabulary faces — recorded, never adjudicated:** (OOV-a) condition classes strictly between fixed powers and double-logs (Osgood-type moduli) in `n ≥ 3` time-dependent — no theorem on either side in the fetched record; (OOV-b) the `p = 1` borderline of the compensation threshold (constant-dependent); (OOV-c) time laws for the class other than the declared kinematic scan — the frozen class fixes no time law, which is exactly where the `[C-sust]` flag and the finite-`ℓ` forcing residue live (closed at profile, not class, level); (OOV-d) axial core migration over the parabolic window (non-migration is not frozen); (OOV-e) the `Φ(0,·) ≡ 0` sub-core specialization (drift caps satisfiable on `Q_{τ^β}` alone; binding radius `τ^α` unaffected); (OOV-f) `𝒱`'s subcore sits on the exactly-critical `v = 3γ_j` face (as `W★`'s region S did — OOV-1 of the β_v record).

---

## 6. Consequences for the frozen map — PROPOSALS ONLY

**Nothing below is applied.** Drafted for the next user-adjudicated freeze review.

- **P1. Frontier row.** Add: *"(Γ-OSC)_feas = VIOLATED (2026-09-02, machinery closure, C0-clean): no known drift–diffusion/boundary-regularity rung is implied by frozen B2 (the implied sub-region of `S_blob` is exactly empty; sup/`L^∞_tL³`/local-energy caps forced-divergent class-wide), and a frozen-compatible smooth divergence-free family defeats every minimal known-sufficient rung τ-uniformly. Sufficient-condition semantics only; `¬(Γ-OSC)_op` and `¬(Γ-DEP)_fld` are not asserted."*
- **P2. Discharge annotations.** Mark D-1 and D-2 DISCHARGED at `EXT_GAMMADEP_DECISION_2026-09-01.md` with the D-1 erratum (the "almost Hölder" variant lives in arXiv:2201.01766, not arXiv:2210.10030).
- **P3. New record-only true statements** in the frozen vocabulary: (i) the forced-amplitude lemma (`|u_pol| ≳ τ^{−γ}` on a positive core-volume fraction, from K6-carrier + Γ-max/envelope + printed blob shape); (ii) the anti-implication of sign-restricted drift caps (`∫u_r dz = 0` + flux balance); (iii) the 5-D divergence-form identity `∂_tΦ + div₅(b₅Φ) = νΔ₅Φ`, `div₅b₅ = 2u_r/r` (superseding the clean-transport print wherever it appears); (iv) Qian–Xi as the unique known paid-hypothesis theorem, conclusion-shape-incapable; (v) the `(ln N)^{−p}, p ≤ 1` compensation threshold as the named quantitative frontier shape.
- **P4. Standing test cases extended.** Adopt `𝒱` (coherent-eddy + declared time assignment) alongside the §A.3 snapshot as standing test objects for every future Γ-depletion-type hypothesis, under the same §B2.4 discipline.
- **P5. Structure-candidate register.** Record §5.3's list (i)–(vi) as the exhaustive-at-this-pass register of non-rung structure a future dynamical closure could consume — sufficient-condition candidates only, no exclusivity claimed.
- **P6. Pivot execution** (user act, per §7): select among the recorded options; the D-3/Seregin watches continue regardless at literature level.

---

## 7. The registered termination rule, applied mechanically

The rule, as registered (`FREEZE_REVIEW_3_2026-09-01.md` §6 condition (3), refined by its §8 addendum adopting `EXT_GAMMADEP_DECISION_2026-09-01.md` §B2.5), verbatim in its operative form:

> *"the branch continues past the feasibility decision **only on IMPLIED** (or a partial IMPLIED naming the consumed NS structure). **VIOLATED** closes the generic-machinery route and drops the Γ-depletion lane's expected value to near-nil — end/pivot justified; a second consecutive **UNDERDETERMINED** exhausts the in-house inventory — the BH branch ends. Pivot options unchanged (the `SPEC.md` numerical candidate program; literature-level Seregin watch)."*

Mechanical application to this pass's verdict:

- The verdict is **VIOLATED** (not IMPLIED, not partial IMPLIED — no consumed-structure derivation exists to print; not UNDERDETERMINED, so the two-consecutive-UNDERDETERMINED clause does **not** fire).
- Therefore the **VIOLATED clause fires**: *the generic-machinery route is closed and the Γ-depletion lane's expected value collapses to near-nil — **end/pivot of the BH branch is justified**.* The branch does not continue on this lane.
- Recorded pivot options, printed as registered: **(a) the numerical candidate program under `SPEC.md`; (b) the Seregin-class ancient-Euler Liouville watch at literature level.** (The D-3 WATCH continues independently of either choice.)
- Execution of the end/pivot is a **user act** at the next freeze review; this record applies the registered rule and stops.

---

## 8. Bottom line

The Γ-OSC feasibility decision returns **VIOLATED**: on the frozen Scope-B class, the weakest known-sufficient drift conditions for the τ-uniform axis oscillation contraction — the minimal antichain L3 / L4a / L4b (generic), AX-L5 / AX-L7 (axisymmetric), audited and completed adversarially — are not implied by the frozen rows (the implied sub-region of `S_blob` is exactly empty; several caps are forced-violated class-wide), and are all defeated, τ-uniformly at power-versus-double-log rates, by an explicit smooth divergence-free axis-regular family compatible with every frozen row. The single driver is the class's own forced supercritical meridional drift `ν^{−1}τ^{α−γ}` at core scale; the axis structure is never the obstruction; the known ladder provably terminates at scale invariance (SSSZ/SVZ/Wu), and the multi-scale escape closes at the `(ln N)^{−p}, p ≤ 1` threshold, exponentially beyond every known constant.

Per the registered termination rule, **end/pivot of the BH branch is justified**; the recorded pivot options are the `SPEC.md` numerical candidate program and the literature-level Seregin watch. (Γ-OSC)_op and (Γ-DEP)_fld remain sufficient-condition objects — nothing here asserts their negations; the surviving routes to them are the named non-rung NS structures of §5.3, none with a known theorem today. **No Navier–Stokes solution is constructed; no blow-up or regularity for NS is asserted; no Clay claim of any kind is made or implied.**

Frontier status after this pass (unchanged pending freeze review): **BH YELLOW-RED · B2 UNKILLED in Scope B · (T-c) OPEN · no CAP trigger · no Clay claim.**

---

Sources (first-hand this pass unless noted): Ożański–Palasek, Annals of PDE (2023), arXiv:2210.10030 · Seregin–Silvestre–Šverák–Zlatoš, JDE 252 (2012) 505–540, arXiv:1010.6025 · Wu, JDE (2024), arXiv:2106.02408 · Silvestre–Vicol–Zlatoš, ARMA 207 (2013) 845–877, arXiv:1205.4364 · Nazarov–Ural'tseva, St. Petersburg Math. J. 23 (2012), arXiv:1011.1888 · Seregin, JMFM 24 (2022) Paper 18, arXiv:2109.09344 · Pan, JDE (2016), arXiv:1410.6260 · Chen–Tsai–Zhang, arXiv:2201.01766 · Qian–Xi, arXiv:1704.02173 · Kubica–Pokorný–Zajączkowski, arXiv:1206.4567; Zajączkowski, Demonstratio Math. 48 (2015), DOI 10.1515/dema-2015-0006 · Lei–Zhang, Pacific J. Math. 289 (2017), arXiv:1505.02628 · Wei, arXiv:1508.03318 · [not fetched, dominated/context: Krylov–Safonov, Izv. Akad. Nauk SSSR 44 (1980) 161–175 · Aronson–Serrin, ARMA 25 (1967) 81–122 · Ladyzhenskaya–Solonnikov–Ural'tseva (1968) · Cranston–Fabes–Zhao, Trans. AMS 307 (1988) 171–194 · Fabes–Kenig–Serapioni, Comm. PDE 7 (1982) · Ignatova–Kukavica–Ryzhik, Comm. PDE 41 (2016) 208–226]. Frozen corpus: `FREEZE_REVIEW_3_2026-09-01.md` · `EXT_GAMMADEP_DECISION_2026-09-01.md` · `TYPE2_KILL_TABLE_2026-08-19.md` · `BH_BETAV_ENDPOINT_PINNING_2026-08-23.md` (incl. E1–E5).

---

## 9. Execution addendum (main-loop, 2026-09-02)

- **Adjudication audited before commit**: the forced-amplitude lemma (Chebyshev split
  against the enveloped swirl's `L^3` budget `\lesssim \Gamma_0^3 \tau^{\alpha-\beta_v}` vs the required
  `\tau^{3(\alpha-\gamma)}`), the AX-L7 rate (`M(\tau^\alpha) \asymp \tau^{\alpha-\gamma}`), the multi-scale closure
  bookkeeping (`\log(1/\tau)` scales vs any fixed power), and the DR-4 identity
  (`\partial_t\Phi + \mathrm{div}_5(b_5\Phi) = \nu\Delta_5\Phi`, `\mathrm{div}_5 b_5 = 2u_r/r` — re-derived from the `u_\theta`
  equation) were independently recomputed at the main loop: all agree.
- **Parallel D-3 lane (this pass) — superseded by, and consistent with, the standing
  first-hand triage** (`D3_TRIAGE_2606_07869_2026-09-02.md`): live-arXiv re-verification
  of the identification; new facts routed to the triage's addendum — the companion
  "3D reduction" paper is evidently **arXiv:2606.07875** (same author, same day), and
  the author's 2026 math.AP cluster contains **both** unconditional-regularity and
  finite-time-singularity claims for the same equations (red-flag pattern; details in
  the triage addendum). No refutation, endorsement, v2, or withdrawal located.