# FREEZE REVIEW — ROUND 3 (2026-09-01) — EXECUTED

Date: 2026-09-01 (JST). **Adjudication authority: the user's dated 2026-09-01
instruction, verbatim:** "Freeze Review Round 3 を実行せよ。F37–F43、P1–P7、Seregin
row-(i) update を個別に裁定し、特に F37/F38 を反映した後の Scope-A / Scope-B frontier を
一意に再固定すること。過去の『(Γ-DEP) が唯一の閉じ手』『third route はない』という主張は
再利用せず、Γ-DEP は十分条件としてのみ扱う。" Execution scope: this record + the
matching annotation block **C0–C14** appended to `TYPE2_KILL_TABLE_2026-08-19.md` +
the re-fixed frontier block. Agenda sources: the 2026-09-01 audit queue (F37–F43,
`TYPE2_KILL_TABLE` erratum section), the β_v decision proposals P1–P7
(`BH_BETAV_ENDPOINT_PINNING_2026-08-23.md` §6), and the queued Seregin row-(i)
update (HANDOFF literature watch).

*Label hygiene:* the C-numbers of this round's kill-table annotations continue the
A (round 1) / B (round 2) series; they are unrelated to the anchor audit's internal
contradiction ledger C1–C9 and to region C of `W★`.

## 0. Standing policy adopted this round (user-directed)

**(Γ-DEP) is henceforth cited as a SUFFICIENT closer only.** The β_v decision's §5/§8
claims "the single named object", "minimal" (unqualified), and "there is no third
route" are **retired from citation** (they already carry the 2026-09-01 erratum E1 at
source). The recorded closure shapes for the middle limb are: (i) (Γ-DEP)
[sufficient]; (ii) an outright proof of (SB-ANCH) (never as a premise — circularity);
(iii) a memberwise dichotomy `(Γ-DEP) ∨ (SB-ANCH)`. None is derivable from the frozen
rows; no exclusivity among shapes is claimed.

## 1. Individual rulings — audit queue F37–F43

| Item | Ruling | Disposition |
|---|---|---|
| **F37** (B6's "`(H2) ⟹ (H1)`" false after distance-clause deletion) | **ADOPT** | Executed as **C1**: B6's implication clause replaced by the corrected one-sided-bounds form; the single equivalent form, the B6 rider, and "(SB-ANCH) ⟺ `β_v = γ`" stand (they consume only the conjunction) |
| **F38** ((T3) Scope-A emptiness established only on `ρ_T = γ`) | **ADOPT** | Executed as **C2**: B2's (T3) bullet and its narrowing rider re-scoped; the frontier's exhaustiveness-gate sentence carries the same restriction (frontier block, §4 below) |
| **F39** (Seregin edge endpoint `γ = 3/5` outside K10) | **ADOPT** | Executed as **C3**: §2 S_blob sentence replaced ("exactly … for `γ ∈ (1/2, 3/5)`; the endpoint is on the edge, outside the class") |
| **F40** (S_ring K8-arm strictness) | **ADOPT** | Executed as **C4** jointly with C5 (the F40 joint form) |
| **F41** (S_ring missing K11; adjudication (a) vs (b)) | **ADOPT option (a)** — add `γ + α ≥ 1 (K11)` to S_ring | Executed as **C5**. Grounds: both K11 derivations are core-shape-agnostic (term balance uses only amplitude and finest scale; the energy-flux route cancels `ρ`); the K11 row asserts no blob restriction; B1 declares K1–K11 arithmetic scope-free. Shrink-only; implied under the [C] K8 arm; live for the Scope-B ring reopening |
| **F42** (A16 "lifetime `1` fails everywhere" — marginal at `ρ = 1/2` on `γ+α = 1`) | **ADOPT** | Executed as **C6**: A16 clause amended with the exactly-marginal exception |
| **F43** (A19 winding parenthetical drops `∫T4` + viscous defect) | **ADOPT** | Executed as **C7**: A19 parenthetical replaced by the corrected inequality |

## 2. Individual rulings — β_v decision proposals P1–P7

| Item | Ruling | Disposition |
|---|---|---|
| **P1** (A2 middle-limb rider: analysed, row-compliant on all of `S_blob`; NO side closed, debt not discharged) | **ADOPT, amended** | Executed as **C8**. Amendment (per §0): the residual-debt clause reads "(i) the existence question (out of scope for exponent bookkeeping) and (ii) the closure shapes of C0" — no uniqueness-of-closer language |
| **P2** ((T3) Scope-B status strengthening) | **ADOPT, amended** | Executed as **C9**. P2's drafted final clause "its Scope-A emptiness by the amplitude corollary is untouched" **conflicts with F38** and is replaced by "its Scope-A emptiness holds on the `ρ_T = γ` sub-branch only (C2/F38)" |
| **P3** (frontier sentence addition after "(SB-ANCH) ⟺ `β_v = γ`") | **ADOPT, amended** | Executed as **C10** and inside the re-fixed frontier block. Amendments: "closing it **needs** the new object (Γ-DEP)" → "(Γ-DEP) is a **sufficient** closer; an outright (SB-ANCH) proof closes it from the other endpoint; a memberwise dichotomy is a third shape" (§0); the Scope-A-corollary caveat carries the F38 scoping ("a class change, and effective only on `ρ_T = γ`") |
| **P4** (two record-only true statements: (i) `{β_v = γ, τ-uniform} ⟺ (SB-ANCH)`, hence `M ⟹ ¬(SB-ANCH)` and `{β_v < γ} = ¬(SB-ANCH)`; (ii) sup-swirl-poverty on all of `{β_v < γ}`) | **ADOPT** | Executed as **C11**. Both survive F37 (they consume the conjunction / the (H1)-side Γ-max bound, not the withdrawn implication); (ii) is the same arithmetic as F37's counterexample, now on record as a positive statement |
| **P5** (strike §2's stale "pinned to `σ = α`" K9 sentence) | **ADOPT** | Executed as **C12**: sentence superseded in the annotation layer with a pointer to A1/A2 (σ_core/σ_sup split; saturation-scale trichotomy) |
| **P6** (record-only [B] bookkeeping row: middle-limb realization tuples on all of `S_blob`) | **ADOPT** | Executed as **C13**, verbatim with its own disclaimer ("exponent bookkeeping, NOT a survival claim about Navier–Stokes"). Spot-reverified this round: K5a/K5b/K6/K11/Γ-max/envelope all pass for `C = (α, α, 3α, γ, 2α−β_v)`, `S = (β_v, β_v, 3β_v, β_v, β_v)` on `S_blob × {β_v ∈ (α,γ)}`; S sits on the `v = 3γ_j` marginal face exactly as `W★` (OOV-1, recorded not decided) |
| **P7** (Seregin arXiv:2606.29468 row-(i) update — queued independently) | **ADOPT** (= agenda item 3) | Executed as **C14**, below |

## 3. Individual ruling — Seregin row-(i) update (arXiv:2606.29468)

**ADOPT as a K10 row-(i) annotation (C14), pressure-only, with a named verification
debt.** Content: Seregin arXiv:2606.29468 (28 Jun 2026) generalizes the Euler-scaling
Type-II class beyond exact power laws to **log-corrected families** and again reduces
their exclusion to **open** Liouville theorems for ancient Euler solutions in
scaled-energy classes. Consequences recorded: (i) the conditional exclusion pressure
on the `γ + α = 1` edge extends to log-corrected neighborhoods of the edge — which
live on the map's **out-of-vocabulary faces** (OOV-4 of the β_v decision, which
places exactly these families there), so
this is a **scope annotation, not a new cut**: the power-law rows are unchanged and
nothing new is excluded; (ii) F39/C3's endpoint carve-out is unaffected (the
generalization stays conditional on the same open Liouville inputs); (iii) the
reduction target is the recorded (OO)-adjacent ancient-Euler Liouville problem —
already logged as the natural successor decision candidate. **Verification debt
(named, carried): [V?]** — the extraction is literature-watch level; full-text
verification of the exact class and hypotheses is owed before any chain consumes C14.

## 4. The re-fixed frontier (unique; supersedes the round-2 frontier block)

Executed in the kill table as the **Post-round-3 frontier** — **the sole operative
frozen text**; the bullets below are a summary of that block, not a second print:

- **Scope A.** Quasi-static = B2; its Γ-saturated realization is the target of R-B2′
  [C] on (E⁺⁺) = (COH-Δ)+(ANCH-κ), (P), (N-Γ)[B11 split] (+ imported layer (E⁺),
  frozen dictionary [C-dict] — B10). The Scope-A gate is the exhaustiveness step,
  **which is established only on the `ρ_T = γ` sub-branch (C2/F38)**: there, every
  Scope-A B2 candidate enters the Γ-saturated realization and A2's middle-limb debt
  narrows to its endpoint. **On `ρ_T > γ`, (T3)-emptiness is NOT established even in
  Scope A** (the amplitude corollary supplies (H1) and only caps `Γ(L)` — F31); the
  middle limb is open there absent a (Γ-DEP)-type location theorem.
- **Scope B.** **B2 UNKILLED.** First named gap **(SB-ANCH) ⟺ `β_v = γ`** (τ-uniform
  corner attainment, B13), a genuine **conjunction** (H1) ∧ (H2) with **neither
  clause implying the other** (C1/F37); B6's rider stands ((SB-ANCH) alone does not
  transplant R-B2′). The middle limb `β_v ∈ (α, γ)` is **row-compliant on all of
  `S_blob`** (β_v decision, `YES (CONSISTENT)`); its recorded closure shapes are
  §0's three — (Γ-DEP) [sufficient only], outright (SB-ANCH), memberwise dichotomy —
  with no exclusivity claim. `{β_v < γ} = ¬(SB-ANCH)` exactly, and sup-swirl-poverty
  holds on all of `{β_v < γ}` (C11/P4).
- **Carried unchanged:** M2 RESTRICTED (no independent members) · M3 RESTRICTED
  (`γ₂ < γ` dies by K4′ [(E⁺)+(P)], (P)'s Scope-B status unresolved — B1) · one-scale
  ring and M2 reopen in Scope B (S_ring now carries K11, C5) · (NECK) = standing
  dictionary-extension request (`θ_coh`, `ℓ_neck`) · fork-(β) declinations in force ·
  all three in-house vehicles retired/blocked · trigger (T-c) OPEN (B5) · Seregin
  row-(i) pressure extends to log-corrected OOV faces, [V?] (C14) · BH verdict
  **YELLOW-RED maintained** · no CAP trigger · **no Clay claim**.

## 5. Reconciliation with the external (ChatGPT-side) adversarial record — IMPORT-PENDING

The user reports (2026-09-01, this commissioning message) that a ChatGPT-side pass
produced: **(i) `Γ-DEP = UNDERDETERMINED`** and **(ii) a smooth divergence-free
snapshot counterprofile**. Registered here as **(EXT-ΓDEP-1)**, status
**IMPORT-PENDING**: the artifacts (exact decision statement, the profile's formula,
the list of constraints checked, and the failure/underdetermination points) are in
no file of this repository or the local working directories, and under the
provenance guardrail (`AGENTS.md`) a chat-reported result **binds nothing** until
imported verbatim. Nothing in §§1–4 consumes it.

**Conditional consequence analysis (valid if the import confirms the report):**

1. `Γ-DEP = UNDERDETERMINED` is **consistent with** this corpus: the β_v decision
   already established that no frozen row decides (Γ-DEP) (it is "genuinely new"),
   and this round's §0 treats it as sufficient-only. An UNDERDETERMINED verdict adds
   the stronger point that the pass found **no derivation and no refutation** from
   the frozen inputs plus whatever dynamical structure it consumed — the exact
   statement and consumed inputs must be read from the imported record.
2. A **smooth divergence-free snapshot counterprofile** (a single-time field
   realizing middle-limb Γ-saturation within all single-time constraints) would
   discharge the **snapshot half** of the coexistence debt (R-5) for the middle limb
   and prove that **(Γ-DEP) is not derivable from single-time/elliptic constraints
   alone**: any proof of (Γ-DEP), of (SB-ANCH), or of the dichotomy must be
   **dynamical** (use the time evolution). This sharpens, and does not contradict,
   the frozen record. Import checklist for the profile: axisymmetry; smoothness at
   the axis (`Γ = O(r²)`); `∇·u = 0`; finite energy; Γ-max + envelope; the printed
   blob shape or an explicit note of any unprinted shape consumed; which K-rows were
   checked at which radii.

## 6. Verdict: is Γ-OSC feasibility the right next decision theorem?

**YES — conditionally sound (妥当), with three binding conditions.** Grounds:

- **The snapshot counterprofile (if imported intact) closes the static route.** After
  it, the only live attack on the middle limb is dynamical, and the natural bounded
  question is exactly a Γ-coherence/oscillation feasibility statement: can τ-uniform
  middle-scale Γ-saturation **persist** under the Γ transport–diffusion dynamics
  (`DΓ/Dt = ν(Δ − (2/r)∂_r)Γ`, axis Dirichlet, maximum principle), or do the frozen
  budgets force the saturation set to oscillate/migrate and break the fixed-`c′`
  τ-uniform quantifier?
- **The toolset is genuinely better than the failed κ̂ campaign's.** The COH/winding
  retirement was driven by an unsigned, unestimated T4 and the absence of any level
  bound; `Γ` by contrast is a scalar with a **maximum principle and sign-definite
  diffusion**, and the corpus's bookkeeping already shows that the two strongest
  (unavailable, granted-hypothetically) inputs would place the limb's corner at
  `β_v = 1/2` — the viscous scale — where Γ-diffusion is exactly marginal over the
  lifetime. The decision has real content precisely where the map already localized
  the fight.
- **Both outcomes are progress** *(framing superseded by §8's reduced
  IMPLIED / VIOLATED / UNDERDETERMINED form and its refined termination rule)*:
  INFEASIBLE (oscillation forced) is a dynamical step toward the memberwise
  dichotomy (§0 shape (iii)); FEASIBLE (a sustainable saturation mechanism at
  bookkeeping level) is the first dynamical survival evidence for the middle limb
  and re-ranks (SB-ANCH) work.

**Binding conditions:** (1) **import (EXT-ΓDEP-1) verbatim first** and audit it
against §5's checklist — commissioning Γ-OSC on an unimported counterprofile would
consume an unverified premise; (2) the statement fixer must define Γ-OSC **inside
the frozen vocabulary** (residence/erosion horizons in the K11-consistent range,
`c′` fixed, τ-uniform), inventing no new dictionary entries beyond the one named
object; (3) **pre-registered termination rule**: this branch has produced no
unconditional kill since the ring corollary (2026-08-19) and both prior in-house
dynamical campaigns starved; if Γ-OSC also returns UNDERDETERMINED, the in-house
dynamical inventory is exhausted — **two consecutive underdetermined decisions end
the BH branch** (pivot options: the numerical candidate program under `SPEC.md`, or
the Seregin-class ancient-Euler Liouville watch at literature level). This is the
honest bound on the losing line the user asked for: with the rule in place the
downside is one bounded pass, not an open-ended campaign.

## 7. Stop-rule audit (this round)

Docs-only; no numerics; no proof attempts; no Lean source touched (the 8769-job
local gate of commit `f65c463` remains the verification baseline). No promotion of
any external claim (§5 is import-pending and consumed by nothing). No CAP trigger.
Verdicts after this round: **BH YELLOW-RED · B2 UNKILLED in Scope B · (T-c) OPEN ·
no Clay claim** — unchanged.

## 8. Addendum (same day, after adjudication): (EXT-ΓDEP-1) IMPORTED; §6 verdict finalized

Later on 2026-09-01 the user supplied the external record — **two documents** —
verbatim in the commissioning conversation. Both are imported with an in-repo audit
as `EXT_GAMMADEP_DECISION_2026-09-01.md` (§A/§A2 verbatim; §B/§B2 audit: **PASS at
snapshot level**, all derivable claims recomputed; named debts D-1 [V?]
Ożański–Palasek citations, D-2 bare-drift falsity risk, D-3 WATCH on an
unidentified June-2026 global-regularity preprint — consumed by nothing).

Effects on this record:

- **§5's IMPORT-PENDING status is discharged.** §6 binding condition (1) is met.
- **§6's object is refined per the second external document (adopted §B2.3):** the
  next decision is the **Γ-OSC feasibility decision** — extract the weakest
  quantitative drift condition for the τ-uniform oscillation contraction at
  `R = τ^α`, then verify both directions against frozen B2 — with the 3-valued
  verdict **IMPLIED / VIOLATED / UNDERDETERMINED** and no new exponent
  restrictions as substitutes. This is strictly smaller than "prove (Γ-OSC)" and
  is the commissioning-ready form.
- **(Γ-OSC) language weakened (adopted §B2.1), C0-consistent:** it is *the most
  direct identified sufficient condition* for (Γ-DEP)_fld — `(Γ-OSC) ⟹ (Γ-DEP)_fld`
  is verified; the converse and any exclusivity are unproved and are not to be
  cited.
- **(Γ-DEP)_fld (the `c_* ∈ (0, c₀)` statement) is the operative field-level form**
  (adopted §B.5) — a statement-level refinement of the 08-23 print, not a change of
  its ruling.
- **The snapshot counterprofile is a standing test case** for every future
  Γ-depletion-type hypothesis (adopted §B2.4).
- **§6 condition (3) — the termination rule — is refined (adopted §B2.5):** the
  branch continues past the feasibility decision **only on IMPLIED (or a partial
  IMPLIED naming the consumed NS structure)**; **VIOLATED** ⟹ the Γ-depletion
  lane's expected value collapses, end/pivot justified; a second consecutive
  **UNDERDETERMINED** ⟹ the BH branch ends. This is the registered bound on the
  losing line.

**Final §6 verdict: Γ-OSC feasibility (in the §8 reduced form) is the sound next
decision theorem — commissioning-ready.** Commissioning itself remains a user act;
nothing is started by this record.
