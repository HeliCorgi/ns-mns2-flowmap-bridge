# Biot–Savart deviation ledger — R-B2 discharged on the frozen dictionary; the debt localizes to (NECK)

Date: 2026-08-20 (JST). One bounded in-house estimation pass (3 Opus analysts +
adversarial verification + critic; stop rules clean: no in-house Liouville — KNSS 5.3 and
Lei–Zhang 1.2 quoted, never re-proved; no new ansatz — one close call, (NECK), admitted
as negation witness only; no numerics; no promotions; no map edits; no files written by
agents). Baseline: `BH_CONSTANT_EXCLUSION_ROW_2026-08-20.md`. Tags: [H] hard/textbook,
[D] derived this pass (independently re-derived), [V] verified first-hand, [C]
conditional, [C-dict] dischargeable inside the frozen dictionary, [B] bookkeeping.

## 1. RULING (i): R-B2 DISCHARGED on the frozen dictionary — tag [C]

The deviation ledger delivers **`r_y|w_pol| ≤ C`, τ-uniform, on the receding ball
`R₀ ≤ |y| ≤ R_k`**, `R₀ = 10√42·Γ₀`, `R_k = ε₀·τ^{(β−γ)/2} → ∞`, where `β` is the
finest non-local gradient-scale exponent (`α` for B2, `ρ₂` for the M3 tie face, `α₂`
on-axis); `|w| ≤ 2√42` inside `R₀` is automatic. Full chain:

> frozen dictionary [C-dict] + Γ-max [H] + the axisymmetric elliptic kernel with both
> asymptotic regimes [H] + axis regularity [H] + the re-centred Lemma-6.1 extraction [V]
> + **(E⁺⁺)** [C] + (N-Γ) [C-dict] + (P) [C]
> ⟹ `|w| ≤ C/r` globally on the ancient limit (swirl half free: `Γ` exactly
> scale-invariant) ⟹ **KNSS Thm 5.3 [V] ⟹ `w ≡ 0` ⟹ `v = b·e_z` ⟹ `Γ ≡ 0`** —
> contradicting the Γ-saturated core ⟹ **the M3 tie face AND B2's Γ-saturated blob are
> excluded**, consuming no Type-I rate, no viscosity, no energy, no Scope-A geometry,
> no V1.

**The kill routes through KNSS Thm 5.3 (R-B2), not Lei–Zhang.** R-B1 is delivered a
fortiori and demoted to redundant confirmation — its pressure-class and smallness debts
leave the critical path.

## 2. Kernel facts established [H]/[D] (formula sheet, re-derived twice)

- Exact kernel `ψ = ∫∫ G ω_θ`, `G = (√(rr′)/2π)B(k)`; small-k expansion computed
  exactly; **the monopole is absent identically in the far zone** (`∫ω dV ≡ 0` in
  axisymmetry, for restricted regions too — the leading multipole is always the
  ring-impulse dipole `P = ∫r′²ω_θ`), while the near zone of a thin structure carries
  the meridional circulation `κ = ∫ω_θ dA = ∮u_pol·dl` as a `κ/2πd` field — valid only
  in the window `ℓ_region ≪ d ≪ a_region`;
- **axis structure**: `G = O(r²)` and `O(r′²)` — near-axis vorticity is a
  weight-`r′²` source, `ψ(0,z) = 0` is forced, and for an **axis-straddling region the
  2-D window is empty** (a large κ there never produces a `1/d` field) — this voids the
  naive 2-D over-estimates of B2's main core and on-axis R2;
- `b_τ` is **derived, not assumed**: any source at axis-distance `≫ τ^γ` induces
  `u_z = −A(z)`, `u_r = ½A′(z)r` on the zoom ball; `b_τ := −A(z_k)`; its axiality is a
  computation (the radial residue costs `τ^{(γ−β)/2} → 0`), so the boosted field stays
  axisymmetric;
- the contour form `|κ_R| ≤ ‖u_pol‖_{L^∞(∂R)}·|∂R|` is the δ-free route:
  `κ_tip ≲ τ^{ρ_T−γ} ≤ 1` (confirmed twice — contour and dynamical), `κ₁ ≤ 1`;
  the unbounded κ's (main core, R2) reach the ball only through the `½A(z)r²` form;
- **corrections to prior text**: the audit's `‖ω‖_∞ ≳ τ^{−γ−ρ_T}` on the tongue is
  `∇Γ`-generated `ω_r/ω_z` — **invisible to the poloidal kernel, not chargeable**; and
  the gate's "`r|u_pol| ≲ 1` for `r ≥ τ^γ`" is an **exterior-tail** statement, false as
  an interior bound (inside the core `r|u_pol| ≍ τ^{α−γ} → ∞`) — what is small on the
  ball is the *deviation*, not the field (F5).

## 3. The ledger table (per term, rescaled units, both configurations)

| term | bound | status |
|---|---|---|
| non-local Taylor remainder | `≲ R²τ^{γ−β} ≤ 1` by construction of `R_k` | [D] |
| un-subtracted radial boost residue | `≤ τ^{(γ−β)/2} → 0` | [D] |
| Γ-saturated tip (local) | `≲ κ_tip·a²/D² ≲ C/R²` (dipole-with-image) | [D] |
| tongue/axis filament, any length | `≲ 2λa²/r_y ≲ C/R` (solenoid limit, length-uniform; covers the P5 elongated filament) | [D] |
| swirl half | `r|w_θ| ≤ Γ₀` free (exact `Γ` scale-invariance) | [D] |
| **(NECK)** | **unassigned by the dictionary; charged to (E⁺⁺)** | [C] |

Consistency checks: the ball radius `R_kτ^γ = τ^{(γ+β)/2}` **equals** (E⁺)'s printed
C¹ radius (the ledger and (E⁺) are the same object — no slack); `R_k` is the geometric
mean of the zoom scale and the Taylor-validity radius. Time-uniformity rests on
`2γ−1 > 0` alone. Every κ/P entry is a one-sided **upper** bound — the "violation"
numbers (core (Q)-failure radius etc.) are potential, not forced, and must not be cited
as kills of other lines.

## 4. (E⁺⁺) and (NECK) — the localized debt

**(E⁺⁺)** [C] (freeze wording, F3): (E⁺) with its C¹ clause read as — within
`τ^{(γ+β)/2}` of the zoom centre the poloidal field is generated by the labelled
regions **at their assigned poloidal exponents**; in particular the forced tongue's
meridional (`ω_θ`) content is the core's (`ω_θ ≲ τ^{−γ−α}`), its recorded vorticity
excess being `∇Γ`-generated and invisible to the poloidal kernel. Exception, granted
and handled: the tip's C¹ failure at rate `1/ε` — estimated by explicit Biot–Savart,
not Taylor.

**(NECK)** [named object, F4]: an annular meridional vortex sheet
(`ω_θ ≈ λδ(z−z₀)`, `λ = dκ/ds = O(1)` rescaled — a poloidal shear layer of full
amplitude order riding the sub-saturated stretch of the forced tongue,
`r_y ∈ [√42Γ₀, R_k]`). It defeats **both** rows at rate `R`, and Γ-max does not confine
it (Γ-max confines the tip only). **Ruled a negation witness, not a survivor**, by the
program's own policing standard: no dictionary line assigns the neck elevated `ω_θ`;
introducing it is a dictionary extension — admissible as a negation witness,
inadmissible as a class member. It is exactly what (E⁺⁺) is charged to exclude; any
future dictionary extension assigning the tongue a poloidal-shear exponent re-opens
R-B2.

**Honest statement of what moved**: the debt is not erased — it is *localized*: from an
abstract far-field hypothesis on an ancient limit, to **one exponent the frozen
dictionary declines to assign to one named sub-structure**. This is the escalation the
constant-exclusion row anticipated: the tie face dies at the same hypothesis strength
that killed `γ₂ < γ`.

## 5. R-B1's "one logarithm of slack" — REFUTED (F1)

Lei–Zhang's BMO-stream-function hypothesis is **equivalent to `w_pol ∈ BMO^{-1}`** —
the same critical scaling as (Q); zero logarithms. It is implied by (Q) (Carleson
`≲ C²`), and its genuine slack over (Q) is L²-averaging (sign cancellation, exceptional
sets, near-axis singularities) — not a logarithm of size: a log-excess in `r|w_pol|`
breaks it in every gauge (the `e_θ` rotation converts scalar growth into unbounded
angular oscillation at every scale). The prior sizing sentence is replaced verbatim in
the freeze queue.

## 6. Freeze-review queue additions

F1 (replace R-B1 sizing) · F2 (new primary row **R-B2′ [C]**: re-centred limit + Γ-max
+ (N-Γ) + axial boost + this ledger under (E⁺⁺) ⟹ KNSS 5.3 ⟹ tie face and B2's
Γ-saturated sub-core excluded; R-B1 redundant confirmation) · F3 ((E⁺⁺) verbatim +
configuration-dependent radius) · F4 ((NECK) as named negation witness) · F5
(geometry-gate exterior-tail correction) · F6 (legend: the poloidal kernel sees `ω_θ`
only; `∫ω dV ≡ 0`; the `r′²` weight) · F7 (`b_τ` axial by derivation) · F8 (ball
radius = (E⁺) radius exactly; time-uniformity = `2γ−1 > 0`).

## 7. The single next bounded task (critic-designated)

**Discharge or refute (E⁺⁺) by a dynamical `ω_θ`-budget on the neck.** The pass just
completed was kinematic; the only mechanism creating meridional vorticity on a swirl
structure is `D(ω_θ/r)/Dt = (1/r⁴)∂_z(Γ²) + ν(Δ + (2/r)∂_r)(ω_θ/r)`. At the tip this
independently reproduces `κ_tip ≲ τ^{ρ_T−γ}` — a found consistency. Run the same
estimate along the sub-saturated neck (`r ∈ [√42Γ₀τ^γ, τ^{(γ+α)/2}]`, local eddy time,
dictionary-assigned quantities only). Outcomes: (a) production bounded by the core's
own `ω_θ` ⟹ **(E⁺⁺) discharged in-house**, the chain's tag improves [C] → [C-dict],
and R-B2′ is a kill conditional only on (P)+(N-Γ); (b) the estimate requires the
tongue thickness `δ_T` ⟹ print `δ_T` as a dictionary-extension request and (NECK) as
the branch's named open object. Do not stand down to V1; no in-house Liouville at any
point.
