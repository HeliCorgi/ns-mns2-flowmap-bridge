# HSEL_TGRAM_DECOMPOSITION — 2026-09-04 — T-DIR-SPK proof-route decomposition: the matrix evolution identity `G′ = −2νD − 2𝒫` and the canonical fixed-direction free L¹ budget are derived; all six commissioned mechanisms adjudicated; VERDICT: ONE-LEMMA REDUCTION FOUND — T-GRAM, the canonical-direction production budget (RECORD-ONLY)

**Repo:** `c:/Users/corgi/Downloads/ns-mns2-flowmap-bridge` · **Status: RECORD-ONLY.** No Lean file touched; no numerics; no formalization; no proof search on any open head (the derivations below are the commissioned decomposition — kinematic/linear-algebraic identities and free-budget bookkeeping at [D]-standard level; every open statement remains OPEN). Lean baseline unchanged (8775-job gate).

**Commissioning provenance.** User instruction, 2026-09-04 (seventeenth session): decompose, counterexample-first, the exact-NS mechanisms that could prove the sole open head `inf_{e∈S²} ∫₀^{T}(−∫(∂_e u)ᵀS(∂_e u)dx)_+ dt ≤ C(ν,T,M)` (= T-DIR-SPK). Mandatory items: (1) strain eigenframe vs the gradient Gram matrix `G(t)`; (2) CF-type vorticity-direction coherence; (3) `λ₂⁺`-type strain geometry; (4) temporal eigenframe rotation and the rotating-anisotropy barrier; (5) what the ONB identity `Σᵢ P_{eᵢ} = −∫ωᵀSω` yields beyond the trace (determinant/spectral/anisotropy information); (6) whether energy/dissipation can limit the orientation-rotation rate of `G(t)`. Per-time good directions `e(t)` are explicitly insufficient — one FIXED `e` over the horizon is required. Tag each candidate KINEMATIC / EXACT-NS / KNOWN / NEW-MATH / COLLAPSES-TO-WALL / FAILCASE. If T-DIR-SPK compresses to ONE further geometric/dynamical lemma, select exactly that one; else NO-FURTHER-REDUCTION (and only then the numerical probe).

Notation: `G_{ij}(t) = ⟨∂ᵢu,∂ⱼu⟩_{L²}` (Gram, PSD, `tr G = E`); `𝒫_{ij}(t) := ∫(∂ᵢu)ᵀS(∂ⱼu)dx` (the **production tensor** — symmetric by the symmetry of `S`); `D_{ij}(t) := ⟨∇∂ᵢu,∇∂ⱼu⟩` (dissipation tensor, PSD); `X_e = eᵀGe`, `P_e = eᵀ𝒫e`.

---

## §1 — Structural yields [D] (item 5, answered first — it organizes everything)

- **Y-1 (the matrix evolution identity).** Polarizing the proved channel identity `X_e′ = −2ν eᵀDe − 2eᵀ𝒫e` over all `e`: **`G′(t) = −2νD(t) − 2𝒫(t)`** as symmetric matrices. Its trace is the classical enstrophy identity (`tr𝒫 = −∫ωᵀSω`, `tr D = ‖∇²u‖²` — KNOWN); the full matrix form appears to be new to this program's record and is the natural state equation of the whole channel family.
- **Y-2 (the canonical fixed-direction free L¹ budget — the decomposition's key find).** Let `ē = ē(T′)` be a unit eigenvector of `∫₀^{T′}G(t)dt` for its smallest eigenvalue. Then
  > `∫₀^{T′} X_{ē}(t)\,dt = λ_min\big(∫₀^{T′}G\,dt\big) ≤ tr(∫G)/3 = (∫₀^{T′}E\,dt)/3 ≤ M²/(6ν)` — **FREE.**
  A *fixed* direction with a free `L¹_t` budget exists canonically — this is strictly more than the per-time statement (`∫λ_min(G(t))dt ≤ M²/6ν`, also free, also recorded) which the commission correctly rules insufficient. The lineage's architecture is thereby completed at the general level: free `L¹` budget at a canonical fixed direction + the proved sup-bound arrow (§3) + the V-15-verified published bridge; open = one production budget.
- **Y-3 (determinant/anisotropy functionals).** `d/dt log det G = −2ν tr(G⁻¹D) − 2tr(G⁻¹𝒫)` and the isotropy index `det G/(trG/3)³` have **no signed structure** from known identities (`G⁻¹` degenerates exactly in the favorable regime); no usable invariant found beyond Y-1/Y-2. The quartic tensor `∫G⊗G dt` reformulation of T-DIR is recorded (min of a degree-4 form on `S²`) — no free bound on its minimum exists beyond what Y-2 + a sup-bound give.

## §2 — Mechanism table (counterexample-first; tags per the commission)

| ID | Mechanism | Analysis | Tags |
|---|---|---|---|
| R1 | strain eigenframe vs `G`; alignment depletion | pointwise `−(∂_e u)ᵀS(∂_e u) ≤ (−λ₁)_+\|∂_e u\|²`: negative production requires `∂_e u` mass in the compressive strain cone. But the channel vector is **gradient-like**: `D_t(∂_e u) = −(∇u)(∂_e u) + νΔ(∂_e u) − ∇∂_e p` — the same minus-gradient stretching as a passive-scalar gradient, whose compressive alignment is the GENERIC observed behavior (DNS phenomenology: `∇θ` aligns with `e_{λ₁}`). The dynamics *drives* the adverse alignment — it is the growth engine, not a depletion resource | EXACT-NS · NEW-MATH · **FAILCASE as a standalone route** (adverse polarity) |
| R2 | CF-type vorticity-direction coherence | ω-direction coherence bounds the TRACE-level stretching (conditionally), not individual `eᵀ𝒫e`; and importing it trades T-DIR-SPK for the open HR-1 head — a route-swap, not a reduction | EXACT-NS · **FAILCASE as a route** (imports an open head; cross-link recorded) |
| R3 | `λ₂⁺` strain geometry; spectral split of `𝒫` | pointwise: where `λ₂ ≥ 0` the compressive cone is 1-dimensional and production concentrates on one projection — compound-usable only with R1 (adverse). Spectrally: T-DIR-SPK splits into **(SP-a)** production anisotropy `∫(−μ₃(𝒫))_+dt ≤ C` (per-time SOME direction has controlled production) × **(SP-b)** frame coherence (a fixed `e` inherits it). Both factors are separately kinematically defeated — isotropic fields give `𝒫 ∝ (tr𝒫/3)I ≺ 0` (SP-a wall-level), F2 rotation defeats (SP-b) — so each factor is NEW-MATH consuming dynamics; the split is a map of the difficulty, not a reduction in count | EXACT-NS · NEW-MATH ×2 |
| R4 | eigenframe-rotation control via energy/dissipation (item 6) | **Answer: NO at the free-budget level.** Perturbation theory gives frame-rotation rate `~ ‖G′‖/gap(G)`; by Y-1, `‖G′‖ ≤ 2ν‖D‖ + 2‖𝒫‖`, and `∫‖D‖dt`, `∫‖𝒫‖dt` are wall-level budgets (`∫‖∇²u‖²dt` requires the enstrophy-production budget; `‖𝒫‖ ≲ ‖∇u‖₃³`). Moreover the gap itself vanishes in the isotropic regime — which is exactly the wall-level bad case. Any rotation-control lemma is NEW-MATH beyond energy | COLLAPSES-TO-WALL (as a free route) · NEW-MATH otherwise |
| R5 | beyond-trace information | = §1: Y-1 (matrix identity — [D], the family's state equation), Y-2 (canonical fixed-direction free `L¹` — [D], the key find), Y-3 (no signed det/anisotropy structure) | KINEMATIC/[D] yields; trace part KNOWN |

Battery notes: the F2 rotating-anisotropy kinematic barrier defeats every purely kinematic/per-time route above (each candidate re-checked); frozen `S_blob` cores are gradient-isotropic at wall level ⟹ violate all channel objects coherently (polarity forced anyway via the verified bridge); Tao averaged: no channel structure survives averaging — C-3 respected by every route.

## §3 — THE SELECTED LEMMA (the one-lemma compression exists)

> **T-GRAM (canonical-direction production budget; norm-uniform; OPEN — never asserted):** ∀ν>0, ∀T<∞, ∀M<∞: ∃Q₀(ν,T,M) < ∞ such that every certified solution from the admissible `M`-ball satisfies, on every certified horizon `T′ ≤ T`, with `ē = ē(T′)` a unit eigenvector of `∫₀^{T′}G(t)dt` for its **smallest** eigenvalue:
> **`∫₀^{T′}\big(−ēᵀ𝒫(t)ē\big)_+\,dt ≤ Q₀`.**

**Proved chain [D] (one arrow to T-DIR; thence the V-15-verified bridge):** integrating Y-1 along `ē` and discarding the good-signed pieces (`ēᵀDē ≥ 0`), `X_{ē}(t) ≤ X_{ē}(0) + 2∫₀^t(−ēᵀ𝒫ē)_+ ≤ M² + 2Q₀` (sup bound); pairing with the free Y-2 budget, `∫₀^{T′}X_{ē}²dt ≤ (M²+2Q₀)·M²/(6ν)` — **T-DIR holds with `Q₀′ = (M²+2Q₀)M²/(6ν)`**, and horizon-dependence of `ē(T′)` is exactly what DQ-1 already handles. Downstream: the verified quantitative chain to `sup_t‖u‖_{H³} ≤ F(ν,T,M,·)`, H-SEL^nu, N0, and the Lean assembly.

**Why this is the selected compression (C0 — a selection, not uniqueness):** (i) it is ONE lemma, eliminating the ∃-quantifier by an explicit canonical construction — maximally concrete and probe-ready (`ē`, `X_{ē}(t)`, and the budget are all computable observables); (ii) it pairs the direction with the free Y-2 `L¹` companion — the exact general-level replica of the swirl-channel architecture (free budget + sup-arrow + published bridge, open = one budget); (iii) precedent: the T-SPK compression was likewise sufficient-shaped and selected for structure. **Caveat printed:** T-GRAM is a specialization (possibly strictly stronger than T-DIR — the `L¹`-optimal direction need not be `L²`- or production-optimal); **revision trigger:** if a probe shows the canonical `ē` adversarially bad while generic directions behave, fall back to T-DIR-SPK(∃e) — both statements stay on file. Battery: polarity is logically forced (blow-up ⟹ ¬T-DIR ⟹ ¬T-GRAM); kinematic violators exist (F2 pattern — the barrier stands: any proof must consume NS dynamics); no certified counterfamily constructible (O-1/O-2/O-4 landscape unchanged).

## §4 — VERDICT

**ONE-LEMMA REDUCTION FOUND: T-GRAM selected.** The commissioned six mechanisms are adjudicated (R1 adverse, R2 route-swap, R3 two-factor map, R4 negative answer on rotation control, R5 = the structural yields Y-1/Y-2/Y-3 that make the compression possible). Since a reduction WAS found, the commission's "only then proceed to the numerical probe" clause does **not** auto-fire; the recorded next options (each a user act): (a) a probe now targeted at the T-GRAM observables (`ē`, `X_{ē}(t)`, `(−ēᵀ𝒫ē)_+(t)`, `spec G(t)`); (b) T-GRAM proof commissioning (counterexample-first discipline satisfied through this lineage); (c) fallback objects on file: T-DIR-SPK(∃e), T-DIR, the Gram form.

## §5 — Claim boundary

RECORD-ONLY; no Lean edit; no numerics; no proof search — Y-1/Y-2 and the §3 arrow are linear algebra, free-budget bookkeeping, and sign-discarding at [D]-standard level; T-GRAM, T-DIR-SPK, T-DIR, H-SEL, and N0 all remain OPEN and unclaimed. **Nothing here asserts, approaches, or implies a resolution of the Navier–Stokes Millennium problem in either direction.** The logical-collapse caveat applies to T-GRAM as to every head (universally proved it yields general-class continuation through the verified bridge); its value is structural concreteness. R4's negative answer and R1's adverse phenomenology locate the proof's required inputs without deciding anything. Commissioning the probe or any proof work is a user act. C0 discipline throughout.
