# HSEL_P3_FALSIFICATION_SEARCH — 2026-09-02 — analytic falsification search for H-SEL^nu: seven seed lanes adjudicated, four obstruction lemmas, no certificate constructible; VERDICT: SURVIVES, with the falsification frontier pinned to the C²-escape window (RECORD-ONLY)

**Repo:** `c:/Users/corgi/Downloads/ns-mns2-flowmap-bridge` · **Status: RECORD-ONLY.** No Lean file touched; no proof search on the open core of H-SEL^nu; no numerics; nothing frozen moved. Lean baseline unchanged (8775-job gate).

**Commissioning provenance.** User instruction, 2026-09-02 (eighth session): run P-3 (analytic falsification search) **ahead of** P-2 and of any proof search on H-SEL^nu. At fixed `(ν,T,M)`, attempt to construct smooth divergence-free Schwartz data families `{u0^{(k)}}` with `‖u0^{(k)}‖_{H³} ≤ M` whose **known-smooth** NS solutions unbound

> `S(k) = sup_{t ≤ T} ‖∇U^{(k)}(t)‖_∞ / (1 + ‖∇U^{(k)}(t)‖_{L²}²)`.

Commissioned seeds: C-1 Lu–Doering maximizer geometry; high-frequency staircase; multi-scale packets; shear/strain transients; the small-data global class. Mandatory per construction: (R1) `H³`-ball uniformity; (R2) Schwartz / divergence-free / realness; (R3) fixed `ν,T`; (R4) solution smoothness guaranteed by known theory; (R5) an actual lower bound on `S(k)`. **Not admissible as certificates:** snapshot fields, averaged-NS solutions, finite-cylinder (or torus) flows. Verdict ∈ {COUNTEREXAMPLE / SURVIVES / UNDERDETERMINED}; only SURVIVES/UNDERDETERMINED proceeds to P-2 or proof-route selection.

**Method.** Single-session adversarial analysis by the main loop (this is a mathematics task, not a literature task; targeted citations from memory are tagged [H] per house discipline — no bulk literature fetch, per commission). Every seed is run counterexample-first against the five requirements; closures are proved at [D]-standard level (known mathematics, derivations sketched in-record); nothing about the open core is claimed in either direction.

Notation: `E(t) = ‖∇U(t)‖²_{L²}`, `Q₅ = ‖∇U‖_∞/(1+E)`; `‖·‖_{H³}` classical (carrier-comparable via the proved SEL-1 constants). Divergence-free Schwartz data are built as `u0 = ∇×A`, `A` Schwartz (the Leray projector need not preserve Schwartz class — known repo caveat — so curl-potential form is used throughout; realness by real `A`).

---

## §1 — Frame facts

- **F-1 [D].** `Q₅(0) ≤ C_emb·M` on the whole ball (SEL-2). Any divergence of `S(k)` must be dynamic, on `(0,T]`.
- **F-2 (EQ-6, carried).** On `t ∈ [0, T₀(ν,M)]` (the Lean explicit lifespan), `S ≤ C_emb·(M+1)` uniformly over the ball. The search is entirely about `T > T₀(ν,M)`.
- **F-3 [D].** A certificate member needs R4 + R5 *simultaneously*: a family member whose smoothness on `[0,T]` is not known cannot certify `¬H-SEL^nu` (if it actually blew up it would be an EQ-4 object — a different and stronger claim). This "double obstruction" (O-4 below) governs every nonperturbative seed.

## §2 — Obstruction lemmas (seed-closure tools; known mathematics, [D]-standard)

- **O-1 (small-critical persistence).** ∃ε₀ = ε₀(absolute) such that `‖u0‖_{Ḣ^{1/2}} ≤ ε₀ν` implies a global smooth solution with `sup_{t≥0} ‖u(t)‖_{H³} ≤ ‖u0‖_{H³}·exp(C ν^{−2}‖u0‖²_{Ḣ^{1/2}}) ≤ M e^{Cε₀²}`. Sketch: the `Ḣ^{1/2}` energy inequality `d/dt‖u‖²_{Ḣ^{1/2}} + 2ν‖u‖²_{Ḣ^{3/2}} ≤ C₁‖u‖_{Ḣ^{1/2}}‖u‖²_{Ḣ^{3/2}}` closes under smallness (Fujita–Kato lineage [H]), giving the budget `∫‖u‖²_{Ḣ^{3/2}}dt ≤ ν^{−1}‖u0‖²_{Ḣ^{1/2}}`; the `H³` level obeys `d/dt‖u‖²_{H³} + 2ν‖∇u‖²_{H³} ≤ C₂ν^{−1}‖u‖²_{Ḣ^{3/2}}‖u‖²_{H³}` (standard product estimate + Young [H: Bahouri–Chemin–Danchin-type]); Grönwall against the budget. **Corollary: `S ≤ C_emb·M·e^{Cε₀²}` uniformly on the small-critical sub-ball — this whole sub-ball is closed to falsification** (and, incidentally, H-SEL^nu *holds* there by known math; recorded as a map fact, not claimed as head progress).
- **O-2 (frequency quarantine).** If `supp û0 ⊂ {|ξ| ≥ N}` then `‖u0‖_{Ḣ^{1/2}} ≤ N^{−5/2}‖u0‖_{Ḣ³} ≤ M N^{−5/2}`. Hence every family whose active frequencies diverge falls into O-1 once `N_k ≥ N*(ν,M) := (M/(ε₀ν))^{2/5}`. **Inside a fixed `H³` ball, O(1) critical norm can only be carried at frequencies `≤ N*(ν,M)`**: the falsification battle is confined to the O(1)-frequency nonperturbative core, with all higher-frequency content perturbatively dressed. (For mixed data this locates the critical norm; it closes pure high-frequency families outright and no more.)
- **O-3 (no scaling import at fixed horizon).** Importing growth by parabolic zoom `u_λ = λv(λx, λ²t)`, `λ > 1` (to compress a base growth time `τ` into `T`) costs `‖u_{0,λ}‖_{Ḣ³} = λ^{5/2}‖v0‖_{Ḣ³}`, forcing the base into `‖v0‖_{H³} ≲ Mλ^{−5/2}` — which is O-1 territory for large `λ` — while the ratio gains at most `λ²`: net `Q₅^λ ≲ C_emb M λ^{−1/2} → 0`. The zoom ladder loses.
- **O-4 (double obstruction for nonperturbative seeds).** For data outside O-1 with `T > T₀(ν,M)`: (i) no theorem guarantees smoothness on `[0,T]` (R4 fails as a *guarantee*), and (ii) no theorem in the fetched record (P-1, two independent refutation-search lanes, EMPTY) produces any dynamic lower bound driving `‖∇U‖_∞` beyond datum-uniform constants (R5 unobtainable). A certificate needs both at once.

## §3 — Seed adjudication table

| Seed | Construction (R1–R3 all arrangeable: curl-potential Schwartz, real, normalized to the `M`-ball) | R4 smoothness guarantee | R5: best provable bounds on `S(k)` | Status |
|---|---|---|---|---|
| **S-1** high-frequency staircase (single scale `N_k → ∞`) | packets `∇×(a_k A(x)e^{iN_k x·e} + c.c.)`, `a_k ~ MN_k^{−3}` | YES for `k` large — O-2 puts the family in O-1 | **UPPER bound**: `S(k) ≤ C_emb M e^{Cε₀²}` uniformly; no divergence possible | **CLOSED** (O-1 ∘ O-2) |
| **S-2** multi-scale packets (staircase across scales `2^j, j ≤ k`) | `Σ_j c_j`-packets, `Σ c_j² 2^{6j} ≲ M²` | not guaranteed beyond `T₀` for the O(1)-frequency part | critical norm is geometrically dominated by `j ≲ log N*`: the high tail is perturbative dressing (O-2), the low part is the S-5 core; the hoped forward-cascade amplification is precisely the mechanism that (per P-1) has **no rigorous NS instance** — its only rigorous realization is Tao's *averaged* model, inadmissible by rule | **UNCONSTRUCTIBLE** (falls into S-5 + O-4) |
| **S-3** Lu–Doering maximizer geometry (C-1 seed): colliding-ring data scaled into the ball | not guaranteed beyond `T₀` | at ball-capped data the LD mechanism maximizes `dE/dt ≤ CM⁶/ν³` — a *denominator* push (LD configurations maximize enstrophy production, **not** `Q₅`; `Q₅`-extremal fields at fixed `E` are Bernstein-saturating concentrates whose dynamical persistence is exactly what the depletion numerics deny [V-num: Ayala–Protas; Kang–Yun–Protas `ℰ₀^{3/2}`]); no growth theorem; solutions could in principle *dynamically* reach LD geometry at large `E(t)` — but no theorem produces even sustained enstrophy growth | **UNCONSTRUCTIBLE** (O-4; numerics adverse) |
| **S-4** shear/strain transients (Orr / lift-up non-normal growth) | not guaranteed beyond `T₀` for O(1) bases | two rigorous closures [D]: (i) frequency-scaling attacks die by O-2 — tilting/lift-up amplification factors are frequency-independent (wavevector tilt `k(t) ~ N(1+St)` gives relative gradient gain `(1+ST)`, energy gain algebraic in `ST`), so perturbation gradients stay `≲ MN_k^{−2}·A(M,T) → 0`; (ii) for bases with ball-*and*-C²-bounded profiles, linearized gradient amplification is `≤ exp(C(‖∇u_b‖_∞ + 1)T) ≤ e^{C(C_emb M+1)T}` — `k`-uniform. The un-closed variant (bases with unbounded `∇²`) is exactly the C²-escape window = S-7 | **CLOSED as a mechanism** (residue routed to S-7/S-5) |
| **S-5** the O(1)-frequency nonperturbative core (implicit general seed after O-2) | not guaranteed beyond `T₀` | no growth theorem in either direction (P-1 lanes empty); no smoothness guarantee; adverse numerics; this is the open content of H-SEL^nu itself | **UNCONSTRUCTIBLE** (O-4) — the live territory |
| **S-6** small-data global class | **YES (global, known)** | **UPPER bound** `S ≤ C_emb M e^{Cε₀²}` (O-1): the class *validates* the head instead of falsifying it | **CLOSED** (O-1) |
| **S-7** **NEW — the C²-escape window: axisymmetric no-swirl families with `‖ω₀/r‖_∞ ~ k → ∞` inside the ball** | **YES — global smoothness is UNCONDITIONALLY known** (Ladyzhenskaya / Ukhovskii–Yudovich [H]) — the only nonperturbative seed satisfying R4 | construction [D]: `H³(ℝ³) ↪ C^{1,1/2}` only — the ball does **not** control `‖∇²u0‖_∞`, so log-concentrated second derivatives give smooth axisym no-swirl Schwartz members with `‖u0‖_{H³} ≤ M`, `D₀^{(k)} := ‖ω₀^{(k)}/r‖_∞ ~ k → ∞` (each member has finite `D₀` by smoothness — the family, not the member, escapes). Known quantitative global bounds control the solution through `D₀` (the `ω_θ/r` maximum principle `‖ω(t)/r‖_∞ ≤ D₀` is the entry point), so the known **upper** bounds on `S(k)` degrade as `k → ∞` — uniformity is genuinely untested here. But **no lower-bound theorem exists**: nothing proves `S(k)` actually grows (`Q₅(0) ≤ C_emb M` still — F-1) | **UNCONSTRUCTIBLE as a certificate; the sharpest live seed** — routed to P-2 as the priority family |

Guard rows: snapshot fields (`u_τ`/`𝒱`-type) solve no PDE — inadmissible by rule and never invoked; Tao averaged solutions — inadmissible (wrong equation), used only as the C-3 method bar; Palasek's family — inadmissible twice over (torus, and not `H³`-bounded); 2-D and planar-invariant flows — not finite-energy Schwartz on `ℝ³`; finite-cylinder — excluded by rule.

## §4 — VERDICT

**SURVIVES.** No falsification certificate for H-SEL^nu is constructible from any commissioned seed with current known mathematics: two seed classes are **provably closed** (S-1/S-6 by the persistence lemmas O-1/O-2 — on them the head *holds* by known math; S-4's scaling and C²-bounded variants by O-2 and the linear amplification cap), and every remaining lane (S-2/S-3/S-5/S-7) hits the double obstruction O-4 — no dynamic lower-bound mechanism exists (consistent with the P-1 empty refutation search), and (except S-7) no smoothness guarantee either. The zoom import is closed (O-3). This is a counterexample-search closure, not an UNDERDETERMINED stall: every commissioned seed was adjudicated to a definite status.

**The falsification frontier is now pinned [D]:** it lies in the **C²-escape window** — families bounded in `H³` but unbounded in `‖∇²u0‖_∞` (equivalently: quantities like `‖ω₀/r‖_∞` in the axisym no-swirl class) — where the ball constraint stops protecting and every known quantitative bound degrades; S-7 is its cleanest representative because member smoothness is unconditionally known there, so the *only* missing ingredient is a growth mechanism (or its absence). Secondary live territory: the S-5 nonperturbative core and dynamically-reached LD geometry (S-3), both additionally blocked by the smoothness-guarantee gap.

## §5 — Consequences (statement-level; commissioning any is a user act)

- **P-2 target refinement:** priority family = **S-7** (axisymmetric no-swirl, `D₀^{(k)} ~ k`, ball-normalized): every member is provably smooth (no blow-up ambiguity contaminates the measurement), the family is cheap to simulate (axisymmetric reduction), and `S(k)` versus `D₀^{(k)}` at fixed `(ν,T,M)` is exactly the uniformity question. Secondary families: S-3 reconnection geometry and quarantine-boundary data (`N ≈ N*(ν,M)`). Whole-space-approximating methodology to be fixed at P-2 commissioning (finite-cylinder runs are evidence-inadmissible as certificates by the standing rule).
- **Proof-route selection input:** O-1/O-2 delimit where any future proof of H-SEL^nu must work (the O(1)-frequency core with the C²-escape window inside); C-1/C-2/C-3 continue to bind; the S-7 window says a proof must in particular handle data with unbounded `∇²` — i.e., cannot lean on any `C²`-datum quantity.

## §6 — Claim boundary

RECORD-ONLY; no Lean edit; no numerics; no proof search on the open core of H-SEL^nu (O-1/O-2 are seed-closure lemmas in known mathematics; their incidental corollary — the head holds on the small-critical sub-ball and up to `T₀(ν,M)` — is standard and was already flagged at EQ-6-level; it is recorded as map information, not as progress on the open statement). **Nothing here asserts, approaches, or implies a resolution of the Navier–Stokes Millennium problem in either direction.** H-SEL^nu remains OPEN on its nonperturbative core; SURVIVES means "no certificate constructible from the commissioned seeds with current known mathematics", not "all falsification scenarios excluded" — the live windows are printed in §4, not hidden. Citations from memory this session ([H]): Fujita–Kato 1964; Bahouri–Chemin–Danchin (product estimates); Ladyzhenskaya 1968 / Ukhovskii–Yudovich 1968 (axisym no-swirl global regularity) — first-hand fetch of the axisym no-swirl quantitative-bound structure is a named debt **V-11** to be discharged at or before P-2 commissioning (it is load-bearing for S-7's design, not for this verdict). C0 discipline: seed statuses are statuses of the fetched-and-derived record; no exclusivity claimed.
