# (COH) winding pass — outcome (ii), a method obituary, and a correction to frozen text: the debt is TWO objects, (COH-Δ) + (ANCH)

Date: 2026-08-21 (JST). One bounded in-house pass (3 Opus analysts (derive / estimate
/ adversary) + 2 independent verifiers (mathematics lens / dictionary-compliance
lens) + critic; stop rules clean — full audit in §8). Baseline:
`BH_NECK_OMEGA_BUDGET_2026-08-20.md` §8 (the critic-designated task). Tags: [H]
hard/textbook, [D] derived this pass (independently re-derived), [V] verified
first-hand, [C] conditional, [C-dict] dischargeable inside the frozen dictionary,
[B] bookkeeping.

## 1. RULING: outcome (ii) — and the frozen (COH) "iff" is WRONG

**(COH) is not discharged at any radius. The winding form is RETIRED as a discharge
vehicle for (E⁺⁺). (E⁺⁺) stays [C]; no [C] → [C-dict]; R-B2′ remains conditional on
(E⁺⁺)[C] + (P)[C] + (N-Γ)[C-dict] — unchanged. No reversal: no dictionary-admissible
history reaching (NECK) was produced** (W6 is a flagged extension, §5).

**The pass's headline [D, critic]: the INCREMENT/LEVEL GAP.** The winding form — and
equally the production budget it reproduces — bounds only `|κ̂(t) − κ̂₀|`. (E⁺⁺)
needs `|κ̂(t)| ≤ C`. On the ancient limit — the object the ledger actually charges —
there is no `t₀` and no anchor; in the pre-limit an anchor exists only at
`τ′ = O(1)` and then charges the whole unassigned inward journey. **Therefore
`θ_coh(ρ) ≥ 2ρ` throughout the neck would NOT deliver (E⁺⁺):** the neck-budget
report's "(E⁺⁺) is delivered iff θ_coh(ρ) ≥ 2ρ" is REFUTED in its ⟸ direction, and
F11 as printed is superseded (F16).

**The branch's debt is therefore TWO named objects, not one:**

- **(COH-Δ)** — the coherence exponent exactly as previously printed (threshold
  `θ_coh(ρ) ≥ 2ρ`; reference horizons: turnover `ρ+γ` clears ν-free, viscous `2ρ`
  exactly marginal, lifetime `1` fails everywhere on the neck by K11) — now
  **necessary but NOT sufficient**;
- **(ANCH)** — a *level/anchor* statement: at some time in the (ancient) history the
  rescaled `κ̂` over a dyadic meridional annulus at every neck radius is `O(1)`; or
  equivalently an erasure exponent for pre-existing `κ̂`, which by R-NEG4 (no bulk
  sink) can be carried **only by T4**. The dictionary's only level bound at neck
  radii is the contour form `|κ̂| ≤ ‖û‖_∞|∂R̂| ≤ CR` — **exactly (NECK) level** —
  and F5 bars improvement (the whole neck is inside the core).

**Method obituary, two strengths kept separate (load-bearing distinction):**
(a) [D, structural, unconditional] an *increment* identity cannot discharge a
*level* hypothesis at any radius, under any TV bound whatsoever, without (ANCH);
(b) [C-dict, input starvation] even as an increment bound it needs residence,
confinement, fold count (`TV_s(r)`/`N` — witness W4), and the material preimage of a
Eulerian annulus — four unassigned Lagrangian inputs — plus the unestimated T4. It
is **not** proved impossible: the one sharpness claim submitted (TV `t`-linearity)
was itself refuted (§4, row 3).

## 2. What the winding form delivered (all [C-dict] or [D], twice-derived)

1. **The exact identity.** `u·dl = u_pol·dl + Γ dθ` pointwise; Kelvin + winding
   number 0 (axis invariance keeps `r > 0`, the integer winding is continuous in t,
   zero at t₀) give
   `κ_pol(t) = κ₀ − ∮_{C(t)}Γ∂_sθ ds + ∫₀ᵗ T4 dt′ + ν∫₀ᵗ∮(Δ−(2/r)∂_r)Γ dθ dt′`,
   and with midrange `Γ̄`: `|κ_pol(t) − κ₀| ≤ ½ osc_loop(Γ)·TV_s(θ(t))`.
   **The second viscous term cancels on differentiation against
   `DΓ/Dt = ν(Δ−(2/r)∂_r)Γ`, returning `dκ_pol/dt = ∮(Γ²/r³)dr + T4` exactly** —
   the baseline's "T4 is the only viscous entry" stands unrefuted; **R-NEG4
   re-verified first-hand by a fifth route**. No new hole in the frozen budget.
2. **Exponent content: nothing new.** From dictionary lines
   (`osc_loop(Γ) ≤ 2Γ₀`, `θ̇ = Γ/r² ≤ Γ₀τ^{−2ρ}` under confinement):
   `κ̂ ≤ κ̂₀ + CΓ₀²T̂/R²` — the production budget **power-for-power** with a worse
   constant (C ∈ {2, 11/4, 7/2} vs 3/8, all conditional on an unassigned fold
   count [B]). The threshold `θ_coh ≥ 2ρ` is **reproduced, not discharged**;
   **R-NEG3 is independently reconfirmed by a second route.**
3. **Failure is strict at every radius** with the only horizon the dictionary
   permits (`θ = 1`; it lower-bounds no `|u_r|`): at `ρ_in = (γ+α)/2` the bound is
   `Γ₀²τ^{1−(γ+α)} → ∞` (K11; `O(1)` only on the K10 edge `γ+α = 1`); at
   `ρ* = (1+γ)/3` it is `Γ₀²τ^{(1−2γ)/3} → ∞` for every `γ > 1/2`; at `ρ = γ` it is
   `Γ₀²T̂ → ∞`. **The residence-free sub-wedge of S_blob is empty** (2ρ ≤ 1 needs
   γ ≤ 1/2). Estimate's structural remark, worth printing: the budget's *margin* is
   worst at the outer end `ρ = γ`, while the *consequence* of failure is worst at
   the inner end (damage `R_k`) — different radii.

## 3. The two task hints, definitively closed (both negative, [C-dict])

- **osc hint VACUOUS by dictionary arithmetic:** on the neck
  `r ≥ √42Γ₀τ^γ ⟹ rτ^{−γ} = R ≥ √42Γ₀ > Γ₀`, so `min(Γ₀, rτ^{−γ}) = Γ₀`
  **identically** — the local amplitude cap is inactive at every neck radius
  ("sub-saturated" *means* exactly this). Inviscid material conservation and the
  source-free Γ maximum principle both preserve exactly the assigned cap `2Γ₀`
  (local osc may grow toward, never past it). **Zero exponent bought.** C8 blocks
  the Γ-band alternative. Retire the hint (F17).
- **Cumulative-free hint real but INERT:** the inequality genuinely is
  instantaneous (no time-integral of production) — but `TV_s(θ(t))`, the current
  total angular sweep of the material loop, is an **unassigned Lagrangian geometric
  quantity with no maximum principle**. The sign-coherence debt is converted into a
  geometric debt of equal size — same ledger, different currency. (Verifier B's
  framing, adopted; "merely relocated" understates, "illusory" overstates.)

## 4. Contradictions adjudicated (critic; 11 rows)

| # | contradiction | ruling |
|---|---|---|
| 1 | viscous defect: two terms (derive) vs one (estimate, adversary) | derive's algebra CONFIRMED, its "previously untracked / new hole" editorial REFUTED — the second term cancels (§2.1) |
| 2 | adversary's "DECISIVE METHOD KILL: identity inviscid-only, validity window `τ^{2ρ}/ν`" | **REFUTED** — with both defect terms the identity is exact for all t; `νκ/ℓ²` sizings re-litigate C3 (NOT DERIVED) and consume the unassigned neck gradient scale `ℓ` |
| 3 | derive's "t-linearity of TV_s(θ) is SHARP" | **REFUTED as [D] → [C]** — `osc_s(∫f) ≥ t·osc_s(f)` presumes time-coherence of the spatial pattern, which *is* (COH); hence no impossibility theorem, obituary rests on starvation |
| 4 | estimate's "the single separating input is `\|u_r\| ≳ ν/r`" | **REFUTED twice** — direction backwards (that would *shorten* residence), and magnitude-only is insufficient: inward excursions raise the integrand, so confinement is a logically separate input |
| 5 | adversary's "for `ρ < ρ*` persistence is FORCED" | **REFUTED** — inverts a one-sided upper bound, omits T4, assumes the fold count it elsewhere declares unassigned |
| 6 | osc grows viscously (derive) vs `osc ≤ 2Γ₀` always (adversary) | NOT a contradiction — growth capped at the global value; identical operational content |
| 7 | constants 7/2 / 11/4 / 2 vs baseline 3/8 | [B] immaterial; exponent unanimous |
| 8 | estimate's "refutes (NECK) iff ρ<ρ*" and adversary's M3 sub-band discharge | **REFUTED as positives** — increment-only, both silently carry `κ̂₀ ≤ C` undischarged; and (COH) must hold *throughout* while `ρ = γ` fails always |
| 9 | `κ̂ ≤ τ^{2ρ−γ−α} ≤ 1 ⟺ ρ ≥ (γ+α)/2` | CONFIRMED as the *definition* of the neck's inner endpoint; **REFUTED as an anchor** — the per-region gradient scale is a region descriptor, not a pointwise `ω_θ` bound (R-NEG6) |
| 10 | the frozen (COH) "iff" | **REFUTED (⟸ direction)** — the increment/level gap (F16) |
| 11 | Lagrangian↔Eulerian mismatch (found independently ×3) | **CONFIRMED [D]** with the circularity sharpening: the material-loop ⟷ Eulerian-annulus transfer is licensed by the amplitude cap only for `t ≲ τ^{ρ+γ}` — the turnover horizon, which already clears the threshold; at `t = τ^{2ρ}` the displacement bound is `Rτ^ρ ≫ τ^ρ`; over the epoch `τ^{1−γ} ≥ τ^α` (K11) exceeds the whole core radius. **Zero localization** (F18) |

## 5. Persistence (adversary channel C) — ruled explicitly

**Is "(NECK)-level `κ̂` present initially and persisting" excluded? NO. Is it
forced? NO** (row 5). It needs an unassigned object, and that object is **NEW — it
is not (COH) in material form**: (COH) is silent on the level. With no bulk sink
(R-NEG4), the only ways to have large `κ̂` are (a) build it — governed by (COH-Δ);
(b) always have had it — governed by **nothing in the dictionary**; that gap is
(ANCH). **Does it re-open R-B2? No — a precise no:** R-B2 is discharged
*conditional on (E⁺⁺)*, and (NECK)-unexcluded is the standing state; persistence
adds no new re-opening. What it does is **enlarge what a discharge must deliver**
from one object to two and invalidate the claim that (COH) alone suffices.
Symmetric caution preserved: a one-sided bound failing to close, plus the absence
of a destruction mechanism, is **not** evidence for (NECK). Witnesses: **W4**
(fold/run count of a material meridional loop), **W6** (initial-loading witness;
its single honest dictionary extension `ω_θ ≈ τ^{−γ−ρ}` vs the core's `τ^{−γ−α}`
recorded, not laundered; energy/L³ shares `τ^{3(ρ−α)} → 0` check out — no V1
consumed). W5 undefined — renumber at the freeze review (F20).

## 6. Vehicles that remain (flagged as structure, NOT commissioned)

Not a budget — every increment route inherits the increment/level gap. Outside this
pass: (a) **T4 with a sign** — a viscous erasure statement for pre-existing `κ`;
any sizing consumes the neck gradient scale `ℓ` (`β` for M3), which is unassigned;
(b) a **level route** via the `ω_θ/r` transport (source-free it would be a maximum
principle anchoring the whole neck) — needs an assigned pointwise `sup(ω_θ/r)` and
control of the `r^{−4}`-weighted source.

## 7. Freeze-review queue additions (exact wordings, continuing from F14)

**F15.** *The winding form, and its retirement as a discharge vehicle.* "`u·dl =
u_pol·dl + Γdθ` is pointwise exact; Kelvin plus winding number 0 on a loop
meridional at `t₀` gives `κ_pol(t) = κ₀ − ∮Γ∂_sθ ds + ∫T4 + ν∫∮(Δ−(2/r)∂_r)Γ dθ`,
whose second viscous term cancels on differentiation against
`DΓ/Dt = ν(Δ−(2/r)∂_r)Γ`, returning `dκ_pol/dt = ∮(Γ²/r³)dr + T4` — **the neck
budget's viscous ledger is complete as printed (R-NEG4 re-verified)**. With `Γ̄`
the midrange, `|κ_pol(t)−κ₀| ≤ ½ osc_loop(Γ)·TV_s(θ(t))`; from dictionary lines
this yields `κ̂ ≤ κ̂₀ + CΓ₀²T̂/R²`, the production budget power-for-power with a
worse constant, hence the threshold `θ_coh ≥ 2ρ` reproduced and not discharged.
**Retire the winding form as a discharge vehicle for (E⁺⁺)**, structurally (F16)
and for input starvation (residence, confinement, fold count `TV_s(r)`/`N`,
material preimage). Do not print it as *impossible*: no sharpness theorem was
established." [D]

**F16.** *(COH) amended — the increment/level gap; F11 superseded.* "Budget and
winding routes bound only `|κ̂(t) − κ̂₀|`; (E⁺⁺) requires `|κ̂(t)| ≤ C`. On the
ancient limit there is no anchor time, and the dictionary's only level bound at
neck radii is `|κ̂| ≤ CR` (contour form; F5 bars improvement) — i.e. exactly
(NECK). **The frozen 'iff' is therefore wrong: `θ_coh(ρ) ≥ 2ρ` throughout the neck
is NECESSARY but NOT SUFFICIENT for (E⁺⁺).** The branch's debt is two objects:
**(COH-Δ)** — the coherence exponent as previously printed, threshold
`θ_coh(ρ) ≥ 2ρ`, reference horizons turnover `ρ+γ` (clears, ν-free), viscous `2ρ`
(marginal), lifetime `1` (fails everywhere by K11) — **and (ANCH)** — a
level/erasure statement for `κ̂` at neck radii, carried by `T4` alone since there
is no bulk sink. Either failing re-opens R-B2 at rate `R = τ^{ρ−γ}`." [D]

**F17.** *`osc_loop(Γ)` hint retired.* "At every neck radius
`r ≥ √42Γ₀τ^γ ⟹ rτ^{−γ} = R ≥ √42Γ₀ > Γ₀`, so `min(Γ₀, rτ^{−γ}) = Γ₀`
identically: the local amplitude cap is inactive on the whole neck and no smallness
of `osc_loop(Γ)` is assigned. Inviscid material conservation and the source-free Γ
maximum principle both preserve exactly the assigned cap `2Γ₀` and buy zero
exponent. C8 blocks the Γ-band alternative." [C-dict]

**F18.** *No Lagrangian localization.* "Material-loop statements transfer to the
ledger's Eulerian dyadic annulus only for `t ≲ τ^{ρ+γ}` (amplitude cap:
displacement `≤ τ^{−γ}t ≤ τ^ρ`) — the turnover horizon, which already clears the
threshold; at `t = τ^{2ρ}` the displacement bound is `Rτ^ρ ≫ τ^ρ`, and over the
epoch `τ^{1−γ} ≥ τ^α` (K11) exceeds the whole core radius. Any Lagrangian
discharge of (E⁺⁺) is circular unless flow-map distortion is assigned." [D]

**F19.** *Negative rows.* "**R-NEG5**: the winding identity has **no viscous
validity window** — with both defect terms it is exact for all `t`; sizings of the
form `νκ/ℓ²` re-litigate C3 and consume the unassigned neck gradient scale `ℓ`.
Arguments retiring the method on a window are void. **R-NEG6**: the per-region
gradient scale (`τ^α` for the core) is a region descriptor, **not** a pointwise
bound on `ω_θ` inside the region (multiregion §1; §3 records the `ω`-sup relocating
off the core). `κ̂ ≤ τ^{2ρ−γ−α}` defines the neck's inner endpoint
`ρ = (γ+α)/2`; it is **not** an anchor for (ANCH)." [D]

**F20.** *Legend and hygiene.* "`T4` is unestimated after six passes and is now the
sole carrier of (ANCH); any sizing requires the neck gradient scale `ℓ` (`β` for
M3) — print it as a **named unassigned input**, not as a free parameter. Witness
numbering: `W4` and `W6` were introduced with `W5` undefined — renumber. Two
competing `F15` drafts were submitted this pass; this queue's `F15` supersedes
both. `C10`: fix `R₀` at one of `√42Γ₀` / `10√42Γ₀`." [B]

## 8. The single next bounded task (critic-designated): FREEZE REVIEW

The standing order holds and is **reinforced**. The one competing candidate
("estimate T4 on the neck") cannot be commissioned honestly: any T4 sizing consumes
the unassigned `ℓ` and would return outcome (ii) by construction, a third time.
More decisively, this pass produced a **correction to frozen text** (F16), and by
the multiregion precedent such corrections are freeze-review business.

**Task:** convene the freeze review on the (E⁺⁺)/(NECK) branch with agenda F15–F20
(within the full accumulated agenda) and rule on one question: **does the frozen
dictionary assign the neck a poloidal exponent (equivalently, a level bound on `κ̂`
over a dyadic meridional annulus at `r = τ^ρ`), or does it decline?**

- **Fork (α) — it assigns one:** (ANCH) is discharged at [C-dict]; the branch
  reduces to (COH-Δ) alone; next bounded task = the coherence estimate with a
  *stated* anchor.
- **Fork (β) — it declines:** print (NECK) as a standing dictionary-extension
  request with **two** named unassigned inputs, `θ_coh(ρ)` and `ℓ`; R-B2′ stays [C]
  on (E⁺⁺)+(P)+(N-Γ); the branch's cheapest remaining in-house move is then the
  level route (`ω_θ/r` maximum principle, §6(b)) — commissioned only *after* the
  review supplies `sup(ω_θ/r)` or records that it too is declined.

No V1 stand-down at either fork; no in-house Liouville at either fork.

**Stop-rule audit (whole pass):** no in-house Liouville (only Kelvin, the
source-free Γ maximum principle, Stokes — textbook, hypotheses stated); no
numerics (explicit rationals only; constant spread [B], exponents unanimous); no
new ansatz except W6, flagged, its one extension recorded; no map edits; no files
written by agents; no V1 stand-down (tongue energy share `τ^{2(γ−α)} → 0` is why
none was needed); no promotions by fiat — six analyst/verifier claims demoted or
refuted and recorded (§4), none silently repaired. Trap audit clean ×5 on F5
(no poloidal bound better than `|u| ≤ τ^{−γ}` used on the neck), C8, K11. One
residual slip recorded: derive's sharpness construction was not flagged as a
witness — harmless (argues the negative) and refuted anyway.

Sources read first-hand this pass: `BH_NECK_OMEGA_BUDGET_2026-08-20.md`,
`BH_BIOT_SAVART_LEDGER_2026-08-20.md`, `BH_MULTIREGION_AUDIT_2026-08-20.md`,
`BH_CONSTANT_EXCLUSION_ROW_2026-08-20.md`, `TYPE2_KILL_TABLE_2026-08-19.md`.

## Erratum (2026-09-01, synthesis audit — appended, not silently repaired; the F15/F16 retirement rulings are unaffected)

**E1 (§2 item 1, the midrange inequality; repeated in F15; transcribed as kill-table
A19's parenthetical): the printed inequality drops two terms of its own identity.**
The midrange/oscillation bound controls **only the winding term**: with winding
number 0, `|∮Γ∂_sθ ds| = |∮(Γ−Γ̄)∂_sθ ds| ≤ ½ osc_loop(Γ)·TV_s(θ)`. Rearranging the
identity printed immediately before it,
`κ_pol(t) − κ₀ = −∮Γ∂_sθ ds + ∫₀ᵗT4 dt′ + ν∫₀ᵗ∮(Δ−(2/r)∂_r)Γ dθ dt′`, so as printed
`|κ_pol(t) − κ₀| ≤ ½ osc_loop(Γ)·TV_s(θ(t))` is false unless the `∫T4` and viscous
Γ-defect entries vanish — and neither may be dropped: the ν-term's cancellation
lives only under `d/dt` (F19/R-NEG5; §4 row 2), and T4 is unsigned and unestimated
after six passes (F20). **Corrected form [D]:**

> `|κ_pol(t) − κ₀ − ∫₀ᵗ T4 dt′ − ν∫₀ᵗ∮(Δ−(2/r)∂_r)Γ dθ dt′| ≤ ½ osc_loop(Γ)·TV_s(θ(t))`

— equivalently: the oscillation bound controls the winding term alone; the T4 and
viscous-defect entries must be carried separately, additively. §1(b)'s starvation
list already names T4 as an unassigned input, so this is a mis-transcription in the
frozen wording, not a hidden logical hole; the retirement of the winding form as a
discharge vehicle (F15/F16) is unaffected — strengthened, if anything, since even
the increment bound carries the unestimated T4 additively. Kill-table A19's
parenthetical is queued as F43.

**E2 (§1 (COH-Δ) restatement and F16's frozen wording: "lifetime `1` fails
everywhere on the neck by K11").** Non-strict K11 delivers only `ρ ≥ 1/2` at the
neck's `ρ = (γ+α)/2` endpoint; at `ρ = 1/2` on the `γ+α = 1` edge (in-window
exactly for `γ ∈ (1/2, 3/5]`), `θ = 1 = 2ρ` is exactly marginal — the same status
the sentence assigns the viscous horizon — not failing. §2 item 3's own "`O(1)`
only on the K10 edge `γ+α = 1`" already records the exception. Corrected clause:
"lifetime `1` fails everywhere on the neck by K11 except exactly-marginal at
`ρ = 1/2` when `γ+α = 1`". Origin erratum: `BH_NECK_OMEGA_BUDGET_2026-08-20.md`
(appended there); kill-table A16 queued as F42. No result moves.
