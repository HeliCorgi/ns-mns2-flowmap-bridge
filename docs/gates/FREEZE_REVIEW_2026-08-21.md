# FREEZE REVIEW — fork (β): the dictionary declines the neck level bound; accumulated agenda executed

Date: 2026-08-21 (JST). **This is the pen-holding pass on the frozen map**, executed
under the user's explicit fork-(β) adjudication (received 2026-08-21, verbatim rulings
transcribed in §2). Scope: the full accumulated agenda — multi-region audit §9 (items
1–8) + M3×KNSS gate §4 + constant-exclusion §4 + Biot–Savart F1–F8 + neck-budget
F9–F14 + winding F15–F20. Edits applied: this record + one authorized annotation
appended to `TYPE2_KILL_TABLE_2026-08-19.md`. Nothing else is touched; prior reports
keep their history (superseded lines are superseded *here*, not rewritten there).

## 1. The decisive ruling: fork (β)

> **The frozen dictionary does NOT assign the neck a poloidal level bound on `κ̂`
> (nor a pointwise `sup(ω_θ/r)`, nor a neck gradient scale `ℓ`).**

Grounds (user's, confirmed against the record): every audit that touched the point
says so — the Biot–Savart pass *defined* (NECK) as introducible only by dictionary
extension; the winding pass confirmed the dictionary's only level bound at neck radii
is the contour form `|κ̂| ≤ CR`, i.e. exactly (NECK) size; and R-NEG6 rules that a
region's gradient scale is not a pointwise `ω_θ` bound. Choosing fork (α) would have
meant **inventing a new assumption at the review to manufacture a kill** — excluded
by the program's own policing standard. Consequently `sup(ω_θ/r)` is also declined
(same after-the-fact objection), which blocks the `ω_θ/r`-maximum-principle level
route as an in-house move.

**Post-freeze state of the Scope-A quasi-static branch (frozen):**

> **R-B2′ kill [C]**, conditional on **(E⁺⁺) + (P) + (N-Γ)**, with (E⁺⁺) printed in
> fully decomposed form: **(E⁺⁺) = (COH-Δ) + (ANCH)** (κ-form, F12+F16).
> **(NECK) = standing dictionary-extension request** with exactly **two named
> unassigned inputs: `θ_coh(ρ)` and `ℓ_neck`**. `δ_T` deleted (refuted, three
> routes). T4 is the sole known carrier of (ANCH). No kill is inflated.

## 2. Adjudication table (user's rulings, applied)

| # | agenda item (source) | ruling | execution |
|---|---|---|---|
| 1 | `σ_core` / `σ_sup` separation (multiregion §9.1) | **ADOPT** | annotation §A1 |
| 2 | K9 → Γ-saturation trichotomy (multiregion §9.2) | **ADOPT** | annotation §A2 |
| 3 | K6 [D2] location premise + region-count "≥3 → minimum 2" (multiregion §9.3) | **ADOPT** | annotation §A3 |
| 4 | Prop G/V global-`s` misuse → per-level + core-Markov repair (multiregion §9.4) | **ADOPT** | annotation §A4 |
| 5 | P6 numbers → `7/16`, `21/64`; falsifier PASS, trigger does not fire (multiregion §9.5) | **ADOPT** | annotation §A5 |
| 6 | K4 restore (multiregion §9.6 / M3×KNSS §4.1) | **ADOPT [V]** — restored at [V] (first-hand Thm 6.1, rate-free), with the **mandatory rider**: applies only via amplitude-normalized zoom → ancient limit → KNSS; never to the unzoomed flow (`K₄ ∩ M3 = ∅` as sets of unzoomed candidates) | annotation §A6 |
| 7 | `S_blob` boundary `α > 2γ−1` strict (multiregion §9.7) | **ADOPT** | annotation §A7 |
| 8 | DVEP / geometry-gate §10 trigger (multiregion §9.8) | **DO NOT FIRE; AMEND** — trigger wording restricted to a non-localizable witness **inside the primary class `A_NS`** (DVEP is piecewise-smooth, outside the primary C¹ class) | annotation §A8 |
| 9 | K4′ (M3×KNSS §4.2, amended constant-exclusion §4.1) | **RETAIN, amended to (E⁺)+(P)** | annotation §A9 |
| 10 | K4″ / Lei–Zhang BMO row (constant-exclusion §4.2) | **RETAIN as redundant confirmation only** — not a primary kill | annotation §A10 |
| 11 | K4‴ — `Γ` exact scale invariance (constant-exclusion §4.3) | **ADOPT [D, unconditional]** | annotation §A11 |
| 12 | P5 elongated filament (M3×KNSS §4.4) | **RECORD as auxiliary corollary** — not a main map row | annotation §A12 |
| 13 | P7 energy / `Γ ∈ L^p` scaling legend (M3×KNSS §4.5 + constant-exclusion §4.4) | **ADOPT** | annotation §A13 |
| 14 | K2 KNSS attribution correction (M3×KNSS §4.6) | **ADOPT** | annotation §A14 |
| 15 | M3 class verdict: OPEN → RESTRICTED, no independent members (M3×KNSS §4.3) | **ADOPT** — `γ₂ < γ` dies by K4′ [(E⁺)+(P)]; the tie face `γ₂ = γ` is a B2 point wearing a Γ-saturated sub-core; the BH Scope-A open set contracts to B2 alone | annotation §A9 + post-freeze frontier block; record §5 |
| 16 | F1 — R-B1 "one log of slack" withdrawn (ledger §5) | **ADOPT** | annotation §A10 |
| 17 | F2 — R-B2′ primary row (ledger §6) | **ADOPT, tag stays [C]** | annotation §A15 |
| 18 | F3 — old pointwise (E⁺⁺) wording (ledger §4) | **DISCARD; replaced by F12 κ-form** | annotation §A16 |
| 19 | F4 — old (NECK) wording (ledger §4) | **UPDATE per F16/F20** (two named inputs) | annotation §A16 |
| 20 | F5–F8 (ledger §6: exterior-tail correction; poloidal-kernel legend; `b_τ` axial by derivation; ball radius = (E⁺) radius, time-uniformity = `2γ−1 > 0`) | **ADOPT all** | annotation §A17 |
| 21 | F9–F10 (neck budget: production form; `δ_T` refuted/retired) | **ADOPT** | annotation §A18 |
| 22 | F11 — "(COH) iff" (neck budget) | **DISCARD; explicitly superseded by F16** (its non-superseded K11 clause — whole neck inside `√(ντ)` — is retained in §A16) | annotation §A16 |
| 23 | F12 — κ-form (E⁺⁺) (neck budget) | **ADOPT** | annotation §A16 |
| 24 | F13–F14 (neck budget: R-NEG3/R-NEG4; T4 legend, `R₀`) | **ADOPT** (R₀ resolved record §6.2) | annotation §A18 (F13); §A19+§A20 (F14) |
| 25 | F15 — winding form retired as discharge vehicle (winding pass) | **ADOPT** | annotation §A19 |
| 26 | F16 — increment/level gap; (E⁺⁺) = (COH-Δ)+(ANCH) (winding pass) | **ADOPT — the most important correction of the review** | annotation §A16 |
| 27 | F17 — osc hint retired (winding pass) | **ADOPT** | annotation §A19 |
| 28 | F18 — no Lagrangian localization (winding pass) | **ADOPT** | annotation §A19 |
| 29 | F19 — R-NEG5/R-NEG6 (winding pass) | **ADOPT** | annotation §A18 |
| 30 | F20 — T4/`ℓ` named unassigned inputs; witness renumber; F15-conflict; `R₀` (winding pass) | **ADOPT** | annotation §A19 (T4/`ℓ_neck`) + §A20 (witness renumber, F15-conflict, `R₀`); record §6.1–§6.3 |
| 31 | Constant-exclusion residuals: KNSS implicit-(6.16) footnote (§4.5); negative rows R-NEG1/R-NEG2 + "constants are genuine members" legend (§4.6); branch wording "M3 tie face and B2 reduce to one object (OO); branch (a) retired as an exact-maximizer artifact" (§4.7) | **ADOPT** (recordings only; no kill content; under the general adoption principle — no objection raised, nothing invented) | annotation §A20 |

## 3. The frozen wordings, as they now stand

**(E⁺⁺)** [C] — *final form (F12 + F16)*: within the ledger ball, the neck's poloidal
circulation over any dyadic meridional annulus obeys `λ_y ≤ C/R` (equivalently `κ̂ ≤ C`),
τ- and R-uniformly. Decomposition (both required):
- **(COH-Δ)** — increment control: the coherence exponent `θ_coh(ρ)` with threshold
  `θ_coh(ρ) ≥ 2ρ` throughout `ρ ∈ [(γ+α)/2, γ]` (reference horizons: turnover `ρ+γ`
  clears ν-free; viscous `2ρ` exactly marginal; lifetime `1` fails everywhere by K11).
  **Necessary but not sufficient.**
- **(ANCH)** — level/anchor control: a level or erasure statement for `κ̂` at neck
  radii; by R-NEG4 (no bulk sink) its only known carrier is **T4**, whose sizing
  consumes the unassigned `ℓ_neck`.

**(NECK)** [named object] — *final form*: a poloidal shear structure of full amplitude
order (`λ_y = O(1)` rescaled) on any dyadic meridional annulus at rescaled radius
`R → ∞`, `R ≤ min(R*, R_k)`, riding the sub-saturated neck. **Standing
dictionary-extension request** with exactly two named unassigned inputs:
`θ_coh(ρ)` and `ℓ_neck`. Admissible as a negation witness; inadmissible as a class
member. `δ_T` is **deleted** from its description (refuted by three independent
routes, F10).

**R-B2′** [C] — *primary kill row (unchanged in strength, clarified in condition)*:
re-centred Prop-6.1/Lemma-6.1 zoom on the Γ-saturated level + Γ-max + (N-Γ)
[C-dict] + axial boost (derived) + the Biot–Savart deviation ledger under (E⁺⁺) ⟹
`|w| ≤ C/r` globally on the ancient limit ⟹ KNSS Thm 5.3 [V] ⟹ `w ≡ 0` ⟹
`v = b·e_z` ⟹ `Γ ≡ 0`, contradicting the Γ-saturated core ⟹ **M3's tie face and
B2's Γ-saturated blob excluded** — no Type-I rate, no viscosity, no energy, no
Scope-A geometry, no V1. Conditional on **(E⁺⁺) = (COH-Δ)+(ANCH)**, **(P)**,
**(N-Γ)**.

**R-B1 (Lei–Zhang) = map row K4″** (two names for one row: R-B1 in the
constant-exclusion pass, K4″ on the map) — demoted to **redundant confirmation**:
kept on the map, not a primary kill; its "one logarithm of slack" sizing is
withdrawn (BMO-stream ⟺ `w_pol ∈ BMO^{−1}`, zero logarithms, F1/A10).

**K4 (KNSS `|v| ≤ C/r` Liouville)** — **restored at [V]** (Thm 6.1 first-hand:
rate-free, Type-II-valid), with the mandatory rider: it acts only through the
amplitude-normalized zoom → ancient limit → KNSS route; it never applies directly to
the unzoomed flow. The 2026-08-19 retirement rationale ("K3's `r^{−1+ε}` family
covers it") is recorded as **false** — the `ε = 0` endpoint is not covered.

**Geometry-gate §10 CAP-revisit trigger (DVEP)** — amended in its Scope-B limb
only, not fired: "Scope B opens — **localizability shown non-necessary inside the
primary class `A_NS`**, or a **non-localizable witness inside `A_NS`** appears."
The other two limbs stand unchanged (ring corollary fails verification; P6
falsifies the one-parameter reduction — now settled PASS, A5). DVEP
(arXiv:2005.04380) stays recorded as the Scope-B witness that `p = p(ψ)` necessity
is false without regularity hypotheses — outside the primary C¹ class, hence no
fire and no CAP trigger.

## 4. What the review did NOT do (fork-β discipline)

- **No neck poloidal level bound assigned.** (ANCH) remains open; the branch's level
  debt is not papered over.
- **No `sup(ω_θ/r)` assigned** — the `ω_θ/r` maximum-principle level route is
  thereby **not commissioned** (it would be the same after-the-fact invention;
  R-NEG6).
- **No `ℓ_neck` assigned** — T4 remains unestimated, printed as a named unassigned
  input, not a free parameter.
- **T4-with-a-sign not commissioned** — the viscous-erasure vehicle (winding §6(a))
  is blocked by the same declination: any T4 sizing consumes the unassigned
  `ℓ_neck` and would return outcome (ii) by construction (winding §8).
- **No kill inflation**: M2/M3/B2 verdicts, S_blob, and every [C] tag carry exactly
  the strength their sources verified. R-B2′ stays [C]. K4″ stays a redundant
  confirmation. P5 stays a corollary.
- **No report rewritten**: superseded lines (F3, F4 old forms, F11, the neck-budget
  "iff", the 2026-08-19 K4 retirement) are superseded by this record and the
  annotation; the original files keep their text and their errata trails.

## 5. Consequences for the branch verdicts (frozen)

- **Scope-A quasi-static landscape**: one-scale localizable ring — excluded
  (annotated 2026-08-19); M2 — RESTRICTED, no independent members; M3 — RESTRICTED
  (`γ₂ < γ` dies under K4′ [(E⁺)+(P)]; tie face merges into B2); **everything
  reduces to B2**, and B2's Γ-saturated blob + the tie face reduce to **one
  literature-named open object (OO)** under the re-centred zoom, killed by R-B2′
  conditional on (E⁺⁺)+(P)+(N-Γ). BH verdict: **YELLOW-RED maintained** (no
  unconditional kill claimed; no CAP triggered).
- **The conditional frontier is now exactly three named objects**: (COH-Δ), (ANCH)
  [together = (E⁺⁺)], and (P) — plus (N-Γ) at [C-dict].
- Deliberately deferred (unchanged): intra-core `β_v ∈ (α,γ]` branch; K8/K10 on
  multi-region tuples; coexistence/one-pressure vocabulary limit; Jiu–Xin body;
  separatrix continuation lemma; ζ-averaging / K12″; formal-side optional
  refinements. (Scope-B reconnaissance is **not** in this list — it is a live
  handoff option, §7, and is strictly cheaper now that the trigger wording is
  fixed, A8.)

## 6. Hygiene items executed

1. **Witness renumbering**: W1 (stacked-slab forcing), W2 (outward evacuation of
   `κ_tip`), W3 (truncated neck sheet at `R*`), W4 (fold/run count of a material
   meridional loop), **W5 (initial-loading witness — formerly W6; renumbered)**.
   No gap remains.
2. **`R₀` / C10 resolved as two distinct objects, both kept** — a reasoned
   departure from F14/F20's "pick one": ledger §1 defines the ball radius
   `R₀ = 10√42Γ₀`, ledger §4 defines the neck span `r_y ∈ [√42Γ₀, R_k]`; they were
   conflated by the neck-budget task text; no inconsistency — the ledger's
   automatic zone `|w| ≤ 2√42` inside `R₀` covers `[√42Γ₀, 10√42Γ₀]`. [B]
3. **F-numbering conflict resolved**: two competing F15 drafts were submitted in
   the winding pass; the winding report's §7 queue is the numbering of record —
   its F15 (winding identity + retirement as discharge vehicle) supersedes both
   drafts. F15–F20 as printed in `BH_COH_WINDING_2026-08-21.md` §7 are canonical.
   [B]
4. **KNSS footnote**: their receding-axis branch uses Thm 5.1 + Rem 6.1 **plus
   (6.16)** (rescaled Type-I) — the last step implicit in the paper. [V]
5. Legend: `T4` = unsigned viscous boundary flux, unestimated after six passes,
   sole carrier of (ANCH); the poloidal kernel sees `ω_θ` only; `∫ω dV ≡ 0`; the
   `r′²` axis weight. Negative rows R-NEG1–R-NEG6 collected in the annotation.

## 7. Next after the freeze

The Scope-A quasi-static branch is **parked at its frozen conditional state** —
its remaining content is dictionary-extension requests, not in-house estimates
(all three in-house vehicles are retired or blocked: budget/winding by F15–F18;
the level route by fork-β's declination of `sup(ω_θ/r)`; T4-with-a-sign by the
declination of `ℓ_neck`, §4). Options, in the standing
order of the handoff: **Scope-B reconnaissance** (recorded runner-up), or return
to the **formal (Lean) side** (the 5 Clay semantic-promotion edges; the glued
maximal trajectory `u*`). The choice is the user's; no default is asserted here.
